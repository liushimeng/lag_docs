# 虚拟城市 · P1 扩展：真实经济循环引擎（消费品市场 + 内生 CPI + 劳动力市场 + 社会结构）

> **版本**：v1 ｜ **日期**：2026-09-16 ｜ **game_kind**: `wealth`
> 本文件是 P1 第二期的**实现契约**——打通「家庭消费 → 企业营收 → 劳动需求 → 工资 →
> 收入 → 消费」的财富循环流，使 CPI 与失业率**内生化**。
> 上游设计来源：《虚拟城市总体设计》§6（圈层）、《虚拟城市规则与经济系统》§7；
> P1 前序：央行信用创造引擎 / 明斯基与 LPR 引擎（同目录）。
> 实现以本文件为唯一事实来源；与 P0/P1 前序契约冲突时以本文件为准。

---

## 1. 设计目标

P0/P1 前序中，CPI 是央行 MV=PY 理论值（`central_bank.go:168-180`），失业是周期阶段
的外生概率（`events.go:32-39`），玩家的生活支出流向 `world` 黑洞——**消费与生产
两端互不认识**。本次新增三个纯引擎模块，让「你花的每一分钱」都变成「别人的营收
与工资」：

| 引擎 | 新文件 | 核心教育价值 |
|------|--------|------------|
| **消费品市场 + 内生 CPI** | `goods.go` | 统计局八大类消费篮子：需求过热/供给冲击 → 分品类涨价 → 加权 CPI（真实物价统计原理）|
| **劳动力市场** | `labor.go` | 消费 → 企业营收 → 劳动需求 → 失业率 → 个体裁员概率 → Phillips 工资（凯恩斯循环流）|
| **社会结构统计** | `labor.go` | 净资产基尼 / 收入五等份 / 生存-积累-自由圈分布（《总体设计》§6 落地）|

**循环链路**（每月闭合一次）：

```
玩家月结消费(步骤5, 按恩格尔曲线拆 8 类) ──Pay──▶ firms(企业部门)
                                                    │ ×FirmScale 2.5(投资/政府/外需)
                                                    ▼
                                        Revenue → CapacityWage(×0.52 劳动份额)
                                                    │ ÷ WageBill
                                                    ▼
                              TargetEmployment → Unemployment → LayoffWave
                                                    │                    │
                              Phillips 年度工资 ◀──┘          个体裁员概率 ×(U/5%)
                                                    │
                              GoodsMarket.DemandIdx(8 类需求) → 分品类价格 → CPI
                                                    │
                              央行 MonthlyDecision CPI 混合(50% 篮子实测)
```

配套的**消费档位系统**（节俭/标准/精致/奢侈）与 Agent 经济感知工具见 §7；
社会调研系统见同目录《虚拟城市-P1-社会调研系统-v1.md》（引用本文档 §3/§7）。

---

## 2. 消费品市场与内生 CPI（新文件 `goods.go`）

### 2.1 统计局八大类消费篮子（权重照真实 CPI 口径，P1 新定）

| id | 中文 | weight | 弹性/特例 |
|----|------|--------|-----------|
| `food` | 食品烟酒 | 0.30 | 猪周期扰动 seed |
| `clothing` | 衣着 | 0.06 | — |
| `housing` | 居住 | 0.20 | 价格向 Market 区租金指数联动 |
| `household` | 生活用品及服务 | 0.06 | — |
| `transport` | 交通通信 | 0.13 | 油价扰动 seed |
| `education` | 教育文化娱乐 | 0.11 | 繁荣期需求旺（供给系数随周期 +1.0）|
| `healthcare` | 医疗保健 | 0.09 | 随年龄结构上移（恩格尔分配体现，见 §3.4）|
| `misc` | 其他用品及服务 | 0.05 | — |

权重和恒为 1.00（0.30+0.06+0.20+0.06+0.13+0.11+0.09+0.05）。

### 2.2 数据结构（契约，字段名/JSON tag 不可改）

```go
// GoodsItem 单类消费品市场状态。
type GoodsItem struct {
    ID        string  `json:"id"`         // food|clothing|housing|household|transport|education|healthcare|misc
    Weight    float64 `json:"weight"`     // 篮子权重(§2.1)
    PriceIdx  float64 `json:"price_idx"`  // 基期=100
    MomChange float64 `json:"mom_change"` // 上月环比(小数,0.01=+1%)
}

// GoodsMarket 全城消费品市场(挂 World.Goods;引擎纯状态,无锁)。
type GoodsMarket struct {
    Items     map[string]*GoodsItem // 8 类,id → 状态
    CPIYoY    float64               // 12 月滚动年化(小数)
    CPIMom    float64               // 上月环比(小数)
    SupplyIdx map[string]float64    // 产能指数 base=100
    history   []float64             // 月度 CPI 环比历史(内部;len<12 时 CPIYoY 未就绪)
}

// NewGoodsMarket 构造初始市场(8 类 PriceIdx/SupplyIdx=100,权重 §2.1)。
func NewGoodsMarket() *GoodsMarket

// CPIYoYReady 是否已积累 12 个月环比(央行混合 CPI 的前置条件,§2.4)。
func (g *GoodsMarket) CPIYoYReady() bool
```

### 2.3 月度价格更新（World 方法，调用方持锁，随机经 `w.Rand`）

```go
// GoodsMonthStep 月度消费品价格更新(P1 新定)。
// 调用时机:SettleMonth ④ 市场漂移之后(新插入点 ④.5,§5.2)——housing 联动
// 需要本月最新 DistrictIdx。所有随机性经 w.Rand 驱动(确定性单测)。
func (w *World) GoodsMonthStep()
```

逐类算法（伪码，常量在 `goods.go` 顶部）：

```
for g in 8 类:
    DemandIdx_g = 上月该类实际消费额(Σ玩家 ConsumptionByGoods[g]) ÷ 基准需求_g
        基准需求_g = goodsBaselineDemandCNY × weight_g   // 常量 102000(12 人×均值月支出 8500)
    phaseBeta = {recovery:+0.4, boom:+1.0, recession:−0.6, depression:−1.2}
    SupplyIdx_g(t+1) = 100 + phaseBeta×12 + N(0,2)        // 产能随周期扩张/收缩
    food:    15% 概率猪周期 seed → SupplyIdx ×(1 ± 0.04)   // ±4%
    transport: 15% 概率油价 seed → SupplyIdx ×(1 ± 0.04)
    priceRatio = clamp((DemandIdx_g / SupplyIdx_g)^0.4, 0.96, 1.05)
    moneyEffect = clamp((CB.M2GrowthYoY − CB.RealGDPGrowth)×0.05, −0.005, +0.01)  // CB nil → 0
    PriceIdx_g(t+1) = PriceIdx_g(t) × priceRatio × (1 + moneyEffect)
    MomChange_g = PriceIdx_g(t+1)/PriceIdx_g(t) − 1

housing 特例(替代上式 priceRatio):
    MomChange_housing = 0.3 × mean_d(ΔIdx_d ÷ Idx_d)      // 八区房价指数环比均值
    PriceIdx_housing(t+1) = PriceIdx_housing(t) × (1 + MomChange_housing) × (1 + moneyEffect)

CPIMom = Σ weight_g × MomChange_g
history = append(history, CPIMom); 若 len>420 截断
CPIYoY = Π(1 + history[i], 最近 12 月) − 1   // 不足 12 月:值保留但 CPIYoYReady()=false
```

> ⚠️ housing 联动理由：真实 CPI「居住类」以房租为主要口径；游戏内 Market 的
> `DistrictIdx`（`market.go:102-118` HousePrice/HouseRent 同源）已是唯一房价信号，
> 八大类若另起炉灶会出现两套居住价格——故居住类 CPI 直接锚定房价指数环比，
> 系数 0.3（租金涨幅通常低于房价涨幅）。

### 2.4 央行联动（`central_bank.go` MonthlyDecision 修改）

现状：`MonthlyDecision`（`central_bank.go:222-326`）步骤 e 调 `ComputeCPI()`
（MV=PY：`TargetCPI + 0.5×(M2GrowthYoY − RealGDPGrowth)`，clamp 0–5%）。

P1 改造（仅步骤 e 处追加，`ComputeCPI` 本体不动）：

```go
// e. 内生 CPI:混合 = 50% MV-PY 理论值 + 50% 八大类篮子实测 CPIYoY。
cb.ComputeCPI() // 理论分量(写入 cb.CPI)
if w.Goods != nil && w.EconomyEnabled && w.Goods.CPIYoYReady() {
    blended := 0.5*cb.CPI + 0.5*w.Goods.CPIYoY
    cb.CPI = clampF(blended, CPIMin, CPIMax)
    cb.LastCPI = cb.CPI
}
```

50/50 混合的理由见 §12.2。`InflationFactorCB`（`engine.go:322-337`）无需改动——
它读 `w.CB.CPI`，混合后自动传导到生活支出。

---

## 3. 消费决策（Player 字段 + 步骤5 改造 + 新动作）

### 3.1 Player 新增字段（`player.go` Player 结构体追加）

```go
// P1: 真实经济循环(v1 §3)。
ConsumptionLevel    int                    // 0 节俭/1 标准/2 精致/3 奢侈;默认 1(newPlayerFromCard 显式置 1)
ConsumptionByGoods  map[string]float64     // 上月消费结构(元,id→金额;nil=未初始化,视为档位 1)
```

> ⚠️ 零值陷阱：0 是合法档位（节俭）。`newPlayerFromCard`（`engine.go:130-155`）必须
> 显式 `ConsumptionLevel: 1`；存量对局玩家以 `ConsumptionByGoods == nil` 判「未初始化」，
> 首次月结按档位 1 处理（见 §3.3）。

档位读取统一走兜底函数：

```go
// ConsumptionLevelSafe 档位兜底:map 未初始化(nil)或档位越界 → 1(标准)。
func (p *Player) ConsumptionLevelSafe() int
```

### 3.2 档位乘数与效果（常量表，P1 新定）

| 档位 | 名称 | 支出乘数 `levelMult` | 精力效果（步骤5 结算时） |
|------|------|---------------------|------------------------|
| 0 | 节俭 frugal | 0.6 | −1 |
| 1 | 标准 normal | 1.0 | 0（默认）|
| 2 | 精致 refined | 1.5 | +1 |
| 3 | 奢侈 luxury | 2.2 | +2 |

```go
var consumptionLevelMult = [4]float64{0.6, 1.0, 1.5, 2.2}
var consumptionLevelEnergy = [4]int{-1, 0, 1, 2}
```

精力效果 clamp 到 [−3, 10]（既有精力边界，`events.go:127/147` 同款）。

### 3.3 步骤5 改造（`settlement.go:302-318`）

现状：`living = card.Expense × InflationFactorCB`；家庭支出（配偶 2000/孩 5000/
老人 1000）单列，两者均 `Pay(seat → world)`。

P1 改造后（`economy_enabled=true` 路径）：

```go
// ── 步骤5 生活支出:职业基数 × 通胀因子 × 档位乘数(P1)。
level := p.ConsumptionLevelSafe()
if p.ConsumptionByGoods == nil { level = 1 } // 存量兜底
infl := InflationFactorCB(w)
living := int64(float64(p.Card.Expense)*infl*consumptionLevelMult[level] + 0.5)
if living > 0 {
    w.Pay(seat, SeatEntity(seat), EntityFirms, living, CatLiving, "生活支出") // to=firms
    addExpense(living, "living", "生活支出")
}
// 家庭支出金额不变,但归入篮子计数(§3.4)且 to=firms:
familyLiving(配偶/孩/老人) → w.Pay(..., EntityFirms, familyLiving, CatLiving, "家庭支出")

// 精力效果(clamp [-3,10]):
p.Energy = clamp(p.Energy+consumptionLevelEnergy[level], -3, 10)

// 恩格尔分配:living+familyLiving 拆 8 类写入 p.ConsumptionByGoods(覆盖上月)。
p.ConsumptionByGoods = splitEngel(living+familyLiving, p, w.Age())
```

**强制降档**（流动性约束，P1 新定）：步骤5 开头检查——

```go
// 现金 < 2×月生活支出基准(card.Expense×infl,不含档位乘数)→ 强制降为 0 档。
if p.Cash < 2*int64(float64(p.Card.Expense)*infl+0.5) && level != 0 {
    p.ConsumptionLevel = 0
    w.emitEvent("life", seat, fmt.Sprintf("%d 号位现金不足,本月强制节俭档(生活支出×0.6)", seat))
}
```

### 3.4 恩格尔曲线分配（`goods.go` 纯函数，P1 新定）

```go
// splitEngel 把总消费额按恩格尔曲线拆到八大类(份额和恒为 1,金额按份额分摊)。
// 月收入 = p.monthlyIncomeEstimate();家庭人数 = 1+(married?1:0)+Children。
func splitEngel(totalCNY int64, p *Player, age int) map[string]float64
```

份额公式（伪码；income 为月收入，CNY）：

```
foodShare       = clamp(0.30 − 0.002×((income−8000)/100), 0.15, 0.45)
housingShare    = 0.22 + 0.03×(家庭人数−1)                    // 上限 0.40
educationShare  = 0.08 + 0.04×Children                        // 上限 0.25
healthcareShare = 0.05 + 0.003×max(0, age−35)                 // 上限 0.15
其余 4 类(clothing/household/transport/misc)默认权重 0.06/0.06/0.13/0.05:
    if Σ(上述4类) > 0.90: 四类等比缩放至 0.90(家庭极值保护)
    remaining = 1 − Σ(上述4类)   // ≥0.10 由缩放保证
    每类份额 = 默认权重 × remaining ÷ 0.30(四类默认权重和)
金额 = total × 份额;round 到元。
```

家庭支出的篮子归类（金额不变，仅计数口径，P1 新定）：

| 家庭项 | 金额 | 篮子拆分 |
|--------|------|---------|
| 配偶 | 2000 | food 0.45 / housing 0.35 / misc 0.20 |
| 每孩 | 5000 | education 0.55 / food 0.30 / clothing 0.15 |
| 每位老人 | 1000 | healthcare 0.60 / food 0.30 / misc 0.10 |

> 拆分在 `splitEngel` 内一并完成（入参含 family 各项），保证
> `Σ ConsumptionByGoods == living + familyLiving`（单测断言）。

### 3.5 新动作 `set_consumption`（`actions.go`）

```go
// 动作常量追加(actions.go:39-60 段):
ActSetConsumption = "set_consumption"

// Action 结构体复用现有字段:a.Level int `json:"level,omitempty"`(新增 1 个字段)

// actSetConsumption 设置消费档位(耗 1 次动作预算;立即生效,本月月结按新档位结算)。
func (w *World) actSetConsumption(p *Player, a Action) (string, *errcode.Error) {
    if p.ActionBudget <= 0 { return "", errcode.Code(errcode.ErrWealthActionBudgetExhausted) }
    if a.Level < 0 || a.Level > 3 {
        return "", errcode.Code(errcode.ErrWealthConsumptionLevelInvalid) // 35020
    }
    p.ConsumptionLevel = a.Level
    text := fmt.Sprintf("调整消费档位:%s(支出×%.1f,精力%+d)",
        consumptionLevelCN(a.Level), consumptionLevelMult[a.Level], consumptionLevelEnergy[a.Level])
    w.spendBudget(p, "idle", text)
    return text, nil
}
```

`ApplyAction` switch（`actions.go:93-131`）追加 `case ActSetConsumption` 分支。
人类走 WS `game.wealth_action {type:"set_consumption", level}`，与 Agent 同路径。

### 3.6 钱流改造：Ledger 新实体 `firms`（`ledger.go`）

```go
// ledger.go:13-19 实体常量追加:
EntityFirms = "firms" // 企业部门:家庭消费的接收方(P1);只收不付
```

**方向白名单改造**（`ledger.go:99-116` `validFromTo`，I3 纪律）：

| 规则 | P0 现状 | P1 改造 |
|------|---------|--------|
| `from == EntityFirms` | — | **恒非法**（工资仍由 bank 代发；企业营收不回流玩家，P2 可接分红）|
| `to == EntityFirms` | — | 仅允许消费类 category：`living / consume / rent_pay / property / study / social_event / moving / medical / wedding` |
| `to == EntityWorld` | 任意消费类 | 收窄为仅 `donate`（公益转移，非企业营收）+ 禁止 CatInject（不变）|
| `from == EntityWorld` | 仅 CatInject | 不变（初始注入）|

**全量改造点**（to=world → to=firms）：`settlement.go:276/281/306/316`（房租/物业/
生活/家庭）+ `actions.go:855/873/929/980/996`（study/social/moving/consume）+
`events.go:90/122`（婚礼/医疗）。`actions.go:996` 捐赠保留 to=world。

`CatConsume` 自由消费同时计入 `p.ConsumptionByGoods["misc"]`（与步骤5 同口径，
`actConsume` 尾部追加一行）。

> 存量兼容：Ledger 是 append-only 历史，旧条目 `to=world, category=living` 不回溯
> 改写；`validFromTo` 是**写入时**校验，新规则只约束新条目。`firms` 无需显式创建
> （实体 id 惰性出现在 Entry 中）；`World.Goods/Labor` 为 nil 的存量对局首次月结时
> 惰性初始化（§6.3 回退开关关闭时除外）。

---

## 4. 劳动力市场（新文件 `labor.go`）

### 4.1 数据结构（契约）

```go
// FirmSector 企业部门月度状态(挂 World.Labor;引擎纯状态,无锁)。
type FirmSector struct {
    RevenueCNY    int64   // 上月企业营收 = Σ当月 to-firms 流水 × FirmScale(2.5)
    WageBillCNY   int64   // 上月全城工资总额(在职玩家 SalaryBase 之和)
    AvgWageCNY    int64   // 滚动平均工资 = WageBill ÷ 在职人数
    Unemployment  float64 // 内生失业率 0-1(clamp [0.02,0.35])
    Employment    float64 // 就业率 = 1 − Unemployment
    WageGrowthYoY float64 // Phillips 年化工资增长(小数,AnnualAdjust 时更新)
    LayoffWave    int     // 裁员潮强度 0-3(0 平静/1 观察/2 裁员潮/3 已触发个体放大)
}

// 常量(goods/labor 标定,P1 新定,理由 §12.1):
const FirmScale        = 2.5  // 居民消费 → GDP 的倍数(投资+政府+外需)
const LaborShare       = 0.52 // 劳动报酬份额(国民收入口径)
const UnemployFloor    = 0.02 // 失业率下限
const UnemployCeil     = 0.35 // 失业率上限
const UnemployNatural  = 0.05 // 自然失业率(个体概率基准)

// NewFirmSector 构造初始状态(Unemployment=0.05 自然率)。
func NewFirmSector() *FirmSector

// SocietyStats 社会结构统计(§5,月度计算后缓存于 World.Society)。
type SocietyStats struct {
    Gini      float64    // 存活玩家净资产基尼系数 [0,1]
    Quintiles [5]float64 // 可支配收入五等份各组收入占比(低→高,和=1)
    Circles   [3]int     // 圈层人数:{生存圈, 积累圈, 自由圈}
}

// ComputeSociety 月度社会结构统计(纯函数,§5)。
func ComputeSociety(w *World) *SocietyStats
```

### 4.2 月度更新（`SettleMonth` 新插入点 ②.0）

```go
// LaborMonthStep 劳动力市场月度更新(P1 新定)。
// 调用时机:SettleMonth ② MonthlyEvents 之前(events.go 失业概率消费最新 Unemployment)。
// 随机性:无(本步确定性;个体裁员掷骰仍在 MonthlyEvents 经 w.Rand)。
func (w *World) LaborMonthStep()
```

算法（伪码）：

```
conFirms = Σ Ledger 当月(w.Month)To=EntityFirms 条目金额   // 本月企业内需
          // 辅助函数 sumFirmsInflow(month int) int64(扫描 Entries,O(条目数))
Revenue = conFirms × FirmScale(2.5)                          // 含投资/政府/外需
WageBill = Σ alive 且 UnemployedMonths==0 玩家 SalaryBase     // 失业者不计
AvgWage = WageBill ÷ max(1, 在职人数)
CapacityWage = Revenue × LaborShare(0.52)
TargetEmployment = clamp(CapacityWage ÷ max(1, WageBill), 0.60, 1.0)
Unemployment = clamp(Unemployment + 0.25×((1−TargetEmployment) − Unemployment),
                     0.02, 0.35)                              // α=0.25 平滑
Employment = 1 − Unemployment
LayoffWave = switch { Unemployment > 0.12 → 2; Unemployment > 0.08 → 1; default → 0 }
```

> 负反馈自稳：失业人数上升 → WageBill 下降 → TargetEmployment 回升 → 失业率减速——
> 与真实劳动市场的均值回复一致，防 12 人小样本发散。

### 4.3 个体失业概率改造（`events.go:22-49` MonthlyEvents）

现状：`base` 按周期阶段 0.15/0.25/0.05（`events.go:32-39`），再乘认知折扣。

P1 改造（`economy_enabled=true` 时）：

```
p = base(周期) × clamp(Unemployment ÷ 0.05, 0.5, 2.5)
    // 内生失业率=自然率 5% 时 p=base(与 P0 等价);
    // 危机 U=12.5% → ×2.5(衰退期 37.5%/月);过热 U=2% → ×0.4→clamp 0.5。
失业时长 = 2 + w.Rand.Intn(5)        // U(2,6) 月,不变
RehireSalaryRatio(裁员时点抽样,分段):
    U > 0.10 → 0.7 + 0.2×rand   // 0.7–0.9(就业市场冷,降薪再就业)
    U < 0.04 → 1.0 + 0.2×rand   // 1.0–1.2(用工紧俏,跳槽涨薪)
    其他     → 0.8 + 0.4×rand   // 0.8–1.2(P0 原区间)
```

`economy_enabled=false` → 完整回退 P0（base 直乘、ratio 恒 0.8–1.2）。

### 4.4 工资 Phillips 曲线（`settlement.go:530-556` AnnualAdjust）

现状：`p.BrassSalaryGrowth()`（`player.go:324-335`）返回 +3%/+1%/0%/−1%（Brass 档改写）。

P1 改造（`economy_enabled=true` 时，`AnnualAdjust` 内替换增长率来源）：

```go
// phillipsBase 年化基础工资增长 = 3% + 0.5×(5% − U×100)%(P1 新定)。
// U=5% → 3%(与 P0 基线一致);U=10% → 0.5%;U=2% → 4.5%。
func (w *World) phillipsBase() float64 {
    return 0.03 + 0.5*(0.05-w.Labor.Unemployment)
}

// 年增长 g = phillipsBase + Brass 档相对偏移:
//   BrassSalaryDelta: tier1 −2% / tier2 −3% / tier3 −4% / 无信用贷 0
//   (偏移 = BrassSalaryGrowth − 0.03,保持「Brass 压低增长」语义叠加在 Phillips 基线之上)
g := w.phillipsBase() + p.BrassSalaryGrowth() - 0.03
```

**名义工资粘性**（v2.60 N10-2，机制即文档）：工资只在年度调整（AnnualAdjust 不变），
CPI 每月变——高通胀期实际工资自动缩水（`InflationFactorCB` 抬升生活支出而工资不动），
无需额外代码，本节写明供验收观察。

---

## 5. 社会结构统计（`labor.go`，月度计算，供面板）

### 5.1 计算时机与缓存

`SettleMonth` 新插入点 ④.6（GoodsMonthStep 之后、⑤ 月份+1 之前）：
`w.Society = ComputeSociety(w)`。view 层直接读 `World.Society`。

### 5.2 口径（P1 新定，全部只统计 alive 玩家）

| 指标 | 口径 |
|------|------|
| **基尼系数** | 净资产（`p.NetWorth(w.Market)`，`player.go:413-422`）基尼：升序排序后 G = (2Σi·x_i)/(n·Σx_i) − (n+1)/n；Σx=0 或 n<2 → 定义 0（全员等资产=0，单测断言）|
| **五等份** | 可支配收入 = `Monthly.Income − Tax − Social`（clamp ≥0），升序均分 5 组，输出各组收入占总和比例（和=1）；n<5 或 Σ=0 → 全 0.2 |
| **圈层**（§6《总体设计》）| 比值 r = 月被动收入（`MonthlyPassiveIncome`）÷ max(1, `Monthly.Expense`)：r<1 生存圈 / 1≤r<2 积累圈 / r≥2 自由圈；输出各圈人数 |

---

## 6. 视图与协议

### 6.1 `ClientGameState` 追加（`view.go:21-41`）

```go
// ClientGameState 追加(P1;economy_enabled=false 时为零值/空数组下发):
ConsumerMarket ConsumerMarketJSON `json:"consumer_market"`
LaborMarket     LaborMarketJSON   `json:"labor_market"`
Society         SocietyJSON       `json:"society"`
Surveys         []SurveyJSON      `json:"surveys"` // 见社会调研系统文档 §5

type ConsumerMarketJSON struct {
    CPIYoY float64           `json:"cpi_yoy"`
    CPIMom float64           `json:"cpi_mom"`
    Goods  []GoodsItemJSON   `json:"goods"` // 8 类,按 §2.1 表序
}
type GoodsItemJSON struct {
    ID        string  `json:"id"`
    Weight    float64 `json:"weight"`
    PriceIdx  float64 `json:"price_idx"`
    MomChange float64 `json:"mom_change"`
}
type LaborMarketJSON struct {
    UnemploymentRate  float64 `json:"unemployment_rate"`
    EmploymentRatio   float64 `json:"employment_ratio"`
    AvgWageGrowthYoY  float64 `json:"avg_wage_growth_yoy"`
    FirmRevenueCNY    int64   `json:"firm_revenue_cny"`
    LayoffWave        int     `json:"layoff_wave"`
}
type SocietyJSON struct {
    Gini      float64    `json:"gini"`
    Quintiles [5]float64 `json:"quintiles"`
    Circles   struct {
        Survival   int `json:"survival"`
        Accumulate int `json:"accumulate"`
        Freedom    int `json:"freedom"`
    } `json:"circles"`
}
```

### 6.2 PlayerJSON / MyJSON 追加（`view.go:93-115` / `131-149`）

```go
// PlayerJSON 追加(全公开):
ConsumptionLevel int `json:"consumption_level"` // 0-3(档位是公开生活方式)

// MyJSON 追加(仅本人):
ConsumptionByGoods map[string]float64 `json:"consumption_by_goods"` // nil→{}
```

`BuildClientState`（`view.go:221-392`）填充上述字段；数组字段空时初始化为
`[]`（P0-bugfix 纪律，防前端 `null.map` 崩溃）。

### 6.3 `game.month` 帧追加（`ws/game_service_wealth.go:290-313`）

`SettleResult` 追加两个字段；`broadcastWealthMonth` 的 `market_changes` 追加两键：

```go
// settlement.go SettleResult 追加:
CPI             float64 // 本月篮子 CPIYoY(economy_enabled=false 时 = CB.CPI)
UnemploymentRate float64

// ws 层 market_changes 追加:
"cpi": res.CPI, "unemployment_rate": res.UnemploymentRate
```

### 6.4 错误码追加（`errcode/errcode.go`，wealth 段 35016-35020）

```go
// 35016–35020 — 财商流 P1 真实经济循环 + 社会调研(2026-09-16 §财商流P1-2)。
ErrWealthSurveyOptionsInvalid = 35016 // 调研选项数非法(须 2-6)或选项索引越界
ErrWealthSurveyOpenExists     = 35017 // 已有进行中的调研(每房同时 1 个 open)
ErrWealthSurveyMonthlyLimit   = 35018 // 本月已达调研发起上限(每月 1 个)
ErrWealthSurveyNotFound       = 35019 // 调研不存在/已关闭/已回答
ErrWealthConsumptionLevelInvalid = 35020 // 消费档位非法(须 0-3)
```

### 6.5 配置追加（`config/config.go:346-369` WealthConfig）

```go
// P1 新增(json 键):
EconomyEnabled bool `json:"economy_enabled"` // 默认 true;false 回退 P0 行为
SurveyEnabled  bool `json:"survey_enabled"`  // 默认 true(社会调研系统文档 §6)
```

接线：ws 层装配 `wealth.Manager.Config` 追加 `EconomyEnabled/SurveyEnabled`
（NewManager 归一：零值→true）→ `Manager.CreateRoom` 调
`r.SetEconomyFlags(economy, survey)` → `WealthRoom.Start`（`room.go:311`）在
`NewWorld`（`room.go:342`）后回写 `World.EconomyEnabled`（`NewWorld` 恒置 true）。

**`economy_enabled=false` 回退行为**（单测覆盖）：
- 生活支出/家庭支出/房租/物业/学习/社交/迁区/消费/医疗/婚礼 → `to=world`（P0 路径）；
- 失业概率/ratio 回退 P0（§4.3）；工资增长回退 `BrassSalaryGrowth()` 原值；
- `GoodsMonthStep`/`LaborMonthStep`/`ComputeSociety` 跳过；view 三结构零值下发；
- 央行 CPI 不混合（纯 MV-PY）；`set_consumption` 动作仍可用（仅改乘数与精力）。

---

## 7. Agent 适配（wealthplayer）

### 7.1 新工具（`tools.go` 常量 + `BuildTools` 定义）

| 工具 | 参数 | 预算 | 说明 |
|------|------|------|------|
| `set_consumption` | `level:int(0-3)` | **耗 1 次** | 调整消费档位（§3.5 同一 ApplyAction 路径）|
| `answer_survey` | `survey_id, option_index, reason(≤50字)` | **不耗** | 回答进行中调研（见调研系统文档 §4）|
| `query_economy` | 无 | **不耗** | CPI 同比/环比、八大类价格环比、失业率、工资增长、企业营收、基尼、圈层分布 |

`ToolRunner` 接口（`tools.go:18-45`）追加 3 方法；`agent_runner.go` 实现
（`SetConsumption` 走 `a.apply` 持锁模式；`AnswerSurvey`/`QueryEconomy` 持锁读/写，
参照 `QueryMinsky` 的锁纪律）：

```go
SetConsumption(seat int, level int) error
AnswerSurvey(seat int, surveyID string, optionIdx int, reason string) error
QueryEconomy(seat int) (string, error) // 返回单行摘要文本(中文,人读)
```

### 7.2 GameContext 追加（`wealthtypes/context.go:15-53`）

```go
// P1 真实经济循环(引擎侧 BuildContextForAgent 填充):
CPIYoY           float64  // 篮子 CPI 同比(小数)
UnemploymentRate float64  // 内生失业率(小数)
ConsumptionLevel int      // 本人当前档位(0-3,兜底后)
OpenSurveyID       string   // 进行中调研 id(无则 "")
OpenSurveyQuestion string   // 问题文本
OpenSurveyOptions  []string // 选项列表
EconomyBrief       string   // 一行价格涨跌摘要,如 "CPI同比2.3% 失业5.1% 涨幅前二:食品+1.2% 交通+0.8%"
```

### 7.3 prompt 追加（`prompt.go`）

**SystemPromptBlocks**（`prompt.go:44-114`）第 4 段（规则摘要）追加 3 行：

```
- 消费档位:0 节俭(支出×0.6/精力−1)/1 标准/2 精致(×1.5/+1)/3 奢侈(×2.2/+2)。
  档位决定生活支出与精力,也决定全城物价与就业(恩格尔定律:收入越低食品占比越高)。
- 经济循环:你的消费 → 企业营收 → 劳动需求 → 失业率 → 裁员概率与再就业薪资;
  失业率高时保守消费、留现金;菲利普斯定律:失业率高→年度涨薪低。
- 有进行中的社会调研时,用 answer_survey 表达真实偏好(不耗动作次数)。
```

**UserPrompt**（`prompt.go:117-198`）在「■ 市场行情」后追加「■ 经济环境」段：
CPI 同比/环比、失业率、涨幅前二商品（EconomyBrief 已含）、本人消费档位、
待答调研（问题 + 选项编号列表，无则省略）。

---

## 8. 前端 UI 契约（frontend-dev 依据）

| 项 | 内容 |
|----|------|
| `types/wealth.ts`（现 662 行）| 新增 `WealthGoodsMarket/WealthGoodsItem/WealthLaborMarket/WealthSociety` 类型；`WealthGameState` 追加 4 个可选字段 `consumer_market/labor_market/society/surveys` |
| `WealthPanelTab`（`store/wealth.store.ts:21`）| 扩为 `'finance'\|'market'\|'ledger'\|'economy'\|'survey'`；tabs 数组（`pages/WealthGamePage.tsx:164-168`）追加 2 项 |
| **新组件 `EconomyPanel.tsx`** | 八大类价格环比横条 + CPI 仪表 + 失业率仪表 + 工资增长 + 企业营收 + 圈层分布 + 基尼；空态兜底照 `MinskyPanel.tsx`（179 行）防御模式；挂 tab `economy` |
| `ActionPanel.tsx` | 消费档位 4 按钮组（frugal/normal/refined/luxury），选中态对比度 ≥6:1（CLAUDE.md §26.1 AAA）|
| `hooks/useWealth` | `game.month` 的 `market_changes.cpi/unemployment_rate` 更新 store |
| i18n | `wealth.economy.*`、`wealth.consumption.*` 键，zh-CN/en/ja 三语言同步（扁平 key）|
| `styles/wealth.css`（现 1193 行,上限 1800）| 追加 `wealth-economy__*`、`wealth-consumption__*` BEM 类（总行数 ≤1800，超限按 §4 CSS 拆分规约搬移）|
| 图标 | 八大类 emoji：🍚👕🏠🧻🚌📚💊🛍；美术图标就绪后切 `assets/images/wealth/goods/*.png` |

---

## 9. 文件变更清单

| 文件 | 变更类型 | 内容 |
|------|---------|------|
| `ServerGo/game/wealth/goods.go` | **新建** | 八大类篮子 + 价格更新 + 恩格尔分配 + 常量 |
| `ServerGo/game/wealth/labor.go` | **新建** | FirmSector + LaborMonthStep + Phillips + SocietyStats |
| `ServerGo/game/wealth/player.go` | 修改 | ConsumptionLevel / ConsumptionByGoods 字段 + 兜底函数 |
| `ServerGo/game/wealth/actions.go` | 修改 | ActSetConsumption + Action.Level + to=firms 改造 |
| `ServerGo/game/wealth/settlement.go` | 修改 | 步骤5 改造 + 插入点 ②.0/④.5/④.6 + AnnualAdjust Phillips + SettleResult 字段 |
| `ServerGo/game/wealth/events.go` | 修改 | 失业概率内生放大 + ratio 分段 + to=firms |
| `ServerGo/game/wealth/central_bank.go` | 修改 | MonthlyDecision 步骤 e CPI 混合 |
| `ServerGo/game/wealth/ledger.go` | 修改 | EntityFirms + 方向白名单 |
| `ServerGo/game/wealth/engine.go` | 修改 | World.Goods/Labor/Society/EconomyEnabled 字段 + newPlayerFromCard 档位置 1 |
| `ServerGo/game/wealth/room.go` | 修改 | SetEconomyFlags + Start 回写 |
| `ServerGo/game/wealth/manager.go` | 修改 | Config 追加 2 开关 + CreateRoom 接线 |
| `ServerGo/game/wealth/view.go` | 修改 | 4 个 JSON 结构 + 填充 |
| `ServerGo/game/wealth/agent_runner.go` | 修改 | SetConsumption/AnswerSurvey/QueryEconomy + GameContext 填充 |
| `ServerGo/agent/wealthplayer/tools.go` | 修改 | 3 工具定义 + ToolRunner + DispatchTool |
| `ServerGo/agent/wealthplayer/prompt.go` | 修改 | 第 4 段追加 + 经济环境段 |
| `ServerGo/agent/wealthtypes/context.go` | 修改 | §7.2 字段 |
| `ServerGo/config/config.go` | 修改 | economy_enabled / survey_enabled |
| `ServerGo/errcode/errcode.go` | 修改 | 35016-35020 |
| `ServerGo/ws/game_service_wealth.go` | 修改 | market_changes 追加 2 键 + Manager.Config 装配 |
| `ClientWeb/src/types/wealth.ts` | 修改 | §8 类型 |
| `ClientWeb/src/components/wealth/EconomyPanel.tsx` | **新建** | 经济仪表盘 |
| `ClientWeb/src/components/wealth/ActionPanel.tsx` | 修改 | 档位按钮组 |
| `ClientWeb/src/store/wealth.store.ts` + `pages/WealthGamePage.tsx` + `hooks/useWealth.ts` | 修改 | tab 扩展 + store 字段 |
| `ClientWeb/src/styles/wealth.css` + `ClientWeb/src/i18n/*.json` | 修改 | §8 |
| `ServerGo/game/wealth/goods_test.go` | **新建** | §10.1 |
| `ServerGo/game/wealth/labor_test.go` | **新建** | §10.2 |

---

## 10. 测试验收

### 10.1 `goods_test.go`

- 价格更新确定性：固定 seed（`NewWorld(seed, cards)`）连续 24 月 `GoodsMonthStep`，
  断言 PriceIdx 与手算一致（含 clamp 边界 0.96/1.05）。
- CPI 环比 = Σ weight×MomChange；12 月后 CPIYoY = Π(1+mom)−1 且 `CPIYoYReady()==true`。
- 恩格尔分配：高收入玩家 foodShare 落 0.15 下限；4 孩家庭四类等比缩放触发；
  `Σ ConsumptionByGoods == living+familyLiving`（金额守恒）。
- 货币传导：M2GrowthYoY−RealGrowth = ±0.2 → moneyEffect 命中 ±clamp。
- housing 联动：手动抬升 DistrictIdx 1% → MomChange_housing ≈ 0.3%。

### 10.2 `labor_test.go`

- 消费骤降：全员 `set_consumption(0)` 后 3 月 Revenue×0.6 → Unemployment 单调上升，
  LayoffWave≥1；恢复 1 档后回落（负反馈）。
- Phillips：U=0.05 → phillipsBase=0.03；U=0.10 → 0.005；Brass tier1 叠加后 = 基线−2%。
- 再就业比率分段：U=0.12 抽样 1000 次 ratio∈[0.7,0.9]；U=0.03 ratio∈[1.0,1.2]。
- 基尼边界：全员等资产 → 0；一人全占 → ≈(n−1)/n；n<2 → 0。
- 五等份和=1；圈层：被动收入 0 → 生存圈；2×支出 → 自由圈。

### 10.3 settlement 集成（`settlement_test.go` 追加）

- 步骤5 钱流：economy_enabled=true → Ledger 当月出现 `to=firms, category=living`；
  `to=world` 仅剩 donate/inject。
- 强制降档：现金 < 2×月生活支出 → ConsumptionLevel==0 + life 事件。
- `economy_enabled=false` 回退：全量流水 `to=world`、失业概率与 P0 一致（固定 seed 对拍）。
- Ledger 守恒 I1 不破坏（`VerifySeatConservation` 全座位全月）。

### 10.4 编译门禁

`go build -o LsmAgentGame main.go` + `go test ./...` + 前端 `tsc --noEmit` +
`npm run build` 全过。

### 10.5 运行时验收

10 bot 房间跑 36 月：CPI 同比落在 0–5%、失业率常态 4–6%（萧条期可冲 20%+）、
繁荣期食品/教育环比居前；Agent 出现 `set_consumption` 与 `query_economy` 调用。

---

## 11. 实现优先级与 SubAgent 分工

| 阶段 | 工作面 | SubAgent | 产出 |
|------|--------|---------|------|
| 1 | 后端纯引擎 | backend-dev | goods.go / labor.go + 单测 |
| 2 | 后端集成 | backend-dev | settlement/events/actions/central_bank/ledger/engine/room/manager/view 修改 |
| 3 | Agent 适配 | backend-dev | tools.go / prompt.go / context.go / agent_runner.go |
| 4 | 配置+错误码 | backend-dev | config.go / errcode.go / ws 装配 |
| 5 | 前端 UI | frontend-dev | EconomyPanel / ActionPanel 档位组 / tab / i18n / css |
| 6 | 验收 | integration-tester | §10 门禁 + 36 月运行时观察 |

阶段 1–4 可合并为单一 backend-dev 任务；阶段 5 独立 frontend-dev；社会调研系统
（同目录另一契约）与本文件的阶段 2/3 共享提交窗口时先本文件后调研。

---

## 12. 设计取舍

1. **FirmScale=2.5 与劳动份额 0.52 的标定**（12 人小样本校准）：精选 14 卡
   Expense/Salary ∈ [0.62, 0.78]、均值 ≈0.69（`profession/curated.go`）。
   单人基线 TargetEmployment = 2.5×0.52×0.69 ≈ 0.90；成家后（配偶+2000、每孩+5000）
   典型已婚 1 孩顶到 1.0 上限；12 人混合构成（开局部分已婚 + 钟声逐步成家）加权
   ≈0.94–0.96 → 平滑失业率 **4–6%**（自然率带）。全员节俭（×0.6）→ 0.54–0.78 →
   clamp 后失业率升至 20%+，呈现真实「节约悖论」；clamp [0.60,1.0]×[0.02,0.35]
   封死极端值。真实世界劳动份额 ≈52%（国民收入统计），FirmScale 2.5 近似
   「居民消费 ≈ GDP×40%」的倒数为消费乘数口径——两个参数都有现实锚点，
   联合取值由 12 人样本反推微调。
2. **CPI 混合权重 50/50**：纯篮子实测在前 12 月（CPIYoY 未就绪）与消费剧烈波动月
   噪声过大（12 人样本无大数定律）；纯 MV-PY 又回到「货币决定一切」的外生感。
   各取一半：货币供给决定中长期趋势，篮子捕捉短期结构性涨价（猪周期/油价 seed）。
3. **`firms` 只收不付**：企业营收回流玩家需要「分红/雇佣」二级模型（P2 企业投资），
   P1 先闭合「消费→劳动需求」因果链；bank 代发工资保持既有实体语义不动。
4. **精力奖励方向**（奢侈 +2）：品质生活提升精气神——档位权衡是「现金 vs 精力」，
   节俭档省钱但耗神（−1），与 rest/study 动作形成资源替代关系。
5. **强制降档阈值 2×月生活支出**：低于该线意味着一次失业即击穿应急垫；
   自动降档模拟流动性约束（真实世界「没钱只能省」），Agent 无法装死。
6. **Brass 偏移制**：Phillips 基线 + (Brass 增速 − 3%)，保证自然失业率下与 P0
   数值完全一致（回归零差异），Brass 语义（信用贷压增长）原样保留。
7. **家庭支出篮子拆分比例**（§3.4 表）：配偶偏食品/居住、孩偏教育、老人偏医疗——
   按中国家庭消费调查的常识口径取整，教学显著优先统计精度。

---

## 13. 与既有设计/实现的兼容性

| 既有契约 | 处理方式 |
|---------|---------|
| P0 `SettleMonth` 7 步顺序（§9.2 锁定）| 不改顺序，仅插入 ②.0/④.5/④.6 三个新步骤点 + 步骤5 内部改造 |
| P0 Ledger 实体语义（§13）| `world` 收窄为 inject+donate；`firms` 新增；旧条目不回溯 |
| P1 央行 `ComputeCPI` | 本体不动，仅在 MonthlyDecision 混合（CB nil 回退路径零改动）|
| P1 明斯基/LPR | 零冲突（插入点 ③/③.5 与 ②.0/④.5 互不相邻语义）|
| P0 `PhaseTable.CPI`（`market.go:33-39`）| 保留（view 的 Cycle.CPI 回退值 + economy_enabled=false 路径）|
| P0 前端 CyclePanel | 不动；新 EconomyPanel 独立 tab |
| 错误码段 35001-35015 | 追加 35016-35020 |
| `types/wealth.ts`（662 行）/ `wealth.css`（1193 行）| 追加后仍 < 1800 行上限；超限触发 §4 拆分规约 |

---

## 14. 维护规约

1. 本文件与代码同步演进：任何实现偏差先改本文档再改码（或同提交双改）。
2. 新增代码文件 ≤1800 行（CLAUDE.md §4）；本文档 ≤800 行（§3）。
3. 数值引用格式：§2.1/§3.2/§4.1 表 = P1 新定（本文档 §12 标定理由）；
   圈层定义 =《总体设计》§6。
4. 所有新随机性必须经 `w.Rand`（确定性单测纪律，`engine.go:3` 分层约定）。
5. `economy_enabled` 回退路径是**一等公民**：每次改动主路径必须同步验证回退
   对拍（§10.3）。

---

## 15. 实现偏差与文档同步（v1 → 代码落地时的修正）

> 后端实现时发现契约字面公式在 12 人小样本下会出现结构偏差（单边通缩 / 营收漏记），
> 以下 6 处按 §14 维护规约「先改文档再改码（或同提交双改）」在代码落地的同一提交中同步。
> 机制形状与教育价值不变，均为收敛性 / 会计口径修正。

| # | 契约原文 | 代码实现 | 修正理由 |
|---|---------|---------|---------|
| 1 | §4.2 `conFirms = Σ Ledger 当月(w.Month) To=EntityFirms` | 扫描**上一完整结算月 `w.Month−1`** | ②.0 时点当月生活支出尚未入账（步骤③才记账），字面实现会让消费大头永远进不了营收，违反 §12.1 自身标定 |
| 2 | §2.3 `DemandIdx = consumed ÷ 基准需求`（固定基准 102000×weight） | `DemandIdx = 100×consumed÷baseline`；基准首月播种后 **EMA(α=0.1) 跟随**收敛至 `consumed×100/supply` | 固定基准下恩格尔份额与篮子权重结构性不一致，商恒 ~0.01 → 月月触 0.96 下限单边通缩；改需求热度指数后稳态 ratio→1，档位骤变/相位切换仍保留 ±clamp 有界响应 |
| 3 | §3.6 `to=world` 收窄为仅 `donate` | `to=world` 允许 `donate` + 9 类消费类（`living/consume/rent_pay/property/study/social_event/moving/medical/wedding`，排除 inject 与非消费类）| 字面收窄与 §6.5 `economy_enabled=false` 回退路径（消费类仍写 to=world）自相矛盾；修正后 `from=firms` 恒非法、`to=firms` 仅 9 类消费白名单 |
| 4 | §4.3 TopReasons「按提交顺序去重」| 按**座位号升序**（map 迭代序不确定，取确定序）| 确定性单测与回放需要 |
| 5 | §3.4 round 到元 | round 后**残差计入 food** | 保证 `Σ ConsumptionByGoods == living+familyLiving` 精确金额守恒（单测断言）|
| 6 | §6.5 配置 bool | 沿用仓库 bool 零值→true 模式（`agent_enabled` 同款取舍，conf 显式 false 无法与零值区分）；关闭走房间级 `SetEconomyFlags` | 与既有 config 体系一致 |

---

## 16. 更新日志

### v1（2026-09-16）
- 初版：消费品市场与内生 CPI、消费档位与恩格尔分配、firms 钱流、劳动力市场
  （内生失业 + Phillips 工资）、社会结构统计、Agent 三工具、前端 EconomyPanel 契约。

### v1.1（2026-09-16，同提交）
- 实现偏差同步 6 项（§15 表）：conFirms 改扫上月、DemandIdx 改 EMA 需求热度指数、to=world 白名单放宽、TopReasons 座位序、恩格尔残差守恒、config bool 零值模式。机制形状与教育价值不变。
