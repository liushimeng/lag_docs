# 虚拟城市 — 3D 城市渲染优化 · 实施路线图与验收（13-3D城市渲染优化 · 05）

> 2026-09-22。总控文档。阶段顺序即依赖顺序；每阶段独立提交（中文 commit）。
> 契约文档：01 产品设计 / 02 架构设计 / 03 资产方案 / 04 UI 规范。

## 0. 全局硬约束复述

- 所有改动在 `main` 分支（§10.1）；提交前 `git status` 核对外来并行改动，hunk 级只提交本批次内容。
- 单文件 ≤ 1800 行（§4）；`wealth.css` 已 1738 行 → 新样式进 `wealth-city3d.css`。
- 禁止跨游戏 import（§2.1）；新组件落 `components/wealth/`。
- 新字段/新贴图必须 grep 验证接线（§130）；贴图缺失必须降级兜底（§9）。
- 伪随机只用 mulberry32 + hashStr（seed 稳定）。
- 提交前：`tsc --noEmit` + `npm run build` 必过；`ServerGo/` 未动则不跑 go test（本批次纯前端+脚本）。

## 1. 阶段 A · 资产补齐（art-designer 线）

**产出**：
1. `3d_script/procedural_city_textures.py`（新）—— Pillow 程序化生成 32 张新 8 城区贴图，
   幂等 skip-if-exists，`--force` 覆盖，`--only districts,facades,roofs` 子集。
2. `python-generate-image-tool/generate_wealth_city_assets.py` —— FACADES/ROOFS 追加 8 城区描述词；
   新增新 8 城区底板生成（districts 类别若原脚本无则补 `--only districts` 支持）。
3. 运行脚本，32 张 PNG 全部落盘（AI 轨道不可用时程序化轨道兜底，两轨同守 skip-if-exists）。

**验收**：03 文档 §4 四条。

## 2. 阶段 B · 建筑形态（frontend-dev 线）

**产出**：
1. `components/wealth/building_shapes.tsx`（新）：`BuildingArchetype` + `DISTRICT_ARCHETYPE`
   + 五种体块渲染器（tower/slab/house/shed/pavilion）。
2. `BuildingMesh.tsx` 改为按 archetype 分发；贴图加载与降级链不变；emissive 统一 `#ffd9a0`。
3. `DistrictBlock.tsx`：central_park 楼数 1-2 且 archetype=pavilion；heightBase 逻辑不变。
4. `StreetPropsLayer.tsx`：central_park 树 10 棵；其余城区树 2→4。

**验收**：肉眼——金融 CBD 塔楼有裙楼+收分；郊区/老城区坡屋顶；工业区厂房+烟囱；
中央公园绿化为主。mesh 总数 ≤ 1500（02 §5）。

## 3. 阶段 C · 环境氛围（frontend-dev 线）

**产出**：`WealthCityMap.tsx` —— drei `<Sky />`、ACES 色调映射 + exposure 1.05、
PCFSoftShadowMap、方向光 2048 shadow map + bias/normalBias + 阴影相机 ±WORLD_SIZE*0.6、
ambient 0.45 / hemisphere 0.5 / 日光 `#fff2e0`、雾色 `#aeb8c6`。

**验收**：截图有天空渐变；全城建筑投影完整无 acne；旧贴图不发灰不过曝。

## 4. 阶段 D · 街道细节（frontend-dev 线）

**产出**：
1. `Road.tsx`：from 端 t=0.08 斑马线（crosswalk 贴图接线，修复 G4）。
2. `props/TrafficLight.tsx`（新）：纯几何红绿灯，主干道 t=0.12 路侧布点。
3. `props/Vehicle.tsx`：4 车轮 + 前灯/尾灯 emissive。
4. `StreetPropsLayer.tsx`：红绿灯布点接入。

**验收**：`grep -rn "crosswalk" ClientWeb/src --include=*.tsx` 有渲染侧命中；
路口可见斑马线与红绿灯；车辆近景有车轮。

## 5. 阶段 E · UI 布局（frontend-dev 线）

**产出**：
1. `WealthGamePage.tsx`：tabs 分组（data/trade）。
2. `components/wealth/CollapsibleSection.tsx`（新）；CityStatsPanel / WealthBotPanel 接入折叠。
3. `styles/wealth-city3d.css`（新）+ `globals.css` 末尾追加 import。
4. `DistrictBlock.tsx` hover 卡 `zIndexRange={[30, 0]}`；小地图 z-index 40。
5. i18n 三语新键 `wealth.tabgroup.data/trade`。

**验收**：04 文档 §7 五条。

## 6. 阶段 F · 验证交付（主 Agent）

1. `cd ClientWeb && npx tsc --noEmit && npm run build`。
2. 截图巡检（go-web-debug-tool 可用则用，否则人工）：1280×800 / 1920×1080。
3. `./rebuild_restart_app.sh`。
4. 中文 git 提交 + 推送：
   - 提交 1：`docs(虚拟城市): 13-3D城市渲染优化方案文档5份`（lag_docs 子模块先行 commit+push，
     主仓库再提交指针 —— lag_docs 是独立子模块）。
   - 提交 2：`feat(city): 阶段A 新8城区贴图补齐与3d_script程序化纹理管线`。
   - 提交 3：`feat(city): 阶段B 建筑体块形态系统(塔楼/坡屋顶/厂房/公园)`。
   - 提交 4：`feat(city): 阶段C 天空+ACES色调映射+柔和阴影`。
   - 提交 5：`feat(city): 阶段D 斑马线/红绿灯/车辆细节接线`。
   - 提交 6：`feat(city): 阶段E 侧栏Tab分组折叠与防溢出`。
   - 可合并提交以阶段 B–E 两两合并，但总提交数 ≥ 3，每提交独立可编译。

## 7. 完成定义（DoD）

- [ ] 32 张新贴图落盘且构建打包
- [ ] 5 种建筑 archetype 全部可见
- [ ] 天空/阴影/雾协同无回归
- [ ] crosswalk 接线 grep 验证
- [ ] Tab 分组 + 折叠持久化
- [ ] tsc/build 全绿
- [ ] 文档 5 份 + 子模块推送 + 主仓推送
