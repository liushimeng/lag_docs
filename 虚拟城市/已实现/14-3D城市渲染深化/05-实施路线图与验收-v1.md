# 虚拟城市 — 3D 城市渲染深化 · 实施路线图与验收（14-3D城市渲染深化 · 05）

> 2026-09-22。总控文档。阶段顺序即依赖顺序；每阶段独立可编译。
> 契约文档：01 产品设计 / 02 架构设计 / 03 资产方案 / 04 UI 规范。
> 前置：13-3D城市渲染优化 阶段 A–F 已全部交付（提交 f0a97806b1 及之前）。

## 0. 全局硬约束复述

- 所有改动在 `main`（§10.1）；提交前 `git status` 核对外来并行改动，hunk 级只提交本批次。
- 单文件 ≤ 1800 行（§4）；新样式只进 `wealth-city3d.css`；新组件落 `components/wealth/`。
- 禁止跨游戏 import（§2.1）；伪随机只用 mulberry32 + hashStr。
- 新字段/新贴图必须 grep 验证接线（§130）；贴图缺失降级兜底（§9）。
- 提交前 `tsc --noEmit` + `npm run build` 必过；本批次纯前端 + 资产脚本，`ServerGo/` 未动则不跑 go test。
- lag_docs 是独立子模块：文档 commit+push 先行，主仓再提交指针。

## 1. 阶段 G · 地表资产（程序化轨道）

**产出**：
1. `3d_script/procedural_city_textures.py` 扩展 `ground` 类别：
   `ground/{grass_tile,plaza_tile,water_tile}.png`（512² 可平铺，seed=FNV-1a，幂等）。
2. 运行 `--only ground` 落盘 3 张 PNG。
3. `assets/images/wealth/index.ts` 增 `groundTileUrl()`。

**验收**：03 文档 §4 四条。

## 2. 阶段 H · 共享贴图管线 + 渲染微调

**产出**：
1. `components/wealth/textureCache.ts`（新）：`useSharedTexture` 模块级缓存。
2. 9 个消费组件私有 loader 全部替换为共享 hook（BuildingMesh/Road/DistrictBlock/
   WealthCityMap-Ground/Vehicle/Pedestrian/Tree/Sign/RooftopAcc/StreetLight）。
3. `WealthCityMap.tsx`：OrbitControls `enableDamping` / dpr [1,1.75] / `powerPreference`。

**验收**：构建通过；Network 面板同 URL 贴图只加载一次；轨道拖拽有阻尼收尾。

## 3. 阶段 I · 城市细节模型

**产出**：
1. `props/WaterPlane.tsx`（新）+ `WealthCityMap` 内 `<WaterLayer>`：运河（z=17）+ 物流港港池（-30,-4）+ 岸草皮。
2. `building_shapes.tsx`：底商雨棚 + 灯带（tower/slab）；塔楼广告牌（w>1.4）。
3. `DistrictBlock.tsx`：curb 四边檐条；central_park 草地层；transport_hub/finance 广场层。
4. `props/BusStop.tsx` + `props/StreetFurniture.tsx`（新）；`StreetPropsLayer` 接线布点。
5. `props/Pedestrian.tsx`：环线漫步（pathR），REDUCED_MOTION 静止。

**验收**：01 文档 §5 体感标准；`grep -rn "useSharedTexture\|WaterPlane\|BusStop\|StreetFurniture\|pathR" ClientWeb/src` 全部有消费点。

## 4. 阶段 J · UI 布局加固

**产出**：`wealth-city3d.css` 增补 04 文档 §3 块；`WealthGamePage.tsx`/`wealth.css` 若 U4 需 JSX 微调则同步。

**验收**：04 文档 §5 五条。

## 5. 阶段 K · 验证交付（主 Agent）

1. `cd ClientWeb && npx tsc --noEmit && npm run build`。
2. 截图巡检（go-web-debug-tool 可用则用，否则人工）：1280×800 / 1920×1080。
3. `./rebuild_restart_app.sh`。
4. 中文 git 提交 + 推送（总提交数 ≥ 3）：
   - 提交 1：`docs(虚拟城市): 14-3D城市渲染深化方案文档5份`（lag_docs 子模块先行 commit+push，主仓再提交指针）。
   - 提交 2：`feat(city): 阶段G-H ground地表贴图管线与共享纹理缓存`。
   - 提交 3：`feat(city): 阶段I 水系/底商/站台/街道家具与行人漫步`。
   - 提交 4：`feat(city): 阶段J-K UI布局加固与渲染验证交付`。

## 6. 完成定义（DoD）

- [ ] 3 张 ground 贴图落盘且构建打包，幂等可重跑
- [ ] useSharedTexture 全面接线（§130 grep 零漏）
- [ ] 运河/港池/草地/广场/curb/雨棚/广告牌/站台/家具 全部可见
- [ ] 行人漫步 + REDUCED_MOTION 静止
- [ ] UI 5 问题修复，1280×800 无溢出无重叠无大留白
- [ ] tsc/build 全绿；mesh 预算 ≤ 2500
- [ ] 文档 5 份 + 子模块推送 + 主仓推送
