# 虚拟城市 — 前端架构与 2.5D 地图 v1

> **定位**：`ClientWeb/` 前端实现契约：文件布局、2.5D 城市场景规范、小地图、面板清单、
> i18n 键清单、样式与对比度约束、降级策略。协议字段见
> [`虚拟城市-WS与HTTP协议契约-v1.md`](虚拟城市-WS与HTTP协议契约-v1.md)（`src/types/wealth.ts`
> 与其 §3 逐字段对齐）。
>
> 硬约束：CLAUDE.md §2.1（禁止跨游戏 import / 共享工具唯一）、§4（单文件 ≤1800 行）、
> §26（对比度）、§7.1（错误展示）。

---

## 目录

1. [文件布局](#1-文件布局)
2. [类型与状态](#2-类型与状态)
3. [2.5D 城市场景规范](#3-25d-城市场景规范)
4. [小地图](#4-小地图)
5. [面板清单](#5-面板清单)
6. [路由 / 菜单 / 规则页](#6-路由--菜单--规则页)
7. [i18n 键清单（zh/en/ja 全量）](#7-i18n-键清单)
8. [样式与对比度](#8-样式与对比度)
9. [降级策略](#9-降级策略)

---

## 1. 文件布局

```
ClientWeb/src/
├── types/wealth.ts                        WealthGameState / Player / MyState / …（与协议 §3 对齐）
├── store/wealth.store.ts                  zustand：state + frames reducer + ui 态
├── hooks/useWealth.ts                     WS 订阅 + game.wealth_action 发送 + 恢复（仿 useTexasHoldem）
├── pages/WealthLobbyPage.tsx              大厅：banner + 房间列表 + 创建
├── pages/WealthGamePage.tsx               对局页（组装地图 + 面板 + 动作条）
├── components/wealth/
│   ├── WealthCityMap.tsx                  r3f 主场景（Canvas + 灯光 + 地面 + 城区 + token）
│   ├── WealthMinimap.tsx                  左上角 Canvas2D 小地图
│   ├── AgentToken.tsx                     玩家 token（圆柱 + 头像 sprite + 名牌）
│   ├── DistrictBlock.tsx                  单城区楼群 + 底板 + hover 信息卡
│   ├── FinancialPanel.tsx                 三表 + 五维资源 + FI 仪表
│   ├── MarketPanel.tsx                    行情（股指/金/债券利率/8 区房价指数/周期徽章）
│   ├── LedgerPanel.tsx                    财富流动（双式流水 + 月度汇总）
│   ├── ActionPanel.tsx                    底部动作条（人类 12 按钮 + submit_month）
│   ├── MonthTicker.tsx                    月度节拍倒计时 + 事件横滚
│   ├── GameOverModal.tsx                  终局评分 + 6 结局 + 报告
│   ├── WealthCreateRoomModal.tsx          建房弹窗（agent_seats / month_ms / pool / seed）
│   └── WealthGameChatPanel.tsx            聊天薄适配器（复用 components/chat/GameChatPanel，仿 werewolf 87 行版）
├── assets/images/wealth/
│   ├── index.ts                           URL 打包导出（import.meta.url）
│   ├── banner.png                         2560×1440
│   ├── agents/{p01,p03,p05,p07,p08,p09,p10,p11,p15,p16}.png   512×512 透明
│   └── districts/{finance,tech,industry,oldtown,commerce,residential,suburb,riverside}.png  1024×1024
├── i18n/types.ts                          [+wealth 命名空间类型]
├── i18n/locales/{zh-CN,en,ja}.ts          [+wealth.* 键，§7 全量]
├── rules/gameRules.ts                     [GameKind + 'wealth'；GAME_ACCENT；rulesUrl]
├── styles/wealth.css                      [globals.css @import 链末尾追加]
└── public/rules/wealth.md                 规则页静态文档

App.tsx        [+3 路由：/wealth、/wealth/:roomId、/wealth/spectate/:roomId]
AppSidebar.tsx [MENU_GROUPS agent 组 + { to:'/wealth', labelKey:'nav.wealth', icon:'💰' }]
```

所有新文件均在 `components/wealth/` 游戏私有目录内（§2.1 约束 2）；不 import 其他游戏组件；
聊天复用 `components/chat/`（共享目录，合法）。

## 2. 类型与状态

- `types/wealth.ts`：`export interface WealthGameState {…}`（协议 §3 全字段）、`WealthPlayer`、
  `WealthMyState`、`WealthAsset`、`WealthLoan`、`WealthLedgerEntry`、`WealthEvent`、
  `WealthMonthSummary`、`WealthScore`、`WealthAction`（ discriminated union by `type`）。
  字段名 / 可空性与协议**逐字一致**，禁止 camelCase 转写偏差。
- `store/wealth.store.ts`（zustand，仿 `doudizhu.store.ts`）：
  `state: WealthGameState | null`、`applyFrame(type, payload)`（switch：`game.state` 全量替换、
  `game.month` 追加 ticker、`game.event` 追加事件流（截断 100）、`game.over` 置 scores）、
  `ui: { panelTab, selectedDistrict, actionModal }`。
- `hooks/useWealth.ts`：WS 连接由 `AppLayout` 唯一持有（§16 约束），本 hook 只订阅
  `game.*` 帧 + 提供 `sendAction(action)`（封装 `game.wealth_action{room_id, action}`）；
  断线恢复：进入 `/wealth/:roomId` → `game.join` → `game.state`（`useSessionRestore` 增加
  `/wealth/:roomId` 路由解析）；观战：`useSpectatorMode()`（已有，复用）。
- 错误展示（§7.1）：弹窗内动作用内联 `formError`；面板级 / 帧错误 `reportGlobalError(e)`。

## 3. 2.5D 城市场景规范

`WealthCityMap.tsx`（@react-three/fiber + @react-three/drei，与狼人杀 3D 座位同一技术栈）：

| 项 | 规范 |
|----|------|
| Canvas | `<Canvas shadows dpr={[1,2]}>`；挂于左主区，flex 自适应 |
| 相机 | 透视 `position={[14,12,14]} fov={45}`，lookAt 原点（等距视角） |
| OrbitControls | `enablePan`（右键/双指）、`enableZoom`、`enableRotate`；`maxPolarAngle={1.2}`、`minDistance={8}`、`maxDistance={40}`；`target={[0,0,0]}` |
| 地面 | 40×40 `planeGeometry` 旋转 -90°，深色底（`#141a24`）+ 网格辅助线（`gridHelper` 40×40，低对比） |
| 城区 | 8 × `DistrictBlock`：位置 = 协议 `districts` 表 (x,z)（§4 城区定义）；**底板** 8×8 `planeGeometry` 贴 `districts/<id>.png` 纹理（缺失降级纯色）；**楼群** 4–6 栋 `boxGeometry`，确定性伪随机（`seedrandom(districtId)`，**不用 Math.random**——重渲染布局稳定），高度映射 `price_index`（0.8–1.6 指数 → 1–5 单位，繁荣期楼变高、萧条期变矮 = 可视化周期） |
| 道路 | 深色薄 `planeGeometry`（y=0.02）连接各城区中心到 finance(0,0)，宽 0.8 |
| 灯光 | `ambientLight intensity={0.7}` + `directionalLight castShadow position={[10,16,8]}` |
| Token | `AgentToken`：圆柱（r=0.35 h=0.7，职业色）+ drei `Billboard` 头像 sprite（512 PNG）+ `Html` 名牌（`distanceFactor={12}`：座位号 + 昵称 + 职业图标）；**移动**：`useFrame` 中 `position.lerp(target, 0.06)` 到目标区中心（迁区平滑动画，约 1s） |
| hover | `DistrictBlock` 悬停 → 浮层信息卡（区名 / 当前房价 / 月租 / beta / 在区玩家数），离开消失 |
| 选中 | 小地图或面板点击城区 → 地图 `OrbitControls.target` 平滑聚焦该区 |

职业色（Token 圆柱与名牌底色，对比度 ≥4.5:1 于深色地图）：
`P01 #f59e0b / P03 #94a3b8 / P05 #34d399 / P07 #60a5fa / P08 #fb7185 / P09 #a78bfa /
P10 #f87171 / P11 #fbbf24 / P15 #fdba74 / P16 #e879f9`（P0 新定；头像缺失时同一色画圆环兜底）。

## 4. 小地图

`WealthMinimap.tsx`：主地图左上角**绝对定位**（120×120px，`pointer-events:auto`）：

- 技术：Canvas2D（**不开第二个 r3f Canvas**，性能考虑）。
- 绘制：40×40 世界 → 120px 坐标（scale 3）；8 城区色块（§3 主色，透明度 0.35）+ 区名首字；
  agent 点（职业色 4px 圆）；当前相机视野框（由主 Canvas `onFrame` 回传 target/distance 推算白框）。
- 交互：点击城区 → 触发主地图聚焦；`requestAnimationFrame` 与主 Canvas 同频重绘 agent 点位。

## 5. 面板清单

| 面板 | 数据源（协议字段） | 要点 |
|------|------------------|------|
| `FinancialPanel` | `my.*` + `players[my_seat]` | 三表 Tab（损益=monthly.detail / 资产负债=assets+loans / 现金流=ledger_recent 聚合）；五维资源条（E/N/K 带数值）；FI 半圆仪表（0–2，档位色：<0.5 灰 / 0.5–1 蓝 / ≥1 绿 / ≥1.5 金） |
| `MarketPanel` | `cycle` + `market` | 周期徽章（四色）+ LPR/CPI；股指/金价/债券利率数字 + 相对上月箭头；8 区 `price_index` 横条（beta 标注） |
| `LedgerPanel` | `ledger_recent` | 双式流水列表（from→to 实体图标 💺🏦🏛🌍 + 类目 + 金额红绿）；Tab 切「本月汇总」（category 聚合，前端推导） |
| `ActionPanel` | `my` + `phase` + `next_month_at` | 12 动作按钮（产品设计 §7.1）+ 结束本月；`phase!=="acting"` / `ActionBudget==0` / 停赛 → disabled（灰底 + `cursor:not-allowed`，§26.2 反模式 5）；弹窗内联错误 |
| `MonthTicker` | `game.month` / `game.event` / `next_month_at` | 倒计时进度条；事件横滚（生命事件 emoji 前缀）；月度汇总卡（全座位 cash_delta / net_worth 迷你表） |
| `GameOverModal` | `game.over` | 三维评分条 + 结局徽章 + 净资产曲线（SVG 折线，dataviz 规范）+ [查看完整 Ledger] / [返回大厅] |
| `WealthCreateRoomModal` | HTTP POST | agent_seats 多选（`/api/llm/models`）、month_ms 滑杆（3000–30000）、pool 单选、seed 输入；失败内联错误不关弹窗 |
| `WealthGameChatPanel` | `chat.*` 帧 | 87 行薄适配器，包裹 `components/chat/GameChatPanel`（§16） |

## 6. 路由 / 菜单 / 规则页

- `App.tsx`：`/wealth` → `WealthLobbyPage`；`/wealth/:roomId` → `WealthGamePage`；
  `/wealth/spectate/:roomId` → `WealthGamePage`（spectate 模式隐藏 ActionPanel）。
- `AppSidebar.tsx`：agent 组 `{ to:'/wealth', labelKey:'nav.wealth', icon:'💰' }`。
- `rules/gameRules.ts`：`GameKind = … | 'wealth'`；`GAME_ACCENT.wealth = '#c9a227'`（金，
  暗底对比度 6.1:1）；`rulesUrl('wealth') → '/rules/wealth.md'`。
- `public/rules/wealth.md`：面向玩家的规则速览（中文，~200 行：五维 / 三表 / FI / 周期 / 17 动作 /
  贷款 / 税 / 结局）。

## 7. i18n 键清单

`i18n/types.ts` 增 `wealth` 命名空间；三语言文件同步（§12）。键清单（**全量 62 键**，缺一即
`tsc` 报错——types 与 locales 强类型联动）：

| 键 | zh-CN | en | ja |
|----|-------|----|----|
| `nav.wealth` | 虚拟城市 | Wealth Game | 財商流ゲーム |
| `wealth.title` | 虚拟城市 | Wealth Game | 財商流ゲーム |
| `wealth.subtitle` | 25 到 60 岁的人生财商沙盘 | Life finance sandbox, age 25–60 | 25〜60歳の人生ファイナンス沙盤 |
| `wealth.createRoom` | 创建房间 | Create Room | ルーム作成 |
| `wealth.roomName` | 房间名称 | Room Name | ルーム名 |
| `wealth.agentSeats` | Agent 座位 | Agent Seats | Agent 席 |
| `wealth.monthMs` | 月度节拍（毫秒） | Month tick (ms) | 月テンポ(ms) |
| `wealth.pool` | 职业卡池 | Profession pool | 職業カードプール |
| `wealth.pool.curated` | 精选 10 卡 | Curated 10 | 精選10枚 |
| `wealth.pool.docs` | 文档池（75 万卡） | Docs pool | 文書プール |
| `wealth.seed` | 随机种子 | Random seed | シード |
| `wealth.join` | 进入房间 | Join | 入室 |
| `wealth.spectate` | 观战 | Spectate | 観戦 |
| `wealth.full` | 已满 | Full | 満員 |
| `wealth.startEarly` | 提前开始 | Start now | 今すぐ開始 |
| `wealth.month` | 第 {m} 月 | Month {m} | 第{m}月 |
| `wealth.age` | {a} 岁 | Age {a} | {a}歳 |
| `wealth.phase.acting` | 行动中 | Acting | 行動中 |
| `wealth.phase.settling` | 结算中 | Settling | 決済中 |
| `wealth.cycle.recovery` | 复苏期 | Recovery | 回復期 |
| `wealth.cycle.boom` | 繁荣期 | Boom | 繁栄期 |
| `wealth.cycle.recession` | 衰退期 | Recession | 後退期 |
| `wealth.cycle.depression` | 萧条期 | Depression | 不況期 |
| `wealth.lpr` | LPR 利率 | LPR | LPR金利 |
| `wealth.cpi` | CPI 通胀 | CPI | CPI |
| `wealth.stockIndex` | 股票指数 | Stock index | 株式指数 |
| `wealth.goldPrice` | 黄金（元/克） | Gold (CNY/g) | 金（元/g） |
| `wealth.bondYield` | 债券年化 | Bond yield | 債券利回り |
| `wealth.housePrice` | 房价指数 | House price idx | 住宅価格指数 |
| `wealth.district.finance` | 金融CBD | Finance CBD | 金融CBD |
| `wealth.district.tech` | 科技园 | Tech Park | テックパーク |
| `wealth.district.industry` | 工业区 | Industry | 工業区 |
| `wealth.district.oldtown` | 老城区 | Old Town | 旧市街 |
| `wealth.district.commerce` | 商业中心 | Commerce | 商業中心 |
| `wealth.district.residential` | 居住区 | Residential | 住宅街 |
| `wealth.district.suburb` | 郊区 | Suburb | 郊外 |
| `wealth.district.riverside` | 滨河新区 | Riverside | 河畔新区 |
| `wealth.cash` | 现金 | Cash | 現金 |
| `wealth.netWorth` | 净资产 | Net worth | 純資産 |
| `wealth.fiIndex` | 财务自由指数 | FI Index | FI指数 |
| `wealth.passiveIncome` | 月被动收入 | Passive income | 受動収入 |
| `wealth.monthlyNet` | 月现金流 | Monthly net | 月次キャッシュフロー |
| `wealth.energy` | 精力 | Energy | 精力 |
| `wealth.network` | 人脉 | Network | 人脈 |
| `wealth.cognition` | 认知 | Cognition | 認知 |
| `wealth.creditScore` | 信用分 | Credit score | 信用スコア |
| `wealth.incomeBand.low` | 低收入 | Low | 低所得 |
| `wealth.incomeBand.mid` | 中收入 | Mid | 中所得 |
| `wealth.incomeBand.high` | 高收入 | High | 高所得 |
| `wealth.incomeBand.top` | 顶级收入 | Top | 最高所得 |
| `wealth.ledger.title` | 财富流动 | Wealth Flow | 財の流れ |
| `wealth.ledger.monthly` | 本月汇总 | This month | 今月集計 |
| `wealth.action.buyAsset` | 买资产 | Buy asset | 資産購入 |
| `wealth.action.sellAsset` | 卖资产 | Sell asset | 資産売却 |
| `wealth.action.buyHouse` | 买房 | Buy house | 住宅購入 |
| `wealth.action.takeLoan` | 贷款 | Take loan | 借入 |
| `wealth.action.repayLoan` | 还款 | Repay loan | 返済 |
| `wealth.action.sideBusiness` | 副业 | Side business | 副業 |
| `wealth.action.study` | 学习 | Study | 学習 |
| `wealth.action.socialize` | 社交 | Socialize | 交流 |
| `wealth.action.rest` | 休息 | Rest | 休養 |
| `wealth.action.workOvertime` | 加班 | Overtime | 残業 |
| `wealth.action.moveDistrict` | 迁区 | Move | 引っ越し |
| `wealth.action.consume` | 消费 | Consume | 消費 |
| `wealth.action.donate` | 捐赠 | Donate | 寄付 |
| `wealth.action.submitMonth` | 结束本月 | End month | 今月を終える |
| `wealth.gameOver.title` | 人生结算 | Life Settlement | 人生決算 |
| `wealth.gameOver.report` | 人生报告 | Life report | 人生レポート |
| `wealth.ending.winner` | 财务自由 · 人生赢家 | FI · Life Winner | 財務自由・人生の勝者 |
| `wealth.ending.affluent` | 富足安稳 · 中产典范 | Affluent Middle Class | 豊かで安定 |
| `wealth.ending.ordinary` | 平淡度日 · 普通一生 | Ordinary Life | 平凡な一生 |
| `wealth.ending.indebted` | 债务缠身 · 挣扎求生 | Indebted | 借務苦 |
| `wealth.ending.bankrupt` | 破产出局 | Bankrupt Out | 破産退場 |
| `wealth.ending.lonely_rich` | 财务自由 · 孤独富翁 | Lonely Rich | 孤独な富豪 |
| `wealth.error.budget` | 本月动作次数已用完 | Monthly action budget used up | 今月の行動回数を使い切りました |
| `wealth.error.cash` | 现金不足 | Insufficient cash | 現金不足 |

> 键数 62+（上表含 nav 1 + wealth.* 61）；`districts`/`action`/`ending` 子键由代码枚举展开，
> locales 中按扁平键书写（与现有 doudizhu/texasholdem 键风格一致）。

## 8. 样式与对比度

- `styles/wealth.css`：**`globals.css` 的 `@import` 链末尾追加**（§2.1 硬约束 4：只追加不改序），
  注释块注明日期与「追加在末尾」原因（仿 `debate.css` 追加样例）。
- 命名空间：全部选择器前缀 `.wealth-`（`.wealth-panel` / `.wealth-action-btn` / `.wealth-ticker` …），
  杜绝与既有样式互染；CSS 变量挂 `.wealth-root` 作用域（`--wealth-accent: #c9a227` 等）。
- 对比度（§26 硬阈值）：正文 ≥4.5:1；徽章 ≥5.0:1；周期徽章四色配白字全部 ≥5:1
  （recovery `#15803d` / boom `#b45309` / recession `#b91c1c` / depression `#374151`）；
  FI 仪表 selected 档 ≥6.0:1。**禁止**红底红字 / `color:inherit`+半透明底 / `opacity:0.4` 表
  disabled（§26.2 五反模式一票否决）。
- 状态徽章三件套（§26.3）：`income-band-*`、`cycle-*`、`ending-*` 的 JSX 拼接与 CSS 规则
  **同提交**完成；写完 JSX 立即 `grep -rn "income-band-" src/styles/` 验证非零命中。
- 触控：`--wealth-touch-target: 44px`；`prefers-reduced-motion` 下关闭 token lerp 与数字滚动
  （`@media` 兜底）。

## 9. 降级策略

| 缺失 | 降级 | 构建影响 |
|------|------|---------|
| 职业头像 PNG | 职业色圆环 + 职业 emoji（`AgentToken` 内 `onError` 切换） | 无 |
| 城区纹理 PNG | `DistrictDefs` 主色纯色底板 | 无 |
| banner PNG | CSS 渐变（accent 金 → 深蓝） | 无 |
| 3D 不可用（极端低端机） | 不做 P0（r3f 与狼人杀同一栈，平台已验证） | — |

美术生成脚本 `python-generate-image-tool/generate_wealth_assets.py`（Volcengine Ark，绿幕抠像）
**skip-if-exists 可续跑**；前端构建**不依赖**生成成功（Vite `import.meta.url` 引用 + 运行时
`onError` 兜底），CI 无图也能 `npm run build` 通过。

---

## 更新日志

- v1（2026-09-14）：首版。文件布局 / 2.5D 场景规范 / 小地图 / 面板 / i18n 62+ 键三语 / 对比度 / 降级。
