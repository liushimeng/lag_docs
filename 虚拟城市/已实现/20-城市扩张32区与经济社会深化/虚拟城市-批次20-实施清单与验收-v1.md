# 虚拟城市 批次20 · 实施清单与验收 — v1

> 系列：`20-城市扩张32区与经济社会深化 / 文档 4`（执行面）
> 契约来源：文档 1（32 城区地图）、文档 2（副业定价战）、文档 3（选举启用+股票微观结构）。
> SubAgent 分工（CLAUDE.md §13）：BE-1/BE-2 = backend-dev（`ServerGo/`）；FE-1/FE-2 = frontend-dev（`ClientWeb/`）；
> 集成验收 = integration-tester。**文件所有权互斥**，冲突项已显式指派。

## 1. 执行序（含依赖）

```
并行波次 1:  BE-1（32区后端表）   BE-2（三功能后端+Agent）   FE-1（32区前端地图+渲染性能）
                  │                    │                        │
波次 2:            └────────────────────┴──── FE-2（三功能 UI + 面板 + 全部功能 i18n）← 依赖 BE-2 契约(文档冻结即可开工) 与 FE-1 完成（共享文件）
波次 3:          集成：go build/test + tsc + npm build + rebuild_restart_app + E2E 冒烟 + CDP 截图
波次 4:          文档回填（实施记录）+ lag_docs 子模块提交 → 主仓库提交（含子模块指针）→ push
```

## 2. BE-1 文件清单（32 城区表，文档 1 §4）

| # | 文件 | 改动 | 验收 |
|---|---|---|---|
| B1-1 | `game/wealth/districts.go` | +16 行 DistrictDefs、DistrictCount=32、+16 感官键、头注释 | `go test ./game/wealth -run District` |
| B1-2 | `game/wealth/city/calibration.go` | districtCount=32（数组编译期锁） | build |
| B1-3 | `game/wealth/city/backdrop.go` | districtNames +16（表序逐字一致） | `city_test.go` Σ/非空 |
| B1-4 | `game/wealth/profession/card.go` | districtIDs +16、修 2 处「8 区」陈旧注释 | build |
| B1-5 | `game/wealth/profession/frontmatter.go` | 行业分流 §2.4、spreadDistricts 扩容、cityToDistrict 关键词 | `frontmatter_test.go` 更新后绿 |
| B1-6 | `game/wealth/profession/synthetic.go` | 新区合成模板 | `TestDocsPool_DistrictSpread ≥16` |
| B1-7 | `game/wealth/seats12_test.go` | 16→32 + **后 16 区 id/顺序冻结断言**（补批次 11 欠账） | 守卫生效 |
| B1-8 | （子模块）`python-generate-image-tool/generate_wealth_city_assets.py` | DISTRICT 清单 + PLACEHOLDER_COLORS +16 键 | `python -c import` 冒烟 |

**禁改**：`industrial_cluster.go`、`engine.go` 座位/并发常量、`agent/`（归 BE-2）。

## 3. BE-2 文件清单（三功能）

| # | 文件 | 改动 | 归属功能 |
|---|---|---|---|
| B2-1 | `game/wealth/player.go` | SideBusiness.PriceTier/TierSetMonth；StockT1Locked | 定价/T+1 |
| B2-2 | `game/wealth/side_market.go`（新） | 权重/乘数/份额/门槛纯函数 | 定价 |
| B2-3 | `game/wealth/actions.go` | actSetSidePrice + start_side_business tier 参数 + 股票买卖改价/T1/熔断 gate | 定价+微观 |
| B2-4 | `game/wealth/settlement.go` | 副业收入乘 s_i/m(t)；delivery 高价精力；content 高价×0.5；T1 月首清零；竞争 event | 定价+T+1 |
| B2-5 | `game/wealth/market.go` + `market_microstructure.go`（新） | 涨跌停 clamp、阶段价差表、熔断状态 | 微观 |
| B2-6 | `game/wealth/civic_election.go`/`manager.go`/`engine.go`/api handler | CivicElectionEnabled 参数链 + next_election_month 下发 | 选举 |
| B2-7 | `game/wealth/view.go` | side_business 扩字段 + side_market + 微观字段 + next_election_month | 全部 |
| B2-8 | `errcode/errcode.go` | 35043/35044 + 默认消息 | 微观 |
| B2-9 | `agent/wealthplayer/tools.go` | 新工具 set_side_price、start_side_business.tier、股票工具描述、**district 描述串动态化**（:171/:244，从 BE-1 表转归本线） | 全部 |
| B2-10 | `agent/wealthplayer/prompt.go` / `game/wealth/agent_runner.go` | 三块上下文注入（定价市场/选举/微观现价），字节预算文档 2 §4.3+文档 3 B4 | 全部 |
| B2-11 | 测试 | `side_market_test.go`、`market_microstructure_test.go`、`civic_election_test.go` 追加、`settlement_test.go` 零偏移回归 | 全部 |

## 4. FE-1 文件清单（32 区前端，文档 1 §3）

| # | 文件 | 改动 |
|---|---|---|
| F1-1 | `types/wealth.ts` | WealthDistrictId +16、WEALTH_DISTRICTS +16（表序/坐标/颜色逐字 = 文档 1 §2）、side_business/市场/选举类型扩（预留 BE-2 字段名，见文档冻结） |
| F1-2 | `cityScale.ts` | DISTRICT_FLOORS +16 |
| F1-3 | `building_shapes.tsx` | DISTRICT_ARCHETYPE +16 |
| F1-4 | `WealthCityMap.tsx` | WORLD_SIZE=120、GROUND=160、fog 1.0W/2.0W、SUN ×1.5、SHADOW_HALF=0.5W、minDistance=0.1W、导出 MAIN_ROAD_MIN_LEN、运河 CANAL_HALF_X→48（WealthCityMap/WaterLayer 坐标 + CanalBridge 引用同步） |
| F1-5 | `StreetPropsLayer.tsx` | 白名单/密度/倾向 +16 区、行人总上限 110、行道树/灯随 MAIN_ROAD_MIN_LEN、**树/路灯 InstancedMesh（<Instances>）改造**、过期注释修正、`?debug=1` renderer.info 挂载 |
| F1-6 | `RingRoad.tsx` / `CanalBridge.tsx` | len>12 → MAIN_ROAD_MIN_LEN import |
| F1-7 | `civic/RailViaduct.tsx` | z=30→46 |
| F1-8 | `civic/Outskirts.tsx` | 半径带 ×1.33（r∈[50,77]） |
| F1-9 | `WealthMinimap.tsx` | DISTRICT_SIZE 同源 import；SCALE<1.4 隐藏区名首字（色块+hover title） |
| F1-10 | `assets/images/wealth/index.ts` | DISTRICT_TEXTURE_ALIAS（16→旧 stem 映射表，文档 1 §2.3/§3.5）；BuildingMesh 解析走别名 |
| F1-11 | i18n 三语 | 16 区名键（wealthKeys 表驱动处） |
| F1-12 | `CityStatsPanel.tsx` | 紧凑模式「展开全 32 区」折叠列表（滚动内收，防溢出） |

## 5. FE-2 文件清单（三功能 UI，依赖 FE-1 完成）

| # | 文件 | 改动 |
|---|---|---|
| F2-1 | `ActionPanel.tsx` | 副业定价三档选择器 + 份额/预期收入 + 同月限改禁用（文档 2 §5） |
| F2-2 | 副业竞争展开（同文件或拆 `SideMarketChips.tsx`） | 对手档位 chips，折叠 |
| F2-3 | `MarketPanel.tsx` | 股票双价/价差徽章/T+1 冻结角标/熔断红条+禁用 |
| F2-4 | `WealthCreateRoomModal.tsx` | 「启动市长选举」开关 → body.civic_election_enabled |
| F2-5 | 选举横幅 + `CityStatsPanel.tsx` 政务票型面板 | 文档 3 A4（折叠、防溢出、对比度） |
| F2-6 | i18n 三语 | wealth.sidePrice.* / wealth.micro.* / wealth.election.* |
| F2-7 | 错误展示 | 35043/35044/定价 gate 失败走弹窗内联 formError（§7.1 自检清单） |

## 6. 全局门禁（波次 3）

1. `cd ServerGo && go build -o LsmAgentGame main.go && go test ./...` 全绿；
2. `cd ClientWeb && npx tsc --noEmit && npm run build` 全绿；
3. §130 grep 接线清单：`DistrictCount=32` 四方 / `PriceTier` / `set_side_price`（tools+dispatcher+test）/
   `CivicElectionEnabled` / `StockT1Locked` / `BreakerUntilMonth` / `MAIN_ROAD_MIN_LEN`（≥6 消费点）/ 别名表；
4. `./rebuild_restart_app.sh` 成功、进程存活、`https://127.0.0.1:39001` 200；
5. E2E 冒烟（记忆规约「建房链路改动后必跑 E2E」）：captcha 流程登录 test 账号 → 创建城市
   （resident_count=200 + civic_election_enabled=true）→ 观察 ≥3 个月 tick 无卡死 →
   state 含 side_market/微观字段/选举字段 → 触发一次 set_side_price 动作成功；
6. CDP 视觉验收（连 9222 常驻 Chrome，29011 无 WebGL 教训）：默认俯瞰 / 外圈新区特写（university_town、
   airport_town）/ 小地图 / CityStatsPanel 展开 / 政务票型面板 / 定价选择器 各截图；
   `?debug=1` 记录 draw call 目标 ≤1500。

## 7. 提交与归档（波次 4）

- 本目录追加实施记录 `虚拟城市-批次20-实施记录-v1.md`（含实测数字/grep 输出/截图路径）；
- lag_docs 子模块内 commit+push → 主仓库 `git add ServerGo ClientWeb python-generate-image-tool lag_docs` 中文提交 → push；
- 提交前 `git status` 核对外来改动（记忆：main 常有并行修改），仅提交本会话 hunk；
- 测试报告若产生：处理完删除，无问题加 `_无问题` 后缀（CLAUDE.md §22）。

## 8. 回滚预案

- 三功能全部「默认关闭/默认中价」，回滚 = revert 后端两 commit + 前端两 commit 即恢复旧行为，无数据迁移；
- 地图扩区不可半程回滚（契约表四方原子性）；如渲染性能不达标，降级开关：
  `localStorage['disable-district-ring2']` 仅渲染前 16 区（FE-1 内一行过滤，验收不过时启用并记录）。
