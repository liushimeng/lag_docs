# 虚拟城市 — P0 验收清单 v1

> **定位**：`game_kind = wealth` P0 骨架的验收门禁：编译 / 测试门禁、功能勾选表、经济不变量、
> 已知边界。integration-tester 跨端联调（CLAUDE.md §13.1 职责线 5）按本清单逐项打勾；
> 全部 P0 项通过方可宣称 P0 完成。
>
> 契约文档索引：[`../README.md`](../README.md) §1。

---

## 目录

1. [编译与测试门禁](#1-编译与测试门禁)
2. [功能清单勾选表](#2-功能清单勾选表)
3. [经济不变量（服务端断言 / 测试用例）](#3-经济不变量)
4. [前端验收要点](#4-前端验收要点)
5. [Agent 验收要点](#5-agent-验收要点)
6. [已知边界与不修复项](#6-已知边界与不修复项)

---

## 1. 编译与测试门禁

| # | 门禁 | 命令 | 通过标准 |
|---|------|------|---------|
| G1 | Go 编译 | `cd ServerGo && go build -o LsmAgentGame main.go` | 零错误 |
| G2 | Go 测试 | `cd ServerGo && go test ./...` | 全 PASS（含新增 engine/settlement/market/ledger/actions/loader/view 测试） |
| G3 | AgentClassName 接线 | `go test ./agent -run ClassNames` | `AgentClassWealthPlayer` 非空且在 `AllAgentClassNames()` |
| G4 | 行数上限 | `find ServerGo -name '*.go' -exec wc -l {} +`（新增文件） | 全部 ≤1800（CLAUDE.md §4） |
| G5 | 前端类型 | `cd ClientWeb && npx tsc --noEmit` | 零错误（wealth.* i18n 键强类型校验同步通过） |
| G6 | 前端构建 | `cd ClientWeb && npm run build` | 成功（无图资产也能过） |
| G7 | CSS 接线 | `grep -n "wealth.css" src/styles/globals.css` | 仅在 @import 链**末尾**出现一次 |
| G8 | 跨文档锚点 | 见 §1.1 | 全部命中 |

### 1.1 跨文档锚点 grep（一致性门禁）

```bash
cd /usr/local/LsmAgentGame/LsmAgentGame
# 每条必须在协议契约与 Agent 设计两份文档中一致出现：
grep -rn "LsmAgentGame-Wealth-Player" docs/虚拟城市/已实现/
grep -rn "game.wealth_action"        docs/虚拟城市/已实现/
grep -rn "profession_docs_path"      docs/虚拟城市/已实现/
# 代码侧接线（防 §130「声明了却从不接线」）：
grep -rn "AgentClassWealthPlayer"    ServerGo/agent/ ServerGo/game/wealth/
grep -rn "game.wealth_action"        ServerGo/ws/
grep -rn "ProfessionDocsPath"        ServerGo/config/ ServerGo/game/wealth/profession/ ServerGo/api/
grep -rn "AGENTS.md.*-L\|test -L" CLAUDE.md   # 既有 CI 项不受影响
```

## 2. 功能清单勾选表

### 2.1 房间生命周期

- [ ] F01 大厅入口：左菜单 agent 组「💰 虚拟城市」→ `/wealth` 大厅渲染 banner + 房间列表
- [ ] F02 建房：`WealthCreateRoomModal` 提交（含 agent_seats / month_ms / pool / seed）→ `POST /api/games/wealth/rooms` 成功，房间容量 12（MinSeats 10）、中文名「虚拟城市」
- [ ] F03 `agent_seats` >1 时 model_key 去重随机化生效（§14.2；建房响应 `agent_seats_count` 一致）
- [ ] F04 加入：HTTP join + WS `game.join` → `game.joined{my_seat}`；满 MinSeats(10) 自动开局
- [ ] F05 提前开始：≥3 座后房主 `game.wealth_start` → `game.started`；<3 座 → 35003
- [ ] F06 观战：`/wealth/spectate/:roomId`；动作帧 → errcode 30011 硬拒
- [ ] F07 退出：`game.leave` 进行中转 bot 接管（无 provider 则冻结）；终局后 60s 自动 `game.removed`
- [ ] F08 断线恢复：刷新 `/wealth/:roomId` → `game.state` 快照恢复全部面板

### 2.2 经济引擎与月结

- [ ] F09 月度节拍：`next_month_at` 倒计时到点自动月结；`game.month` + `game.state` 顺序到达
- [ ] F10 全员提前 `submit_month` → 立即月结（不等满窗口）
- [ ] F11 月结 7 步顺序（§9.2）：同一确定性 seed 下快照 hash 一致
- [ ] F12 个税表：月薪 20000 → 社保 2100、个税 1170（《规则》§9.1 示例复算一致）
- [ ] F13 年度调整（12 月）：工资 +3%、通胀因子进位；Brass 档改写增长率（§7.2）
- [ ] F14 钟声（60 月）：结婚/生育/健康事件按概率触发；市场周期重掷（转移矩阵 §7.2）
- [ ] F15 市场漂移：seed 注入下股票 σ3%/房产 σ0.8%/黄金 σ1.5% 月度复现
- [ ] F16 失业：衰退 15%/萧条 25% 月度判定 → 工资归零 2–6 月 → 80–120% 再就业
- [ ] F17 破产：现金连续 3 月 <0 → 清算（房产 70%）→ 重组（消费贷减 50%/36 期）→ 停 3 月；仍负 → 出局
- [ ] F18 终局：主时钟 60 岁 → `game.over` 三维评分 + 6 结局映射（含孤独富翁特殊线）

### 2.3 人类操作

- [ ] F19 12 动作按钮全部可用且语义与协议 §4 一致（参数弹窗 + 服务端校验）
- [ ] F20 动作预算：每月第 4 个动作 → 35006；`phase=settling` 动作 → 35004
- [ ] F21 买房：首付/房贷/月供计算正确（等额本息公式复算）；满 5 年唯一免增值税判定
- [ ] F22 弹窗内联错误（§7.1）：失败不关弹窗、红条展示 code+message

### 2.4 Agent 驱动

- [ ] F23 全 Agent 房（0 人类）：8 bot 开局 → 420 月跑完不卡死（watchdog 兜底验证）
- [ ] F24 每月 Agent：≤3 动作 + ≤1 speak + submit_month；超时 20s 强制 submit
- [ ] F25 speak 经 `chatSvc.SendFromBot` 到达前端聊天面板；开场白 opening_hook 开局播报
- [ ] F26 `bot_contexts`：本人座位与观战者可见、其他玩家不可见（view_test 覆盖）
- [ ] F27 混合房：人类 + Agent 同场，动作语义无分叉（同 `actions.go` 路径）

### 2.5 前端渲染

- [ ] F28 2.5D 地图：8 城区楼群 + 道路 + token；hover 信息卡；楼群高度随 price_index 变化
- [ ] F29 token 迁区 lerp 动画；小地图同步点位 + 视野框
- [ ] F30 头像/纹理缺失降级：职业色圆环 + emoji / 纯色底板，无白屏
- [ ] F31 LedgerPanel 双式流水 + 本月汇总；FinancialPanel 三表 Tab；FI 仪表档位色
- [ ] F32 GameOverModal：评分条 + 结局徽章 + 净资产曲线
- [ ] F33 i18n：zh/en/ja 切换全键覆盖（缺键 tsc 报错）

## 3. 经济不变量

（`ledger_test.go` / `settlement_test.go` 实现为断言；任何月结改动必须保持全绿）

| # | 不变量 | 断言 |
|---|--------|------|
| I1 | **座位现金守恒**：对每个 seat n、每个月，`期末现金 − 期初现金 = Σ(to=seat:n) − Σ(from=seat:n)` | 逐月逐座位 |
| I2 | **world 注入唯一性**：`from=world` 的 Ledger 条目仅存在于开局初始储蓄（金额 = 卡面 Savings） | 全局计数 = 座位数 |
| I3 | **无凭空造币**：所有 `to=seat` 条目的 from ∈ {bank, market, gov, world}；`gov` 只收不付（养老金缴存 gov→seat 为 `pension` 类白名单） | 实体方向白名单 |
| I4 | **资产负债恒等式**：`净资产 = 现金 + Σ资产市值 − Σ贷款余额`（逐月快照重算一致） | 《规则》§3.4 |
| I5 | **月结顺序**：损益明细数组的 category 顺序 = §9.2 七步（快照比对） | 固定顺序 |
| I6 | **FI 公式**：`FI = 被动收入/月总支出`（支出 0 → 2.0）；与 `game.month.summaries[].fi_index` 一致 | 复算 |
| I7 | **税费率**：股票卖出税费 = 佣金 0.025%（min 5 元）；房产 5%+2%（满五唯一免）；黄金 0.5% | 逐笔 |
| I8 | **月供公式**：等额本息 `P·r·(1+r)^n / ((1+r)^n −1)` 与引擎输出误差 < 0.01 元 | 30 年/3 年/5 年三档 |
| I9 | **seed 确定性**：同 seed + 同动作序列 → 逐月全量状态 hash 一致（engine_test） | 跨平台跑两次 |
| I10 | **评分权重和**：`total = fi×0.5 + life×0.3 + social×0.2`；结局映射区间无重叠无缝隙 | 终局断言 |

## 4. 前端验收要点

1. **对比度**（§26）：暗色主题正文 ≥4.5:1；周期徽章四色白字 ≥5:1；FI selected 档 ≥6:1；
   用开发工具抽查 `.wealth-*` 关键类。
2. **状态徽章三件套**：`income-band-*` / `cycle-*` / `ending-*` JSX 与 CSS 同提交——
   `grep -rn "income-band-" ClientWeb/src/styles/` 非零命中。
3. **禁用态**：`phase!=acting` / 预算耗尽 / 停赛 → 灰底 + `cursor:not-allowed`，禁止 `opacity:0.4`。
4. **错误展示**：每个 catch 要么 `setErr` 内联、要么 `reportGlobalError`（§7.1 自检清单）。
5. **性能**：8 token + 48 楼栋场景 ≥50fps（中端核显）；小地图 Canvas2D 无第二 r3f Canvas。
6. **触控**：动作按钮 ≥44px；`prefers-reduced-motion` 关闭 lerp/滚动动画。

## 5. Agent 验收要点

1. **不调真实 LLM 的单测**：fake llmCaller（仿 `run_circuit_short_circuit_test.go`）覆盖：
   工具循环 ≤3 轮、超时强制 submit、350xx 错误回喂重试、speak 限流。
2. **协议 wire 合规**（§14.1）：text 块仅 `{type,text}`；tool_use 四键齐全；user/assistant 交替
   （`SanitizeMessagesForAnthropic`）；system 为 SystemBlock 数组。
3. **流式超时**：ChatStreamAccumulate 字节刷新（§197）——用慢速 fake SSE 验证不误杀。
4. **User-Agent**：出站 UA 前缀 = `LsmAgentGame-Wealth-Player/`（抓包或 Provider 单测断言）。
5. **公平性**：同 ModelKey 的两个 bot 同 seed 下决策输入一致；Transcript 可审计。

## 6. 已知边界与不修复项

| # | 边界 | 说明 | 处理 |
|---|------|------|------|
| B1 | 文档池卡年龄差异 | 混龄卡仅展示个人 age；终局按主时钟 60 触发 | 设计取舍（后端架构 §1.1 ⚠️） |
| B2 | 债券不做市值重估 | 周期影响落地为新购利率联动，持仓锁定 | P1 再议浮亏记账 |
| B3 | 配偶/副业收入不二次计税 | 按税后净额入账 | P0 简化（后端架构 §8.2） |
| B4 | 股票无分红、无印花税 | 简报锁定 0 税 + 0.025% 佣金 | 与《规则》§6.1 差异已 ⚠️ 标注 |
| B5 | LLM 决策不进确定性测试 | fake Provider 覆盖；真实 LLM 仅 e2e 手测 | 平台惯例 |
| B6 | 极端低端机 3D 降级未做 | r3f 与狼人杀同栈，平台已验证 | P1 观察 |
| B7 | 人类中途加入从下月参与 | 当月不补结 | P0 新定（产品设计 §4） |
| B8 | pause 仅月结边界生效 | tick 中途暂停可能导致该月略长 | 可接受（≤1.5×month_ms watchdog 兜底） |
| B9 | P0 房间不持久化 | 重启服务丢对局（与德扑/狼人杀 in-memory 一致） | 平台现状 |
| B10 | 捐赠无上限刷社会分 | 上限 10 分已封顶；金额本身不设限 | 教学语义（钱真花出去） |

---

## 更新日志

- v1（2026-09-14）：首版。门禁 G1–G8、功能 F01–F33、不变量 I1–I10、前端/Agent 验收、边界 B1–B10。
