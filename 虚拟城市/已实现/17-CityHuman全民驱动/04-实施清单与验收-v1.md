# 虚拟城市 — CityHuman 全民驱动实施清单与验收 v1

> **批次**：17-CityHuman 全民驱动 ｜ **日期**：2026-09-22 ｜ **性质**：执行依据
> 契约：01（前端/建房 UI）、02（驱动层）、03（职业卡退役/后端收口）、00（全景索引）。
> 执行顺序：后端契约 → 后端实现 → 前端实现 → 门禁 → E2E 冒烟 → 发布。

---

## 1. 后端改动清单（`ServerGo/`，职责线 backend-dev）

### 1.1 驱动层（02 号契约）

| # | 文件 | 改动 | 验收 |
|---|---|---|---|
| B1 | `game/wealth/city/driver.go`（**新建**） | `DriverConfig`/`ResidentDriver`/`RunMonth`/`runOne`/`personaBlocks`/`contextText`/`commonToolDefs`（set_intent+speak）；单轮解析；流式租约调用复用 voice.go 路径 | 编译过；`driver_test.go` 全绿 |
| B2 | `game/wealth/city/backdrop.go` | `resident` 增 `intent`/`moveTarget`；`PickDriverCandidates(n, *uint64)`；`ApplyIntent`；`TickMonth` 意图消费+月末清除；`Snapshot` 增 `Driver *DriverSnapshot`(omitempty) | 20B→22B 内存断言更新；意图演化单测 |
| B3 | `game/wealth/room_city.go` | `startCityLocked` 装配 Driver（Enabled 时）互斥 VoiceScheduler；`launchCityVoices` → `launchCityDriver`（关闭回退旧路径）；`CitySnapshotView` 透出 driver 块 | 回归 `room_city_test.go` |
| B4 | `config/config.go` | `[wealth]` 增 `city_driver_enabled/workers/per_month` + 零值归一 + clamp | conf.example 同步三键 |
| B5 | `game/wealth/city/driver_test.go`（**新建**） | 02 §9 八用例 | `go test ./game/wealth/city/` 绿 |

### 1.2 职业卡退役 + 建房收口（03 号契约）

| # | 文件 | 改动 |
|---|---|---|
| B6 | `api/wealth_api.go` | **整文件删除** |
| B7 | `router/router.go` / `main.go` | professions 路由 + `ProfessionAPI` 装配删除 |
| B8 | `profession/curated.go` | **整文件删除**；P16 四常量迁 `settlement.go` |
| B9 | `profession/synthetic.go`（**新建**） | `SyntheticCards(n, rng)` 确定性合成；接替三处兜底（loader/room/calibration） |
| B10 | `profession/loader.go` | `drawPairs` 回退改 synthetic；头注数字动态化复核 |
| B11 | `game/wealth/room.go` | `buildCardPoolLocked` 恒 docs（synthetic 兜底）；`Start` byID/偏好分支删；`SeatProfession` 全链删；`startedPayload["professions"]` 删；`pool` 字段删除（`WealthRoom`/`pendingOpts` 同步） |
| B12 | `game/wealth/room_city.go` | `startCityLocked` `pool=="docs"` 条件删除（恒锚定） |
| B13 | `service/room_service{,_crud,_wealth_fill}.go` | wealth 建房忽略 `agent_seats` → 合成 12 深度座位；`padWealthAgentSeats`/`wealthMinAgentSeats` 删；`AgentSeatConfig.Profession` 删；`clampWealthResidentCount` 新语义（缺省 10000 / <10→10 / 上限 clamp / 负 400）；`WealthRoomOptions.Pool` 删；列表/详情字段复核 |
| B14 | `api/room_api.go` | 响应删 `agent_seats_count`；`resident_count` 校验注释更新 |
| B15 | `ws/game_service_wealth_bot.go` | `RegisterBotSeats` 两参调用；防御补填注释更新 |
| B16 | `config/config.go` | `ProfessionPoolDefault` 字段删除（先验证 conf 解析宽松性） |
| B17 | 测试改造 | 03 §4 清单全量 |

### 1.3 门禁

```bash
cd ServerGo && go build -o LsmAgentGame main.go && go test ./...
# 03 §5 五条退役 grep 门禁
```

## 2. 前端改动清单（`ClientWeb/`，职责线 frontend-dev）

| # | 文件 | 改动 |
|---|---|---|
| F1 | `components/wealth/WealthCreateRoomModal.tsx` | 01 §1.2 六项删除；背景居民规模数值控件（min10 max100000 + 预设 10/1千/1万/10万，缺省 10000）；载荷删 `agent_seats`/`wealth.pool`；保留线路池信息行/月节拍/种子 |
| F2 | `types/api.ts` | `CreateRoomOptions` 补 `full_agent?: boolean`；`AgentSeatRequest` 保留（其他游戏用）；`wealth` 段类型同步删 pool |
| F3 | `types/wealth.ts` | `WealthRoomOptions.pool` 删；`WealthStartedFrame.professions` 删；`CURATED_PROFESSIONS` 删；`WealthCitySnapshot.driver?` 增；`RESIDENT_MIN=10` |
| F4 | `api/wealth.ts` | `fetchProfessions` + 三个 profession 类型删除（城市居民档案两接口保留） |
| F5 | `components/wealth/CityStatsPanel.tsx` | 「本月驱动 {n} 名居民」行（driver 块存在时） |
| F6 | `pages/WealthLobbyPage.tsx` | 创建载荷组装同步（去 agent_seats/pool）；lobbyHint 渲染不变（键值改） |
| F7 | `pages/WealthGamePage.tsx` | 顶栏 `🤖 {n}/{max}` + 文案键改值；seatsWaiting 键值改 |
| F8 | i18n（`wealthKeys.ts` + `locales/{zh-CN,en,ja}.ts`） | 01 §3 删/改/增三表，三语键集一致 |
| F9 | `styles/wealth.css` | 一览块样式（`.wealth-cards-grid`/`.wealth-card` 系）删除；数值控件样式如新增类按 §26 三件套 |

### 门禁

```bash
cd ClientWeb && npx tsc --noEmit && npm run build
grep -rn "焦点居民\|CURATED_PROFESSIONS\|fetchProfessions\|cardsTitle" src/ 零命中
```

## 3. 集成与发布（integration-tester）

1. **E2E 建城冒烟**（建房链路改动必跑）：
   - 验证码登录（`GET /api/captcha` → 正常登录路径）
   - `POST /api/games/wealth/rooms {name, resident_count: 50, wealth:{month_ms:3000}, full_agent:true}`（不带 agent_seats/pool）
   - 断言：房间自动开局；`game.state` 含 `city.resident_count=50`、`city.driver.enabled=true`；
     深度居民 12 名产生决策（bot_contexts 活跃）；≥1 条 `city_voice` 事件
   - 观察 ≥2 个月 tick；档案锚定进度事件 `city_profiles` 到达
2. `./rebuild_restart_app.sh` 重新编译运行；39001/39002 健康检查。
3. git 提交：`lag_docs` 子模块先 commit+push，主仓库提交指针 + 代码（中文提交信息）。

## 4. 回滚预案

- 不改经济数值与既有 WS 帧结构（新增字段全部 omitempty）→ 回滚 = revert 对应 commit。
- 驱动层开关 `city_driver_enabled=false` 即回退城市之声旧行为（运行时降级，无需回滚代码）。

## 5. 验收清单（总）

- [ ] `go build` + `go test ./...` 全绿（30+ 包）
- [ ] `tsc --noEmit` + `npm run build` 通过
- [ ] 03 §5 退役 grep 五条门禁通过
- [ ] §130 接线验证：`grep -rn "ResidentDriver\|PickDriverCandidates\|ApplyIntent\|SyntheticCards" ServerGo/` 各 ≥3 消费点
- [ ] E2E 建城冒烟（§3.1）通过
- [ ] `./rebuild_restart_app.sh` 服务健康
- [ ] 文档行数 ≤800（§3）；本目录 5 份文档入 README 索引
