# 虚拟城市 · 玩家职业设计目录结构与分片规约

> 本文件规定 `docs/虚拟城市/玩家职业设计/` 的目录形态、路径命名与**扩容路径**。
> 目标是让同一套路径方案可承载 **十万级 → 百万级 → 千万级 → 数十亿级** 的人员档案。

---

## 1. 顶层布局

```
docs/虚拟城市/玩家职业设计/
├── _框架/                       ← 规范、分类体系、索引（下划线前缀，与行业域目录区分）
│   ├── 00-总索引.md
│   ├── 01-人物卡字段字典.md
│   ├── 02-目录结构与分片规约.md      ← 本文件
│   ├── 03-行业分类体系-ICG.md + 03-行业分类体系/   ← 26 个域细分文件
│   ├── 04-数据来源与锚点.md + 04-数据来源与锚点/   ← 数据锚点分册
│   ├── 05-画像与阶层框架.md
│   ├── 06-档案清单/               ← 旧文件 → 新目录 全量映射表
│   ├── 07-历史归档/               ← v3.x 时代分类体系文档
│   ├── 08-人物卡富化规约.md       ← skeleton → rich 富化规则
│   ├── 09-新兴职业与灵活就业数据锚点.md   ← v4.3 调研
│   └── 10-维度索引/               ← v4.3 新增 D2–D7 六维切片索引
│       ├── README.md
│       ├── D2-收入阶层与资产画像.md
│       ├── D3-家庭结构与抚养压力.md
│       ├── D4-人生阶段与生命周期目标.md
│       ├── D5-职业风险类型与健康压力.md
│       ├── D6-资产形态与财商工具适配.md
│       ├── D7-地理流动与社保连续性.md
│       └── 维度交叉矩阵.md
├── _交付说明/
│   └── 20260913-v4.0-重构交付说明.md
│   └── 交付说明_20260914_v4.3_新增1500卡_薄弱行业.md
│   └── 交付说明_20260914_v4.4_归档合并与全息画像.md      ← v4.4 本批次新增
├── _脚本_与_报告/                                            ← v4.4 脚本与 JSON 报告落点
└── <L1行业域>/<L2行业细分>/<L3职业族>/<L4地区档>/<L5年龄段>/<编号>-<姓名>.md
```

**约定**：`_` 前缀目录**不是**行业域，一律排在行业域之前，便于工具与人工识别。

> **v4.4 备注（2026-09-14）**：**数字目录已消亡**。v3.x 时代曾尝试以 `01-19` 数字域替代字母域，
> 仅实施了 26 个数字目录约一半卡量即回退至字母树。v4.4 阶段 A 将 v3.x 数字目录下
> 全部 69,455 张卡按 frontmatter `industry_l1/l2/l3` 重推路径并入字母树；
> **数字目录路径形态**（`<数字>-<名>/<数字>-<名>/<4位码>-<名>/<L4>/<L5>/`）已彻底废弃，
> 仅作历史档案留存于 [07-历史归档/](07-历史归档/)。
>
> **`99_归档与待整理/` 目录**：v4.4 阶段 C 已删除（整目录 `git rm -r`）；
> 旧档案取源改指 [07-历史归档/](07-历史归档/) + git 历史锚点 `a790272e80`（数字目录归档态）与
> `9be9a05`（v3.4 扁平态）。

---

## 2. 路径五段定义

| 层 | 名称 | 取值来源 | 基数 |
|---|------|----------|------|
| **L1** | 行业域 | `03-行业分类体系-ICG.md` §2，单字母 `A`–`Z` | 26 |
| **L2** | 行业细分 | 同文档，`<L1><2位>` | 239（已用） |
| **L3** | 职业族 | 同文档，`<L2><2位>` | 2,224 |
| **L4** | 地区档 | `CN-N-华北` / `CN-NE-东北` / `CN-E-华东` / `CN-C-华中` / `CN-S-华南` / `CN-SW-西南` / `CN-NW-西北` / `OV-海外` / `XX-未知` | 9 |
| **L5** | 年龄段 | `16-24` / `25-34` / `35-44` / `45-54` / `55-64` / `65+` / `XX-未知` | 7 |
| **叶** | 人物卡 | `<编号>-<姓名>.md` | 1 人 |

**完整示例**

```
O-住宿与餐饮/O01-餐饮门店经营/O0101-餐厅店长/CN-S-华南/35-44/N1034000-陈砚青.md
```

### 2.1 路径段净化规则

目录段与文件名中的以下字符**必须**替换为 `_`，避免产生意外层级：

`/` `\` `:` `*` `?` `"` `<` `>` `|` 以及空白字符

> 例：L3 族名 `UI/UX设计` → 目录段 `P0701-UI_UX设计`。

---

## 3. 命名规则

| 对象 | 规则 | 示例 |
|---|---|---|
| L1 目录 | `<字母>-<行业域名>` | `R-专业服务` |
| L2 目录 | `<L2码>-<细分名>` | `R01-法律与合规` |
| L3 目录 | `<L3码>-<职业族名>` | `R0102-企业合规` |
| L4 目录 | `<地区码>` | `CN-E-华东` |
| L5 目录 | `<年龄段>` | `45-54` |
| 人物卡文件 | `<编号>-<姓名>.md` | `N1026362-栗景虞.md` |

- 编号为全局唯一卡号，`N` + 数字；重号冲突者统一改入 `N9000000+` 保留段，
  旧号完整保留在卡内 `_legacy_ids`。
- 姓名为 2–4 字化名，全局唯一；重名者经换字重建，旧名保留在 `_raw.legacy_name`。

---

## 4. 规模实测（v4.4 落地批次）

| 指标 | 数值 |
|---|---|
| 人物卡总数 | **75,115**（v4.4 实测，75,097 N + 18 P） |
| 其中 `richness: rich` 富化卡 | **1,002**（v4.2 1,000 + 归档 2） |
| 字段数（v1.1） | **65**（v1.0 59 + v1.1 6 新增） |
| 目录总数（v4.4 估算） | 约 110,000+（L1 26 · L2 239 · L3 2,223 · L4/L5 组合 ~107,728） |
| 平均单目录占用 | 约 0.7 个文件 |
| **数字目录** | **已消亡**（v4.4 阶段 A 全部并入字母树） |
| **`99_归档与待整理/`** | **已删除**（v4.4 阶段 C） |

> v4.4 后叶子目录占用仍偏低（多数 0–1 文件/目录）属**结构先行**：
> 路径方案按百万级设计，69,455 张新并入卡填补了大量稀疏目录但仍未饱和。
> 随着 v4.5+ 批次扩容，占用率将自然收敛到每目录 30–100 文件。

### 4.1 关键实测数字（v4.4 阶段 D 终验）

- **卡号零重叠**：字母树 75,097 + P18 与归档 69,455 全部唯一（实测脚本扫描）
- **化名零碰撞**：75,115 名（含 18 P）经种子化生成器（`SURNAMES_M/F + GIVEN_M/F + random.Random(seed=20260914)`）全局唯一
- **路径 100% 合规**：所有卡 5 段式目录 + 文件名命中 `^[NP]\d{2,8}-[^/]+\.md$`
- **L4 9 档**：CN-N / CN-NE / CN-E / CN-C / CN-S / CN-SW / CN-NW / OV-海外 / XX-未知
- **L5 7 档**：16-24 / 25-34 / 35-44 / 45-54 / 55-64 / 65+ / XX-未知
- **`richness: rich` 卡数**：1,002（其中 v4.2 1,000 黄金 + 归档 2）
- **Schema v1.1 字段覆盖**：74,113 skeleton 卡 100% 具备 6 新字段；1,002 rich 卡 §6/§7/§8 字节级零修改

---

## 5. 扩容路径（**核心不变式：路径前 5 段永不改变**）

规模增长时，**只切换叶子形态**，不动路径前缀。

| 规模 | 叶子形态 | 单目录文件数 | 切换动作 | 需改动 |
|------|----------|--------------|----------|--------|
| **十万级（当前）** | 一人一档 `N1034000-陈砚青.md` | 1–6 | — | — |
| **百万级** | 在 L5 下插入 **L6 分片目录** `S000`…`S999`，每片 ≤ 500 人 | 20–60 | 新增 L6 一层 | 分片号 = `floor(序号/500)` |
| **千万级** | L6 分片扩为 `S0000`…；或叶子改为**聚合档案** `S000-0001-0100.md`（每文件 ≤ 100 人） | 100–500 | 叶子由「人」改「人群」 | 聚合卡 `card_type: shard` |
| **数十亿级** | 目录树降级为**索引层**；全量数据入 GORM 表 `t_lsm_game_person_card`，目录树由生成器按需导出切片 | 索引层不承载全量 | 数据入库 + 按需导出 | 新增导出台账 |

### 5.1 每级扩容的具体做法

**百万级**
```
…/CN-E-华东/45-54/S000/N1026362-栗景虞.md
                    ↑ 新增分片层
```

**千万级（聚合档案）**
```
…/CN-E-华东/45-54/S000-0001-0100.md     ← 单文件含 100 人（10 张 8 列总览表 + 或折叠正文）
```
聚合档案的 YAML `card_type: shard`，`id` 形如 `SHARD-CN-E-华东-45-54-0000`，
每人在文件内的 `## 人物 <编号>` 小节中保留完整 59 字段。

**数十亿级**
引入数据库为唯一事实来源：

```
t_lsm_game_person_card(
  card_id, name, l1, l2, l3, region, age_band, ... 59 列,
  source_file, legacy_ids, completeness, grounded
)
```

目录树退化为**浏览/导出视图**：需要落盘时按 L1→L5 逐级导出，
每次只导出被请求的分片，磁盘上不存在「全量 65 亿个文件」这一形态。

> **为什么不能纯目录承载十亿级**：任何文件系统都撑不住十亿个 inode，
> 且 git 仓库会因 blob 数量爆炸而不可用。因此方案在千万级即切换到
> 「数据库为源 + 目录为视图」，这是唯一可扩展的形态。

---

## 6. 硬约束

1. **路径深度**：早期落地固定 5 段目录 + 1 个文件；进入百万级后为 6 段 + 1 文件。
2. **段名合法**：所有段必须通过 §2.1 净化规则，且命中 §2 的码表。
3. **编码**：全部 UTF-8 **无 BOM**。
4. **单文件行数**：人物卡 ≤ 1,800 行（CLAUDE.md §4）。
5. **框架文档**：`_框架/**` 除本规约外均为生成物，**不得手工编辑**；
   修改分类或字段请改 `scripts/虚拟城市知识库/` 下源码后重跑。
6. **追加而非改写**：新批次只能**新增** L1/L2/L3（在码表末尾续号），
   不得复用已废弃的码。

---

## 7. 校验清单（每批次必跑）

| # | 校验项 | 通过标准 |
|---|---|---|
| V1 | 卡号唯一 | 0 重复 |
| V2 | 姓名唯一 | 0 重复 |
| V3 | 路径合规 | 深度 = 5，各段命中码表，无非法字符 |
| V4 | YAML 可解析 | 100% |
| V5 | 完整度 | 每卡 `_completeness ≥ 0.80` |
| V6 | 信息零丢失 | 每卡含 `_raw.src_line` |
| V7 | 编码 | UTF-8 无 BOM |
| V8 | 映射完备 | 每个旧档案文件在 `_框架/06-档案清单/` 中有一行 |
