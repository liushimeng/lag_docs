# 模型管理与持久化玩家设计

> 版本：v1.0  |
> 作者：game-designer  |
> 状态：实施就绪  |
> 适用范围：LsmAgentGame 平台 8 款游戏中的 LLM 模型持久化 + 模型玩家化
>
> **核心目标**：将 LLM 模型从「配置文件条目」提升为「数据库里的持久化玩家」，
> 配套管理 UI、金币钱包、对局日志全链路。

---

## 1. 目标与背景

### 1.1 Context

LsmAgentGame 平台已上线 8 个 LLM 模型（豆包 / 智谱 / Kimi / MiniMax 等），统一通过
`ServerGo/llm/registry.go` 加载供狼人杀 Agent 调度。原实现存在三大短板：

| 短板 | 现象 | 影响 |
|------|------|------|
| 配置入口单一 | 增删改必须 SSH 改 `LsmAgentGame.conf` + 重启 | 运维门槛高、变更不可追溯、误改风险大 |
| 模型是「临时算力」 | 7 个 bot 调 LLM 后无任何持久化数据 | 无法复盘 / 调优 prompt / 分析决策 |
| 模型无法参与金币 | 5 款游戏的金币体系无法结算到模型 | 模型不是「玩家」 |

### 1.2 设计目标

1. **DB-first 配置** — 模型元数据走 `t_lsm_game_llm_provider` 表 + HTTP CRUD；
   `LsmAgentGame.conf` 中 `providers[]` 标 deprecated，仅作为首次 seed 来源。
2. **模型即玩家** — 每个模型对应一个 `t_lsm_game_user` 中 `IsBot=true` 的行，
   享有与人类玩家完全相同的钱包 / 流水 / 对局日志。
3. **可复盘** — `t_lsm_game_model_game_log` + `t_lsm_game_model_chat_message` +
   `t_lsm_game_model_action` 三张表共同记录每局决策。
4. **可结算** — 狼人杀对局结束后，按胜/败给所有 bot 玩家自动结算金币（±100），
   走 `WalletService.ApplyTransaction` 双簿记。
5. **可审计** — admin 通过 `/admin/models` UI 全程管理，super 可手动 adjust 钱包。

### 1.3 与本仓其他文档的关系

| 文档 | 关系 |
|------|------|
| `docs/LLM与Agent/LLM供应商设计.md` | **总规约**：Provider 接口 / Registry / HTTP API / 安全 |
| `docs/狼人杀-道具与经济/模型玩家金币设计.md` | **产品规约**：金币体系、结算规则、admin adjust |
| `docs/狼人杀-Agent与系统/狼人杀Agent设计.md` | 狼人杀 Agent 内部机制（in-process 驱动、限流、记忆） |
| `docs/中国象棋金币设计.md` 等 5 款游戏金币设计 | 人类玩家的金币体系（模型沿用相同 ledger） |
| CLAUDE.md §14 | LLM Provider 模块（已被本期扩展） |

---

## 2. 数据库 5 张表 + 1 张扩展

### 2.1 `t_lsm_game_llm_provider` — 模型元数据

每个 LLM 模型一行，**由模型管理页面直接 CRUD**。API Key **必须加密存储**。

```go
// ServerGo/models/t_lsm_game_llm_provider.go
type TLsmGameLlmProvider struct {
    ID               string    `gorm:"type:char(36);primaryKey"            json:"id"`
    AgentName        string    `gorm:"type:varchar(64);uniqueIndex;not null"  json:"agent_name"`     // "豆包 2.0"
    Model            string    `gorm:"type:varchar(64);uniqueIndex;not null"  json:"model"`         // "DouBao-model"
    ProviderType     string    `gorm:"type:varchar(32);not null;default:'anthropic'"  json:"provider_type"`
    APIKeyEnc        string    `gorm:"type:text;not null"                  json:"-"`         // AES-GCM 加密,永不出库到前端
    APIKeyHint       string    `gorm:"type:varchar(16);not null;default:''"  json:"api_key_hint"`  // "sk-abcd...wxyz"
    Endpoint         string    `gorm:"type:varchar(256);not null;default:''" json:"endpoint"`     // 覆盖全局 endpoint,空则用全局
    ThinkingRequired bool      `gorm:"not null;default:false"              json:"thinking_required"`
    ThinkingBudget   int       `gorm:"not null;default:0"                  json:"thinking_budget"`
    Enabled          bool      `gorm:"not null;default:true"               json:"enabled"`
    Remark           string    `gorm:"type:varchar(255);default:''"        json:"remark"`
    CreatedAt        time.Time `gorm:"autoCreateTime"                       json:"created_at"`
    UpdatedAt        time.Time `gorm:"autoUpdateTime"                       json:"updated_at"`
}
```

**索引**：

| 索引 | 列 | 用途 |
|------|----|------|
| `PRIMARY` | `id` | 主键 |
| `uniq_agent_name` | `agent_name` | 唯一约束 |
| `uniq_model` | `model` | 唯一约束（**核心关联键**） |
| `idx_enabled` | `enabled` | 过滤可用模型（狼人杀随机分配用） |

**Model key 归一化（R187-2）**：`model` 列在所有信任边界统一经
`util.SanitizeModelKey` 归一化 —— `strings.TrimSpace` + 剔除全部 Unicode
格式类（Cf）字符（U+200B ZWSP / U+200C ZWNJ / U+200D ZWJ / U+2060 Word Joiner /
U+FEFF BOM / U+00AD Soft Hyphen）。适用位置：Admin CRUD
（`CreateProvider`/`UpdateProvider`）、registry 配置 seed 与 DB 加载
（`seedFromConfigLocked`/`populateLocked`）、房间创建校验
（`ValidateAgentSeats`，原地改写 seat 的 ModelKey）、配置池
（`usableProviderModels`/`alternateModelsLocked`）。背景：曾有 `model` 列被
管理 UI 粘贴进一个肉眼不可见的 ZWNJ（显示为 `Tencent-model` 但实际是
`Tencent<U+200C>-model`），导致 ASCII 形式的 key 全部校验失败。归一化后若与
既有行冲突，由 `uniq_model` 唯一索引兜底报 duplicate。历史脏行在 registry
加载时被透明映射为干净 key，无需手工修库。


**AES-GCM 加密字段说明**：

- `api_key_enc`：AES-GCM 加密后的 base64 密文；`json:"-"` 永不出现在 JSON 响应中。
- `api_key_hint`：key 的前后 4 位明文摘要，例 `sk-abcd...wxyz`，用于 UI 展示。
- 加密密钥：32 字节随机 key 的 base64，存于 `t_lsm_game_kv['llm_encryption_key']`。
  首次启动自动生成；**丢失后所有 provider 需重新 seed**。

### 2.2 `t_lsm_game_user` 扩展字段

不新建表，复用现有 `t_lsm_game_user`，新增 3 字段：

```go
// ServerGo/models/t_lsm_game_user.go（追加）
IsBot                  bool       `gorm:"not null;default:false;index"        json:"is_bot"`
BotProviderID          *string    `gorm:"type:char(36);index"                 json:"bot_provider_id,omitempty"`
LinkedProviderAccount  string     `gorm:"type:varchar(64);not null;default:''"  json:"linked_provider_account"`
```

**字段语义**：

| 字段 | 含义 |
|------|------|
| `IsBot` | 标记这是模型玩家，非人类；**禁止人类用 bot 账号登录** |
| `BotProviderID` | 外键指向 `t_lsm_game_llm_provider.id`，可空（人类用户为 NULL） |
| `LinkedProviderAccount` | 虚拟账号字符串，用于日志和诊断；可与 `account` 相同 |

**backfill**：现有行 `IsBot=false`、`BotProviderID=nil`、`LinkedProviderAccount=''`，无需迁移。

### 2.3 `t_lsm_game_model_game_log` — 模型对局日志

每个 bot 玩家在每局游戏里一条主记录：

```go
// ServerGo/models/t_lsm_game_model_game_log.go
type TLsmGameModelGameLog struct {
    ID            string     `gorm:"type:char(36);primaryKey"  json:"id"`
    ProviderID    string     `gorm:"type:char(36);index:idx_provider_created,priority:1;not null"  json:"provider_id"`
    BotUserID     string     `gorm:"type:char(36);index;not null"  json:"bot_user_id"`
    RoomID        string     `gorm:"type:char(36);index;not null"  json:"room_id"`
    GameKind      string     `gorm:"type:varchar(32);index;not null"  json:"game_kind"`     // xiangqi/chess/junqi/doudizhu/texasholdem/werewolf
    Seat          int        `gorm:"not null"            json:"seat"`
    Role          string     `gorm:"type:varchar(32);not null;default:''"  json:"role"`        // werewolf 里:werewolf/seer/witch/...
    StartedAt     time.Time  `gorm:"index:idx_provider_created,priority:2"  json:"started_at"`
    EndedAt       *time.Time `                              json:"ended_at,omitempty"`
    Result        string     `gorm:"type:varchar(32);not null;default:''"  json:"result"`     // win/lose/draw/abandoned
    CoinDelta     int64      `gorm:"not null;default:0"   json:"coin_delta"`     // 该局金币变化
    LLMCallCount  int        `gorm:"not null;default:0"   json:"llm_call_count"`
    InputTokens   int        `gorm:"not null;default:0"   json:"input_tokens"`
    OutputTokens  int        `gorm:"not null;default:0"   json:"output_tokens"`
    FinalHand     string     `gorm:"type:varchar(255);default:''"  json:"final_hand"`   // 残局/最终手牌摘要
}
```

**索引**：

| 索引 | 列 | 用途 |
|------|----|------|
| `PRIMARY` | `id` | 主键 |
| `idx_provider_created` | `(provider_id, started_at)` | 「按模型看时间序列对局」最热查询 |
| `idx_bot_user` | `bot_user_id` | 单 bot 玩家聚合 |
| `idx_room` | `room_id` | 按房间聚合（7 bot 局 7 行） |
| `idx_game_kind` | `game_kind` | 按游戏类型筛选 |

### 2.4 `t_lsm_game_model_chat_message` — 模型聊天原文

每条 LLM 输出/输入一条记录。**不**复用 `t_lsm_game_chat_message`（那个是公开聊天；这个含
thinking / system / tool_use 完整内容）。

```go
// ServerGo/models/t_lsm_game_model_chat_message.go
type TLsmGameModelChatMessage struct {
    ID         uint64    `gorm:"primaryKey;autoIncrement"  json:"id"`
    GameLogID  string    `gorm:"type:char(36);index:idx_gamelog_seq,priority:1;not null"  json:"game_log_id"`
    BotUserID  string    `gorm:"type:char(36);index;not null"  json:"bot_user_id"`
    ProviderID string    `gorm:"type:char(36);index;not null"  json:"provider_id"`
    RoomID     string    `gorm:"type:char(36);index;not null"  json:"room_id"`
    Seq        int64     `gorm:"not null;index:idx_gamelog_seq,priority:2"  json:"seq"`     // 该 game_log 内的递增序号
    Role       string    `gorm:"type:varchar(16);not null"  json:"role"`        // user/assistant/tool_result/system
    Content    string    `gorm:"type:mediumtext;not null"   json:"content"`     // 完整文本(JSON for tool blocks)
    Phase      string    `gorm:"type:varchar(32);not null;default:''"  json:"phase"`
    ToolName   string    `gorm:"type:varchar(64);not null;default:''"  json:"tool_name"`
    ToolInput  string    `gorm:"type:text;not null"         json:"tool_input"`
    Thinking   string    `gorm:"type:mediumtext;not null"   json:"thinking"`    // 思考块
    StopReason string    `gorm:"type:varchar(32);not null;default:''"  json:"stop_reason"`
    LatencyMs  int       `gorm:"not null;default:0"        json:"latency_ms"`
    CreatedAt  time.Time `gorm:"autoCreateTime"             json:"created_at"`
}
```

**索引**：

| 索引 | 列 | 用途 |
|------|----|------|
| `PRIMARY` | `id` | 主键 auto-increment |
| `idx_gamelog_seq` | `(game_log_id, seq)` | 「按 game_log 拉时序聊天」 |
| `idx_bot_provider` | `(bot_user_id, provider_id)` | 按 bot 玩家聚合 |
| `idx_room` | `room_id` | 房间维度排查 |

### 2.5 `t_lsm_game_model_action` — 模型动作决策记录

每条工具调用/游戏动作一行。**关键决策数据**。

```go
// ServerGo/models/t_lsm_game_model_action.go
type TLsmGameModelAction struct {
    ID           uint64    `gorm:"primaryKey;autoIncrement"  json:"id"`
    GameLogID    string    `gorm:"type:char(36);index:idx_gamelog_phase,priority:1;not null"  json:"game_log_id"`
    BotUserID    string    `gorm:"type:char(36);index;not null"  json:"bot_user_id"`
    Phase        string    `gorm:"type:varchar(32);index:idx_gamelog_phase,priority:2;not null"  json:"phase"`
    ActionType   string    `gorm:"type:varchar(32);index;not null"  json:"action_type"`   // speak/vote/wolf_kill/seer_check/witch_act/...
    ActionTarget string    `gorm:"type:varchar(64);not null;default:''"  json:"action_target"` // 目标座位/牌/玩家
    Payload      string    `gorm:"type:text;not null"         json:"payload"`        // 完整动作 JSON
    Reasoning    string    `gorm:"type:mediumtext;not null"   json:"reasoning"`      // 模型解释/思考
    Accepted     bool      `gorm:"not null;default:true"      json:"accepted"`
    CreatedAt    time.Time `gorm:"autoCreateTime"             json:"created_at"`
}
```

**索引**：

| 索引 | 列 | 用途 |
|------|----|------|
| `PRIMARY` | `id` | 主键 auto-increment |
| `idx_gamelog_phase` | `(game_log_id, phase)` | 单局按时段聚合 |
| `idx_action_type` | `action_type` | 按动作类型筛选 |
| `idx_bot` | `bot_user_id` | 单 bot 玩家聚合 |

### 2.6 `t_lsm_game_kv` — 通用 KV 存储

存 AES-GCM 加密密钥等敏感配置。

```go
// ServerGo/models/t_lsm_game_kv.go
type TLsmGameKV struct {
    Key       string    `gorm:"type:varchar(64);primaryKey"  json:"key"`
    Value     string    `gorm:"type:text;not null"           json:"value"`
    UpdatedAt time.Time `gorm:"autoUpdateTime"               json:"updated_at"`
}
```

**约定 Key**：

| Key | Value | 说明 |
|-----|-------|------|
| `llm_encryption_key` | 32 字节随机 key 的 base64 | AES-GCM 主密钥；首次启动生成；**丢失后所有 provider 需重新 seed** |

### 2.7 索引策略总结

| 查询模式 | 命中索引 | 表 |
|---------|---------|-----|
| 按 `model` 查 Provider | `uniq_model` | `t_lsm_game_llm_provider` |
| 按 `enabled` 过滤可用模型 | `idx_enabled` | `t_lsm_game_llm_provider` |
| 按模型看时间序列对局 | `idx_provider_created` | `t_lsm_game_model_game_log` |
| 单局聊天时序 | `idx_gamelog_seq` | `t_lsm_game_model_chat_message` |
| 单局按时段动作 | `idx_gamelog_phase` | `t_lsm_game_model_action` |
| 按 bot 玩家聚合 | `idx_bot_user` / `idx_bot_provider` | 3 张日志表 |
| 按房间聚合 | `idx_room` | 3 张日志表 |

---

## 3. 后端模块拆分

### 3.1 后端新建文件

| 文件 | 一句话职责 |
|------|----------|
| `ServerGo/models/t_lsm_game_llm_provider.go` | `TLsmGameLlmProvider` GORM 模型 |
| `ServerGo/models/t_lsm_game_model_game_log.go` | `TLsmGameModelGameLog` GORM 模型 |
| `ServerGo/models/t_lsm_game_model_chat_message.go` | `TLsmGameModelChatMessage` GORM 模型 |
| `ServerGo/models/t_lsm_game_model_action.go` | `TLsmGameModelAction` GORM 模型 |
| `ServerGo/models/t_lsm_game_kv.go` | `TLsmGameKV` 通用 KV 模型 |
| `ServerGo/util/crypto.go` | AES-GCM `EncryptAPIKey` / `DecryptAPIKey` |
| `ServerGo/service/bot_user_service.go` | `EnsureBotUserForProvider` 注册 bot 玩家 |
| `ServerGo/service/model_log_service.go` | 对局日志查询 / 聚合 |
| `ServerGo/agent/record_log.go` | `RecordGameAction` / `RecordChatMessage` hook |
| `ServerGo/api/model_admin_api.go` | Provider CRUD + Reload + Test 6 个端点 |
| `ServerGo/api/model_log_api.go` | 对局 / 消息 / 钱包查询 4 个端点 |
| `ServerGo/api/model_wallet_api.go` | 管理员手动 adjust 1 个端点（super only） |

### 3.2 后端修改文件

| 文件 | 修改点 |
|------|--------|
| `ServerGo/llm/registry.go` | 新签名 `NewRegistry(cfg, gormDB)`、DB-first 加载、`Reload(ctx)`、`SeedFromConfig` |
| `ServerGo/db/db.go` | `Init` 中注册 5 张新表 + backfill `t_lsm_game_user` 3 字段 |
| `ServerGo/models/t_lsm_game_user.go` | 加 `IsBot` / `BotProviderID` / `LinkedProviderAccount` |
| `ServerGo/agent/tools.go` | `DispatchTool` 成功后调 `RecordGameAction` |
| `ServerGo/agent/run.go` | `handleSpeakFloorTick` / `speak` tool 后调 `RecordGameAction` |
| `ServerGo/ws/chat_service.go` | `SendFromBot` 同步写 `t_lsm_game_model_chat_message` |
| `ServerGo/llm/anthropic/stream.go` | `ChatStream` 出口写 `t_lsm_game_model_chat_message` |
| `ServerGo/game/werewolf/activity_emitter.go` | `EmitGameOver` 内金币结算（详见 `docs/狼人杀-道具与经济/模型玩家金币设计.md`） |
| `ServerGo/main.go` | 注册新路由 + `service.SetBotUserService` + `llm.NewRegistry(cfg, gormDB)` |
| `ServerGo/config/config.go` | `LLMConfig` 加 `EncryptionKey` 字段；`providers[]` 标 deprecated |
| `LsmAgentGame.conf.example` | 清空 `providers[]`，加注释 |

### 3.3 Agent 接入点（关键 hook 位置）

`ServerGo/agent/record_log.go` 提供 2 个核心方法 + 多个接入点：

```go
// ServerGo/agent/record_log.go
package agent

type Recorder struct {
    gormDB *gorm.DB
}

func NewRecorder(gormDB *gorm.DB) *Recorder

// RecordGameAction 记录一条动作决策。失败仅 log,不阻塞游戏流程(背景 goroutine)。
func (r *Recorder) RecordGameAction(gameLogID, botUserID, providerID, phase, actionType, target, reasoning string, payload []byte)

// RecordChatMessage 记录一条 LLM 聊天原文(完整 request+response)。失败仅 log。
func (r *Recorder) RecordChatMessage(gameLogID, botUserID, providerID, roomID string, seq int64, msg types.Message, phase, thinking, stopReason string, latencyMs int)
```

**接入点 1：`agent/tools.go::DispatchTool`**
- 每个工具调用成功后调 `RecordGameAction`
- 接入示例：
  ```go
  func (a *Agent) DispatchTool(toolName string, input map[string]any) (any, error) {
      result, err := a.toolRunner.HandleTool(a.ctx, toolName, input)
      if err == nil {
          payload, _ := json.Marshal(input)
          a.recorder.RecordGameAction(a.gameLogID, a.botUserID, a.providerID, a.phase, toolName, fmt.Sprint(input["target"]), "", payload)
      }
      return result, err
  }
  ```

**接入点 2：`agent/run.go::handleSpeakFloorTick` / `speak` tool**
- speak 类动作也走 `RecordGameAction`，`action_type="speak"`

**接入点 3：`ServerGo/ws/chat_service.go::SendFromBot`**
- 聊天原文同步写 `t_lsm_game_model_chat_message`
- 复用同一 `Recorder` 实例（注入到 `ChatService`）

**接入点 4：`ServerGo/llm/anthropic/stream.go::ChatStream` 出口**
- 完整 request+response 写 `t_lsm_game_model_chat_message`
- 含 thinking / tool_use 完整内容

**接入点 5：`ServerGo/game/werewolf/activity_emitter.go::EmitGameOver`**
- 结算金币 + 更新 `t_lsm_game_model_game_log.ended_at / result / coin_delta`

---

## 4. HTTP API 全表

### 4.1 Provider 管理 API（`ServerGo/api/model_admin_api.go`）

| 方法 | 路径 | 权限 | 功能 |
|------|------|------|------|
| `GET`    | `/api/admin/llm/providers` | admin | 列出所有模型（`enabled=true/false` 均可，含 hint/endpoint/thinking，**不含 api_key**） |
| `POST`   | `/api/admin/llm/providers` | admin | 新建模型（自动 seed bot_user + wallet；立即 `Reload`） |
| `PUT`    | `/api/admin/llm/providers/:id` | admin | 更新模型（热生效；走 `Reload`） |
| `DELETE` | `/api/admin/llm/providers/:id` | admin | 软删除（`Enabled=false`，不真删；保留 bot 历史；走 `Reload`） |
| `POST`   | `/api/admin/llm/providers/:id/test` | admin | 发一个 dry-run ping（构造最小 `LLMRequest` + 1 token 输出） |
| `POST`   | `/api/admin/llm/providers/reload` | admin | 显式触发 Registry 从 DB 重新加载 |

### 4.2 对局日志 API（`ServerGo/api/model_log_api.go`）

| 方法 | 路径 | 权限 | 功能 |
|------|------|------|------|
| `GET` | `/api/admin/llm/providers/:id/games` | admin | 该模型的所有对局（分页 `?page=1&size=20` + 时间筛选 `?from=&to=`） |
| `GET` | `/api/admin/llm/games/:gameLogID` | admin | 单局详情（参与 chat / action 聚合） |
| `GET` | `/api/admin/llm/games/:gameLogID/messages` | admin | 该局完整聊天 / 思考 / 动作时间线（`?from_seq=&limit=200`） |
| `GET` | `/api/admin/llm/bots/:botUserID/wallet` | admin | bot 钱包余额 + 最近 20 条流水 |

### 4.3 钱包调整 API（`ServerGo/api/model_wallet_api.go`）

| 方法 | 路径 | 权限 | 功能 |
|------|------|------|------|
| `POST` | `/api/admin/llm/bots/:botUserID/wallet/adjust` | **super** | 管理员手动加减金币（写 ledger `tx_type=admin_adjust`） |

### 4.4 通用鉴权与错误码

| 错误码 | 含义 |
|--------|------|
| `30010` | `ErrProviderNotFound` — model_id 不存在 |
| `30011` | `ErrProviderAgentNameTaken` — agent_name 重复 |
| `30012` | `ErrProviderModelKeyTaken` — model 字段重复 |
| `30013` | `ErrProviderKeyInvalid` — 占位符或解密失败 |
| `30014` | `ErrProviderEncryptionKeyMissing` — t_lsm_game_kv 无 llm_encryption_key |
| `30015` | `ErrProviderTestFailed` — `/test` 端点 dry-run 失败 |
| `30020` | `ErrBotAccountLoginForbidden` — 人类尝试登录 bot 账号 |
| `30021` | `ErrBotWalletAdjustForbidden` — admin 权限不足（需 super） |

**鉴权硬约束**：
- `admin` 角色可看 + 改模型元数据、查日志、看钱包流水。
- `super` 角色才可调 `/wallet/adjust`（防止 admin 误改 bot 余额）。
- 钱包调整**必须**走 `WalletService.ApplyTransaction`（ledger 双簿记），**禁止**直接 UPDATE balance。

---

## 5. 前端页面与路由

### 5.1 菜单入口

`ClientWeb/src/components/layout/AppSidebar.tsx` 在 `nav.adminUsers` 之前插入：

```tsx
{ to: '/admin/models', labelKey: 'nav.adminModels', icon: '🤖', minUserType: 2 },
```

i18n 新增 key：`nav.adminModels = 模型管理`（zh-CN / en / ja 三语）

### 5.2 路由

`ClientWeb/src/App.tsx` 新增：

```tsx
<Route path="/admin/models" element={<ModelAdminPage />} />
<Route path="/admin/models/:providerId" element={<ModelDetailPage />} />
<Route path="/admin/models/:providerId/games/:gameLogId" element={<ModelGameLogPage />} />
```

### 5.3 `ModelAdminPage.tsx` — 模型列表

- 表格列：`AgentName` / `Model` / `ProviderType` / `APIKeyHint` / `Enabled` / `UpdatedAt` / 操作
- 操作列按钮：编辑 / 测活（POST `/test`） / 软删 / 重新加载（POST `/reload`）
- 顶部「+ 新增模型」按钮 → 弹窗表单：
  - `AgentName`（必填，唯一）
  - `Model`（必填，唯一）
  - `ProviderType`（下拉，anthropic / openai）
  - `APIKey`（必填，明文 → 后端 AES-GCM 加密入库）
  - `Endpoint`（可选，留空用全局）
  - `ThinkingRequired`（勾选） + `ThinkingBudget`（数字）
  - `Remark`（可选）

### 5.4 `ModelDetailPage.tsx` — 模型详情

- **顶部信息卡**：表单字段只读（点击「编辑」切到编辑态）
- **中部「💰 钱包」区块**：
  - 余额大字（来自 `GET /wallet`）
  - 最近 20 条流水（time / type / amount / balance_after / remark）
- **底部「🎮 对局历史」表格**：
  - 列：`GameKind` / `StartedAt` / `Result` / `CoinDelta` / `LLMCallCount` / 操作
  - 操作列：查看详情（跳 `ModelGameLogPage`）

### 5.5 `ModelGameLogPage.tsx` — 单局时间线

- 顶部游戏信息条：Bot 昵称 / Model / 房间号 / 起止时间 / Result / CoinDelta
- 时间线视图：左侧 phase，右侧该 phase 内 chat_message + action 双列表
  - 聊天卡：`role=user/assistant`，`thinking` 折叠展开，`tool_use` 块用彩色边框
  - 动作卡：`ActionType` + `Target` + `Reasoning`（折叠显示完整 LLM 解释）
- 顶部 filter 开关：
  - 「仅显示工具调用」—— 隐藏 `Role=user/system`
  - 「仅显示思考块」—— 隐藏非 thinking 行
  - 「仅显示动作」—— 切换到 `t_lsm_game_model_action` 单列表
- 默认按 `Seq` 升序，可跳到指定 seq

### 5.6 通用约束

- 复用 `useAuthStore` / `useUiStore` 已有模式
- i18n key 全部走 `useT()`，加到 `ClientWeb/src/i18n/zh-CN.ts` + `en.ts` + `ja.ts`
- 样式：复用现有 `app-page / app-card / table` 等通用 CSS（参考 `AdminUsersPage.tsx`）
- 提交前 `tsc --noEmit` + `npm run build` 必须通过

---

## 6. 测试矩阵

### 6.1 单元测试清单

| 测试文件 | 测试函数 | 验证点 |
|---------|---------|--------|
| `ServerGo/llm/registry_db_test.go` | `TestRegistry_LoadFromDB` | DB 有 N 行 → Registry 加载 N 条；key 正确解密 |
| `ServerGo/llm/registry_db_test.go` | `TestRegistry_SeedFromConfig_OnEmptyDB` | DB 空 + cfg 有 8 条 → 写库后正好 8 条；二次启动不再 seed |
| `ServerGo/llm/registry_db_test.go` | `TestRegistry_ReloadAfterCRUD` | 调 Reload → 新增/更新的 model 立即在 `ListEnabled()` 中可见 |
| `ServerGo/llm/registry_db_test.go` | `TestRegistry_PlaceholderKey` | `api_key=API-KEY-PLACEHOLDER` → `Get()` 返回 `ErrProviderKeyInvalid` |
| `ServerGo/llm/registry_db_test.go` | `TestRegistry_EncryptionKeyMissing` | 删 `t_lsm_game_kv['llm_encryption_key']` → `Get()` 返回 `ErrEncryptionKeyMissing` |
| `ServerGo/util/crypto_test.go` | `TestEncryptDecryptAPIKey_RoundTrip` | Encrypt → Decrypt = 原值 |
| `ServerGo/util/crypto_test.go` | `TestEncryptAPIKey_DifferentCiphertext` | 相同 plaintext 两次加密 → 不同密文（GCM nonce） |
| `ServerGo/service/bot_user_service_test.go` | `TestEnsureBotUserForProvider_New` | 调一次 → user / wallet 各新增 1 行，余额 1000 |
| `ServerGo/service/bot_user_service_test.go` | `TestEnsureBotUserForProvider_Idempotent` | 调两次 → 仍 1 个 user，BotProviderID 一致 |
| `ServerGo/service/bot_user_service_test.go` | `TestBotUser_CannotLoginAsHuman` | `POST /api/auth/login` with bot account → 30020 |
| `ServerGo/service/model_log_service_test.go` | `TestListGamesByProvider_Pagination` | 分页参数生效，total 正确 |
| `ServerGo/agent/record_log_test.go` | `TestRecordGameAction_PersistsToDB` | 调 `DispatchTool('speak', ...)` → `t_lsm_game_model_action` 有 1 行 |
| `ServerGo/agent/record_log_test.go` | `TestRecordChatMessage_PersistsToDB` | 调 `Chat` mock → `t_lsm_game_model_chat_message` 有完整原文（含 thinking） |
| `ServerGo/agent/record_log_test.go` | `TestRecordLog_FailureDoesNotBlockGame` | DB 写失败（mock error）→ 游戏流程不 panic，仅 log |
| `ServerGo/game/werewolf/model_coin_settle_test.go` | `TestGameOver_BotCoinSettlement` | 7-bot 局结束 → 7 个 wallet delta 正确（胜方+100，败方-100），ledger 双写 |
| `ServerGo/game/werewolf/model_coin_settle_test.go` | `TestGameOver_AbandonedNoChange` | 房间异常中断 → 写 ledger `tx_type=game_abandon_no_change`，amount=0 |

### 6.2 集成测试

`ServerGo/api/model_admin_api_test.go`：
- `TestCreateProvider_TriggersBotUserAndWallet` — POST `/api/admin/llm/providers` → 验证 user + wallet 自动建出
- `TestUpdateProvider_KeyRotation` — 改 api_key → 旧 key 立即失效，新 key 生效
- `TestDeleteProvider_SoftDeletePreservesHistory` — DELETE → `enabled=false`，bot user / wallet / 历史日志完整
- `TestReloadRegistry_PicksUpChanges` — 手动改 DB → POST `/reload` → Registry 同步

### 6.3 前端类型检查与构建

- `tsc --noEmit` 必须通过（无 TS 错误）
- `npm run build` 必须成功（无 Vite 警告）

---

## 7. 验证流程

### 7.1 单元 / 编译测试

```bash
cd /usr/local/LsmAgentGame/ServerGo && go test ./...   # 必须全绿
cd /usr/local/LsmAgentGame/ClientWeb && npx tsc --noEmit && npm run build   # 必须通过
```

### 7.2 端到端烟测（基于 `docs/通用功能/测试账号凭证.md`）

1. **启动**：`./rebuild_restart_app.sh`（脚本已存在）
2. **登录 admin**：`POST /api/auth/login` (test_01 / admin token)
3. **创建模型**：`POST /api/admin/llm/providers` (新增 1 个测试模型 `Test-Model` / `Test-model` / 测试 API key)
4. **验证 DB**：
   - `SELECT * FROM t_lsm_game_llm_provider WHERE model='Test-model'` 有一行
   - `t_lsm_game_user` 有 `bot_test_model` 行
   - `t_lsm_game_wallet` 有对应 user_id 的行，balance=1000
5. **创建 7-bot 房间**：`POST /api/games/werewolf/rooms` with `agent_seats=[Test-model x7]`
6. **等游戏结束 → 验证**：
   - `t_lsm_game_model_game_log` 有 7 条记录（每个 bot 一条）
   - `t_lsm_game_model_chat_message` 有 ~50 条/局（覆盖 chat / decision / action）
   - `t_lsm_game_model_action` 有 ~10 条/局（vote / speak / wolf_kill 等）
   - `t_lsm_game_wallet_tx` 有 ±100 的结算流水
7. **UI 验证**：浏览器打开 `https://127.0.0.1:39001/admin/models`
   - 表格列出 8 个原有 + 1 个 Test-model
   - 点 Test-model → 详情页看对局历史
   - 点任一对局 → 时间线展示完整 chat / action
8. **软删除**：`DELETE /api/admin/llm/providers/Test-model` → `Enabled=false` →
   新建房间时该模型不再出现在可选列表，但历史数据保留

### 7.3 自动化测试入口

- 写一个 `ServerGo/scripts/seed_test_models.sh`，启动时若 DB 为空自动 seed 8 个测试模型
- 跑 `go test ./llm/... ./agent/... ./game/werewolf/...` 全绿

### 7.4 Git 提交

按 CLAUDE.md §10：「编译 → 测试 → 小步提交」拆 7 个 commit：

1. `models: 新增 5 张表 (LLM provider / game_log / chat / action / kv)`
2. `crypto: AES-GCM 加解密工具 + t_lsm_game_kv 存储`
3. `llm/registry: DB-first 加载 + Reload API + seed from config`
4. `agent/service: bot_user_service + record_log hook`
5. `api: /api/admin/llm/* 全套管理接口 + werewolf game_over 结算`
6. `frontend: ModelAdminPage + 详情页 + 日志时间线 + 菜单/路由/i18n`
7. `docs: 模型管理设计 + 模型玩家金币设计 + CLAUDE.md 更新`

提交后执行 `./rebuild_restart_app.sh` 启动并烟测。

---

## 8. 风险与回滚

### 8.1 API Key 加密密钥丢失

- **场景**：`t_lsm_game_kv['llm_encryption_key']` 行被误删；DB 备份还原时间差导致 key 不匹配。
- **现象**：所有 `Get(modelKey)` 返回 `ErrProviderEncryptionKeyMissing`；LLM 调用全部失败。
- **回滚路径**：
  1. 紧急恢复 key：若 DB 备份可拿到旧 key 直接写回
  2. 重新 seed：清空 `t_lsm_game_llm_provider` + `t_lsm_game_user` (IsBot=true) +
     `t_lsm_game_wallet` (bot_user_id 关联的) + `t_lsm_game_model_*` 历史 → 重新调
     `SeedFromConfig(cfg)` → 重新调 `EnsureBotUserForProvider` × 8
  3. **历史日志无法恢复**（chat_message 内的密文是当时的 key 加密的）

### 8.2 Registry Reload 期间正在调用的 LLM

- **场景**：admin 在 UI 上改 API key → 后台 `Reload()` → 旧 provider 实例被替换。
- **现象**：**正在进行的** `Chat` / `ChatStream` 调用继续走旧实例至完成；**新发起**的调用走新实例。
- **回滚路径**：无需回滚（设计如此）；若新 key 配置错误，最坏 30s 后单次调用超时返回 `ErrProviderKeyInvalid`。

### 8.3 Bot 用户误用人类登录

- **场景**：人类玩家 `POST /api/auth/login` with `bot_doubao_model` 密码
- **现象**：返回 `ErrBotAccountLoginForbidden = 30020`
- **回滚路径**：N/A（设计如此）

### 8.4 对局日志表过大

- **场景**：7-bot 房间每天 1000 局，3 张表每天各增 ~50K 行；半年后单表 ~9M 行
- **现象**：查询变慢，磁盘占用增加
- **回滚路径**：
  - 短期：3 张日志表按 `created_at` 做分区（MySQL/MariaDB 需开启分区支持）
  - 长期：90 天前的 `chat_message` 归档到 `t_lsm_game_model_chat_message_archive`，
    `game_log` 保留（量小）

### 8.5 金币结算失败

- **场景**：`EmitGameOver` 调 `WalletService.ApplyTransaction` 失败（DB 异常 / 锁等待）
- **现象**：游戏已结束，玩家已离场；金币未结算
- **回滚路径**：
  - **阻塞游戏流？** ❌ **绝不**。结算失败仅 log，不阻塞游戏流程
  - 后续 admin 可通过 `POST /api/admin/llm/bots/:botUserID/wallet/adjust` 手动补单
  - 或后台 cron 任务扫 `t_lsm_game_model_game_log` 中 `coin_delta=0` + `ended_at` 非空的行 → 重试结算

详见 `docs/狼人杀-道具与经济/模型玩家金币设计.md §7 失败与回滚`。

---

> **文档维护说明**：本文件是「模型管理 + 模型玩家持久化」的总规约。
> 任何 DB schema 变更、HTTP API 调整、前端页面改造，都需要同步更新本文件 +
> `docs/LLM与Agent/LLM供应商设计.md` + `docs/狼人杀-道具与经济/模型玩家金币设计.md` 三件套。
