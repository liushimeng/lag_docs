# 辩论比赛 — 工具与记忆系统设计

> 本文档描述辩论比赛的**工具定义**、**Memory 系统**、**记忆压缩（Compact）**、**Context 管理**。
> 复用狼人杀 Agent 的设计模式，适配辩论场景。

## 1. 工具系统概述

### 1.1 工具分类

| 工具类型 | 使用者 | 说明 |
|----------|--------|------|
| **发言工具** | 辩方 Agent | speech / free_debate_speak |
| **质询工具** | 三辩 Agent | cross_exam_question / cross_exam_answer |
| **评审工具** | 裁判 Agent | submit_score / announce / answer_spectator（§20260831-06 观众提问闭环） |
| **通用工具** | 所有 Agent | idle_silent |

### 1.2 工具过滤规则

工具按 **阶段（Phase）+ 辩位（Role）** 过滤，LLM 只能看到当前可用的工具：

| 阶段 | 一辩 | 二辩 | 三辩 | 四辩 | 裁判 |
|------|------|------|------|------|------|
| OpeningArgument | speech | idle | idle | idle | announce |
| Rebuttal | idle | speech | idle | idle | announce |
| CrossExamination | idle | cross_exam_answer | cross_exam_question | idle | announce |
| CrossExamSummary | speech | speech | idle | idle | announce |
| FreeDebate | free_debate_speak | free_debate_speak | free_debate_speak | free_debate_speak | announce |
| ClosingArgument | idle | idle | idle | speech | announce |
| Judging | idle | idle | idle | idle | submit_score, announce |

> **说明**：`idle` = idle_silent 工具；`speech` = 正式发言工具；裁判在评审阶段获得 submit_score。

## 2. 辩方工具详细定义

### 2.1 speech — 正式发言

```json
{
  "name": "speech",
  "description": "正式发言 — 在立论/驳论/质询小结/总结阶段提交发言正文。\n字数限制：立论 ≤ 500 字，驳论 ≤ 400 字，小结 ≤ 300 字，总结 ≤ 600 字。\n必须紧扣辩题和立场。引用对方观点时需注明来源（对方 X 辩）。",
  "input_schema": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "description": "发言正文（纯文本，不使用 Markdown）"
      },
      "references": {
        "type": "array",
        "items": {"type": "string"},
        "description": "引用的对方发言 ID 列表（可选）"
      },
      "internal_thought": {
        "type": "string",
        "description": "内部思考过程 — 观众可在「Agent 思考」面板看到，≤ 200 字"
      }
    },
    "required": ["content"]
  }
}
```

**派发逻辑**：
```
DispatchTool("speech", input, agent, engine)
  → 校验：字数限制、阶段匹配、辩位匹配
  → 截断：超长自动截断
  → 记录：写入 Speech 表 + agent Memory
  → 广播：debate.speech 帧
  → 返回：tool_result 给 LLM
```

### 2.2 cross_exam_question — 质询提问

```json
{
  "name": "cross_exam_question",
  "description": "质询提问 — 向对方辩手发起质询问题。\n规则：只能提问、不能阐述；问题需精准、有针对性；字数 ≤ 50 字。",
  "input_schema": {
    "type": "object",
    "properties": {
      "target_team": {
        "type": "integer",
        "description": "被质询队伍 ID"
      },
      "target_seat": {
        "type": "integer",
        "description": "被质询辩位（0-3，-1 表任意）"
      },
      "question": {
        "type": "string",
        "description": "质询问题，≤ 50 字"
      }
    },
    "required": ["target_team", "question"]
  }
}
```

**派发逻辑**：
```
DispatchTool("cross_exam_question", input, agent, engine)
  → 校验：三辩专属、阶段匹配、目标合法
  → 记录：写入 CrossExam 表
  → 广播：debate.cross_exam 帧
  → 引擎：通知被质询 Agent 回答
```

### 2.3 cross_exam_answer — 质询回答

```json
{
  "name": "cross_exam_answer",
  "description": "质询回答 — 回答对方的质询问题。\n规则：必须正面回应、不得回避或反问；字数 ≤ 100 字。",
  "input_schema": {
    "type": "object",
    "properties": {
      "question_id": {
        "type": "string",
        "description": "被回答的问题 ID"
      },
      "answer": {
        "type": "string",
        "description": "回答内容，≤ 100 字"
      }
    },
    "required": ["question_id", "answer"]
  }
}
```

### 2.4 free_debate_speak — 自由辩论发言

```json
{
  "name": "free_debate_speak",
  "description": "自由辩论发言 — 在自由辩论环节发言。\n字数 ≤ 80 字。需简短有力、针对性强。发言后自动交还发言权。",
  "input_schema": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "description": "发言内容，≤ 80 字"
      }
    },
    "required": ["content"]
  }
}
```

### 2.5 finish_speak — 结束发言

```json
{
  "name": "finish_speak",
  "description": "结束发言 — 主动交还发言权给对方队伍。",
  "input_schema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string",
        "description": "结束原因（可选）"
      }
    },
    "required": ["reason"]
  }
}
```

### 2.6 idle_silent — 沉默

```json
{
  "name": "idle_silent",
  "description": "本轮不出声 — 选择不发言或放弃发言机会。",
  "input_schema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string",
        "description": "选择沉默的原因，≤ 50 字"
      }
    },
    "required": ["reason"]
  }
}
```

## 3. 裁判工具详细定义

### 3.1 submit_score — 提交评分

```json
{
  "name": "submit_score",
  "description": "提交评分 — 对一场辩论的完整评分。\n5 个维度各 1-10 分，附带评语。必须对所有队伍评分。",
  "input_schema": {
    "type": "object",
    "properties": {
      "rankings": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "team_id": {"type": "integer"},
            "scores": {
              "type": "object",
              "properties": {
                "argument_quality": {"type": "number", "minimum": 1, "maximum": 10},
                "logic_rigor": {"type": "number", "minimum": 1, "maximum": 10},
                "language_expression": {"type": "number", "minimum": 1, "maximum": 10},
                "team_coordination": {"type": "number", "minimum": 1, "maximum": 10},
                "rebuttal_effectiveness": {"type": "number", "minimum": 1, "maximum": 10}
              }
            },
            "total_score": {"type": "number"},
            "comment": {"type": "string", "description": "对该队的评语，≤ 200 字"},
            "best_debater": {"type": "integer", "description": "该队最佳辩手座位号"}
          },
          "required": ["team_id", "scores", "total_score", "comment"]
        }
      },
      "overall_comment": {"type": "string", "description": "整体评语，≤ 300 字"},
      "winner_team_id": {"type": "integer", "description": "获胜队伍 ID"}
    },
    "required": ["rankings", "overall_comment", "winner_team_id"]
  }
}
```

### 3.2 announce — 裁判宣告

```json
{
  "name": "announce",
  "description": "公开宣告 — 裁判对全体观众的口语播报。适用：评审开始/评分公布/点评。≤ 100 字。",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {"type": "string", "description": "宣告文本，≤ 100 字"}
    },
    "required": ["text"]
  }
}
```

## 4. Memory 系统设计

### 4.1 Memory 结构

```go
// Memory 是单个 Agent 的多轮对话记忆
type Memory struct {
    mu       sync.RWMutex
    messages []llm.Message    // 完整多轮历史
    tools    []ToolRecord     // 工具调用记录

    // 字节预算
    maxPromptBytes int
    totalSystemToolsBytes int

    // 压缩状态
    lastCompactSummary string   // 上次压缩摘要
    compactCount int            // 压缩次数
}

// ToolRecord 是一条工具调用记录
type ToolRecord struct {
    Name   string         `json:"name"`
    Input  map[string]any `json:"input"`
    Result string         `json:"result"`
    At     time.Time      `json:"at"`
}
```

### 4.2 初始 Memory（身份注入）

```go
// NewDebaterMemory 创建辩方 Agent 的初始记忆
func NewDebaterMemory(role, stance, topic string, teamID, seat int) *Memory {
    m := &Memory{maxPromptBytes: DefaultMaxPromptBytes}

    // 身份消息（第一句话，固定前缀）
    identity := llm.Message{
        Role: "user",
        Content: []llm.ContentBlock{{
            Type: "text",
            Text: buildDebaterIdentity(role, stance, topic, teamID, seat),
        }},
    }
    m.messages = append(m.messages, identity)
    return m
}

func buildDebaterIdentity(role, stance, topic string, teamID, seat int) string {
    roleName := map[string]string{
        "first": "一辩", "second": "二辩", "third": "三辩", "fourth": "四辩",
    }[role]
    stanceName := map[string]string{
        "pro": "正方", "con": "反方", "neutral": "中立",
    }[stance]

    return fmt.Sprintf(
        "你是辩论比赛的%d号辩手（%s），代表%s（%s）。\n辩题：「%s」\n你的立场：%s\n你的职责：%s",
        seat+1, roleName, stanceName, stance, topic, getRoleDuty(role),
    )
}
```

### 4.3 DebateContext（当前可见上下文）

```go
type DebateContext struct {
    Topic           string        // 辩题文本
    TopicType       string        // 辩题类型
    MyStance        string        // 我方立场
    MyRole          string        // 我方辩位
    Phase           string        // 当前阶段
    CurrentRound    int           // 当前轮次
    TimeRemaining   int           // 当前阶段剩余秒数

    // 发言历史
    MyTeamSpeeches  []SpeechEvent // 我方全部发言
    OppSpeeches     []SpeechEvent // 对方全部发言
    AllSpeeches     []SpeechEvent // 全场发言（按时间序）

    // 论点追踪
    MyArguments     []string      // 我方核心论点列表
    OppArguments    []string      // 对方核心论点列表
    KeyClashPoints  []string      // 关键交锋点

    // 队伍信息
    MyTeamID        int
    TeamCount       int
    StanceLabels    map[int]string // 队伍 ID → 立场标签
}

type SpeechEvent struct {
    ID          string `json:"id"`
    Phase       string `json:"phase"`
    TeamID      int    `json:"team_id"`
    Seat        int    `json:"seat"`
    SpeakerName string `json:"speaker_name"`
    Stance      string `json:"stance"`
    Content     string `json:"content"`
    Timestamp   int64  `json:"timestamp"`
}
```

### 4.4 Memory 操作

```go
// Push 追加消息（线程安全）
func (m *Memory) Push(msg llm.Message)

// Prune 按条数 + 字节预算剪枝
func (m *Memory) Prune(maxRounds int)

// Snapshot 返回消息快照（供 LLM 调用）
func (m *Memory) Snapshot() []llm.Message

// RecordTool 记录工具调用
func (m *Memory) RecordTool(name string, input map[string]any, result string)

// SetCompactSummary 更新压缩摘要
func (m *Memory) SetCompactSummary(summary string)

// LastCompactSummary 获取上次压缩摘要
func (m *Memory) LastCompactSummary() string
```

## 5. 记忆压缩系统（Compact）

### 5.1 触发条件

| 条件 | 阈值 | 说明 |
|------|------|------|
| 消息数 | > 50 条 | 防止上下文过长 |
| 字节数 | > 300KB | 防止超出模型限制 |
| 阶段切换 | 每阶段结束 | 阶段结束时自动压缩 |
| 手动触发 | Agent 判断 | Agent 可主动触发 |

### 5.2 压缩策略：8 段结构化摘要

```
## S1. 辩题与立场
  - 辩题文本 + 类型
  - 我方立场 + 辩位

## S2. 我方核心论点与论据
  - 已提出的论点列表
  - 关键论据摘要

## S3. 对方核心论点与论据
  - 对方已提出的论点列表
  - 关键论据摘要

## S4. 关键交锋点
  - 双方争议焦点
  - 攻防结果

## S5. 我方发言摘要
  - 按阶段归档的核心发言要点

## S6. 对方发言摘要
  - 按阶段归档的核心发言要点

## S7. 当前局势
  - 当前阶段 + 剩余时间
  - 待完成任务

## S8. 上次压缩以来的新增
  - 新增的发言要点
  - 新增的交锋点
```

### 5.3 压缩 Prompt

```go
const COMPACT_SYSTEM_PROMPT = `你是辩论比赛的记忆压缩助手。你的任务是把给定的对话历史压缩为 8 段结构化摘要。

不要继续对话。不要回答对话中的问题。只输出结构化摘要。

**身份锁定声明**:
- 当前辩手: %s（%s/%s）
- 辩题: %s
- 严格只输出你作为该辩位能看见的事实和论点

**摘要必须包含以下 8 段**(段名固定,每段 ≤ 80 字):
## S1. 辩题与立场
## S2. 我方核心论点与论据
## S3. 对方核心论点与论据
## S4. 关键交锋点
## S5. 我方发言摘要
## S6. 对方发言摘要
## S7. 当前局势
## S8. 上次压缩以来的新增

**强制格式要求**:
- 保持简洁,总长度 ≤ 600 字
- 论点引用必须注明来源（对方 X 辩）
- 不得编造未出现在对话中的内容`

const COMPACT_USER_PROMPT = `以下是辩论比赛第 %d 轮的对话历史。请压缩为 8 段结构化摘要。

<topic>%s</topic>
<my_stance>%s</my_state>
<my_role>%s</my_role>

<conversation>
%s
</conversation>

请按照 system prompt 中的格式输出 8 段摘要。`
```

### 5.4 压缩校验

```go
// IsValidCompactSummary 校验压缩摘要的合法性
func IsValidCompactSummary(summary string, myStance string, myRole string) bool {
    // 1. 长度校验
    if len(summary) > 2000 {
        return false
    }

    // 2. 结构校验：必须包含 8 段
    for i := 1; i <= 8; i++ {
        section := fmt.Sprintf("## S%d.", i)
        if !strings.Contains(summary, section) {
            return false
        }
    }

    // 3. 立场校验：不得包含对方内部策略
    forbiddenKeywords := []string{"对方策略", "对方计划", "对方内部"}
    for _, kw := range forbiddenKeywords {
        if strings.Contains(summary, kw) {
            return false
        }
    }

    return true
}
```

### 5.5 增量更新模式

```go
const COMPACT_UPDATE_PROMPT = `以下是辩论比赛的对话历史，以及你上一次压缩得到的 8 段摘要。

<previous_summary>
%s
</previous_summary>

<conversation>
%s
</conversation>

请输出一份**更新后**的 8 段摘要，要求：

1. **PRESERVE 优先级**:
   - S1 辩题与立场（不可丢失）
   - S2 我方核心论点（不可丢失）
   - S3 对方核心论点（不可丢失）
   - S4 关键交锋点
2. **ADD**: 把新事实按 S1-S7 分类追加，新增内容同时写入 S8
3. **DELETE/REWRITE**: 仅在旧要点被新事实明确推翻时
4. **保持简洁**: 总长度 ≤ 600 字`
```

## 6. 裁判 Memory 设计

### 6.1 裁判记忆特点

| 特点 | 说明 |
|------|------|
| 全程可见 | 裁判能看到所有发言（全知视角） |
| 独立记忆 | 每个裁判有独立 Memory，互不可见 |
| 评审导向 | 记忆围绕评审维度组织 |

### 6.2 裁判 Memory 结构

```go
type JudgeMemory struct {
    mu       sync.RWMutex
    messages []llm.Message
    tools    []ToolRecord

    // 评审维度追踪
    teamPerformance map[int]*TeamPerformance // 队伍 ID → 表现追踪
}

type TeamPerformance struct {
    TeamID              int
    ArgumentQuality     []ScorePoint // 论证质量评分点
    LogicRigor          []ScorePoint // 逻辑严谨评分点
    LanguageExpression  []ScorePoint // 语言表达评分点
    TeamCoordination    []ScorePoint // 团队配合评分点
    RebuttalEffectiveness []ScorePoint // 反驳效力评分点
    KeyMoments          []string     // 关键时刻记录
}

type ScorePoint struct {
    Phase   string  // 阶段
    Score   float   // 临时评分 1-10
    Reason  string  // 评分理由
    SpeechID string // 关联发言 ID
}
```

## 7. Context 管理

### 7.1 Context 构建时机

| 时机 | 说明 |
|------|------|
| 阶段切换 | 每次阶段切换时重建 Context |
| 轮到我发言 | 收到发言请求时更新 Context |
| 压缩后 | 压缩完成后更新 Context |

### 7.2 Context 大小控制

| 策略 | 说明 |
|------|------|
| 发言截断 | 单条发言最多取前 100 字 |
| 历史窗口 | 最近 20 条发言完整保留 |
| 论点去重 | 重复论点只保留最新版本 |
| 压缩摘要 | 超限时使用压缩摘要替代全文 |

### 7.3 多队 Context 差异

| 模式 | Context 差异 |
|------|-------------|
| 双队 | 仅包含正方 + 反方发言 |
| 三队 | 包含三方发言，立场标注 |
| 四队 | 包含四方发言，BP 制分组 |
| 五队 | 包含五方发言，多角度标注 |

## 8. 工具派发器（DispatchTool）

```go
// DispatchDebaterTool 把辩方 LLM 返回的 tool_use 转换为引擎操作
func DispatchDebaterTool(toolName string, input map[string]any, agent *Agent, engine *DebateEngine) (string, error) {
    switch toolName {
    case "speech":
        content, _ := input["content"].(string)
        thought, _ := input["internal_thought"].(string)
        refs, _ := input["references"].([]string)
        return engine.SubmitSpeech(agent.TeamID, agent.Seat, content, thought, refs)

    case "cross_exam_question":
        targetTeam, _ := input["target_team"].(float64)
        targetSeat, _ := input["target_seat"].(float64)
        question, _ := input["question"].(string)
        return engine.SubmitCrossExamQuestion(agent.TeamID, agent.Seat, int(targetTeam), int(targetSeat), question)

    case "cross_exam_answer":
        questionID, _ := input["question_id"].(string)
        answer, _ := input["answer"].(string)
        return engine.SubmitCrossExamAnswer(agent.Seat, questionID, answer)

    case "free_debate_speak":
        content, _ := input["content"].(string)
        return engine.SubmitFreeDebateSpeech(agent.TeamID, agent.Seat, content)

    case "finish_speak":
        reason, _ := input["reason"].(string)
        return engine.FinishSpeak(agent.TeamID, reason)

    case "idle_silent":
        reason, _ := input["reason"].(string)
        return engine.IdleSilent(agent.Seat, reason)

    default:
        return "", fmt.Errorf("unknown tool: %s", toolName)
    }
}
```
