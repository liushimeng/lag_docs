# 辩论比赛总体的流程设计

> 本文档已迁移至新的设计体系。请参考：
> - `00-辩论比赛总体架构设计.md` — 总体架构
> - `01-辩论比赛游戏流程设计.md` — 详细游戏流程
> - `02-辩论比赛Agent设计.md` — Agent 设计
> - `03-辩论比赛房间创建与配置设计.md` — 房间配置
> - `04-辩论比赛界面与交互设计.md` — 界面设计
> - `05-辩论比赛工具与记忆系统设计.md` — 工具与记忆
> - `06-辩论比赛公平性与评审系统设计.md` — 公平与评审

---

## 首期实现状态(2026-08-31 §20260831-01 / §20260831-02 二期增强)

> **本节记录实现的完成范围**。新增/变更按本节跟踪。

### ✅ 已完成(§20260831-01 首期)

- **后端核心**:
  - `ServerGo/game/debate/` — 12 个 Go 文件(types/cards/room/room_manage/room_spectator/room_action/room_config/view/fair_assignment/engine/engine_phase/engine_judge/doc),全部 ≤ 1800 行(CLAUDE.md §4 硬约束)。
  - 阶段机:`PhaseFilling → PhasePreparation → PhaseOpeningArgument → PhaseRebuttal → PhaseCrossExamination → PhaseCrossExamSummary → PhaseFreeDebate → PhaseClosingArgument → PhaseJudging → PhaseResult → PhaseGameOver`。
  - 房间管理:`DebateManager.CreateRoom / Get / List / Remove / StartGame / StopGame / WipeAllRooms`。
  - 公平模型分配:`FairModelAssignment`(蛇形 + Fisher-Yates 洗牌),`ValidateAssignment`。
  - 评审聚合:`ComputeFinalScores`(5 维中位数)+ `DetermineWinner`(多数决 + 最佳辩手投票)。
- **REST API**:
  - `POST/GET /api/games/debate/rooms` 创建 / 列出房间
  - `GET /api/games/debate/rooms/:id` 房间详情
  - `POST /api/games/debate/rooms/:id/spectate` 观战
  - `POST /api/games/debate/rooms/:id/leave_spectate` 离开观战
  - `POST /api/games/debate/rooms/:id/start` 房主开始比赛
  - `GET /api/games/debate/rooms/:id/history` 发言历史
  - `GET /api/games/debate/topics` 辩题池(30+ 内置题目)
- **WS 帧派发**:`ServerGo/ws/debate_service.go` — 订阅 / 取消订阅 / 观众提问 / 点赞 + 服务器推送 9 类广播帧。
- **Agent 驱动**:
  - `ServerGo/agent/debateplayer/` — 辩方 Bot(基于 llm.LLMProvider.Chat + tool_use 派发 + 辩位差异化 prompt)
  - `ServerGo/agent/debatejudge/` — 裁判 Bot(独立评审 + 5 维度评分 + submit_score)
  - `ServerGo/agent/debaterun/` — Agent 启动器(独立包避免循环引用)
- **前端**:
  - `ClientWeb/src/types/debate.ts` — 25 个 TS 类型
  - `ClientWeb/src/api/debate.ts` — REST 封装
  - `ClientWeb/src/store/debate.store.ts` — Zustand store
  - `ClientWeb/src/hooks/useDebate.ts` — WS 订阅 hook
  - `ClientWeb/src/pages/DebateLobbyPage.tsx` — 大厅页
  - `ClientWeb/src/pages/DebateGamePage.tsx` — 对局页
  - `ClientWeb/src/components/debate/` — 6 个组件(Lobby/CreateModal/Stage/TeamPanel/JudgePanel/SpeechPanel/ScorePanel)
  - `ClientWeb/src/styles/debate.css` — 暗色主题样式(对齐 §26 对比度规范)
  - 路由:`/debate`(大厅)+ `/debate/:roomId`(对局)
- **测试**:`ServerGo/game/debate/debate_test.go` — 18 个单元测试(枚举映射 / 工具过滤 / 公平分配 / 评审聚合 / 房间 CRUD)。

### ✅ 已完成(§20260831-02 二期 — P0 断线修复 + Agent 增强)

- **P0:WS 层全量接线** — 首期 `DebateService` 从未实例化、`debate.*` 帧无路由、零广播:
  - `ws/client.go` 增加 `debate.` 前缀路由 + `AttachDebate`;`Handler/ServeWS` 传递 `*DebateService`;`main.go` 实例化注入。
    - ⚠️ 首版前缀切片误写 `[:8]`("debate." 实为 7 字符,截出 `"debate.s"` 恒不等)致全部帧落入 ack 兜底 —— 已修为 `[:7]`,node 端到端验证 `debate.subscribe → debate.state` 通过。
  - `DebateManager` 新增 `onSpeech / onCrossExam / onJudgeScore / onResult` 广播钩子(锁内读取,无数据竞争),`SubmitSpeech / SubmitCrossExam* / AddJudgeScore` 成功后外抛。
  - `SetOnPhaseChange` 接 `BroadcastPhase`(首期漏接)。
- **P0:引擎-Bot 同步** — 首期 `driveSpeaker` 即发即忘,引擎瞬间跑完所有阶段,Bot 发言全部被阶段校验拒绝:
  - `engine.beginTurn / NotifyTurnDone / waitTurnDone`:触发 Bot → 阻塞等待本轮完成(发言已提交/fallback/放弃)→ 再推进。
  - 等待预算固定 110s(LLM 超时 90s + 余量),**与阶段 deadline 解耦**(实测真 LLM 单次 20~90s,2 人阶段必然超 60s 名义配置;「发言必须完成」优先)。
  - Bot driver 每轮结束(含隔离跳过)必调 `NotifyTurnDone`。
- **P0:评审结果接线** — 首期 `BuildResult` 从未被调用,result 恒 nil:`runJudgingPhase` 收齐评分后构建结果 + 广播 `debate.game_over`;胜方/队伍名用立场名(正方/反方),最佳辩手用真实辩手名。
- **Bug:三辩质询工具集** — `AllowedToolsForPhaseRole` 的 else-if 写法让三辩拿不到 `cross_exam_answer`,实测「有问无答」;改为三辩同时拥有 question + answer(对齐 §05 §1.2)。
- **Bug:质询回答方上下文** — 回答方 Bot 此前既不知道问题文本也不知道 question_id:`collectContext` 注入 `PendingQuestionID/Text`(我是被质询方时),prompt 引导 `cross_exam_answer` 正面回答;引擎按 `CrossExamActive` 实际 target 驱动回答方(target_seat=-1 回退二辩)。
- **Bug:裁判最佳辩手座位越界** — 有模型按 1-based 提交「四辩」=4 越界:`debatejudge.clampBestDebater` 提交时钳制(1-based 换算 → 回退一辩)+ 引擎结果兜底;e2e 第二轮实测:2 问 2 答 / 驳论 2 条 / 总结 2 条(首轮为 1 问 0 答 / 0 条 / 1 条)。
- **辩方 Bot 多轮 tool_use 循环**(§02 §2.3 完整落地):
  - 每轮 ≤ 5 次 tool_use;assistant/tool_result 全量入 Memory(跨轮持久);
  - 派发失败把错误作为 `tool_result` 回填 LLM 自纠错重试;纯文本回复按 speech 提交;
  - 任一工具成功即终态(防连发两次 speech);`sanitizeDebateMessages` 请求期清洗(§14.1:user/assistant 严格交替 + tool_use/tool_result 配对 + 悬空孤儿剔除)。
- **记忆压缩(8 段结构化摘要)**(§05 §5 完整落地):
  - 触发:消息 > 50 条或 > 300KB(轮次开始检查,失败不阻塞发言);
  - `memory_compact.go`:立场锁定 system prompt + 全量/增量(PRESERVE/ADD/DELETE)双模板;
  - `IsValidCompactSummary` 校验(8 段结构 + ≤2000 字节 + 黑名单「对方策略/对方计划/对方内部」);
  - 压缩后重建 `[user: 摘要头] + 最近 8 条`;`AgentClassDebateMemoryCompact` 已登记 §24 class_names.go。
- **前端:5 维评分雷达图** — `DebateRadarChart.tsx` 纯 SVG(网格环 2/4/6/8/10 + 五色相对齐 §26);嵌入 `DebateScorePanel` 每队卡片;维度标签中文化(论证质量/逻辑严谨/语言表达/团队配合/反驳效力)。
- **前端:多队模式 UI** — `DebateTeamPanel` 按 teams 数组动态渲染(2-5 队)+ 队数标题;`angle_1~angle_5` 五色相专属配色(§26.3 JSX/CSS 同提交);`DebateScorePanel` 按 team_scores 数量渲染雷达图。
- **测试**:`engine_sync_test.go`(driveSpeaker 同步/超时兜底/4 类广播钩子/结果构建)+ `agent/debateplayer/memory_test.go`(消息清洗 4 例/压缩校验 5 例/压缩状态)。

### ✅ 已完成(§20260831-03 三期 — 裁判增强 + 解说 Agent)

- **裁判 Agent 多轮 tool_use 循环**(§02 §3.5 完整落地):
  - 每轮 ≤ 5 次 tool_use;assistant/tool_result 全量入 messages(跨轮持久);
  - 派发失败把错误作为 `tool_result` 回填 LLM 自纠错重试;
  - `submit_score` 成功即终态;sanitizeJudgeMessages 请求期清洗(§14.1 对齐);
  - LLM 调用失败 / 超过轮次上限 → fallback 评分(保持向后兼容)。
- **实时解说 Agent (Commentator)**:
  - `ServerGo/agent/debatecommentator/` — 独立包,避免循环引用;
  - 事件驱动:phase_change / speech / cross_exam / judge_score / game_over;
  - 双风格(pro 严谨 / fun 吐槽),仅推送给观战者(spectator-only);
  - 限流 45s + 房间级 LLM 并发闸门(2s 预算,抢不到槽位让位);
  - 连续失败 5 次自我 quarantine(不影响 bot 主线);
  - `AgentClassDebateCommentator` 已登记 §24 class_names.go;
  - `DebateManager.SetCommentatorModelKey / SetCommentatorBroadcast` 注入配置。
- **测试**:`agent/debatejudge/judge_test.go` 继续覆盖 clampBestDebater(6 例)。

### 🚧 待完善(下一阶段)

- 跨局 MEMORY.md 持久化:未实现(对齐狼人杀 §131 agent_memory 模式)。
- 胜率统计落库:✅ 已在 §20260831-08 补全 —— `t_lsm_game_debate_model_stats` UPSERT 原子累加,
  启动回读恢复快照(本条保留仅作索引)。
- 胜率趋势图:统计 API + 大厅表格已有(§20260831-06),趋势折线图留待后续。
- 房主暂停/继续/跳过阶段(§04 §4.2 标注「可选」):留待后续。
- 多队质询流程差异(§01 §5.2-5.4 三队互质询/四队 POI):留待后续。
- 解散房间:✅ 已在 §20260831-05 补全(本条保留仅作索引)。

### ✅ 已完成(§20260831-09 八期 — 裁判实时打分看板完整接线 + 阶段打分时间线)

> **本节记录本批实现的完成范围**。新增/变更按本节跟踪。

- **后端 — WS 帧完整数据修复**(§20260831-09 设计完整落地):
  - `ServerGo/ws/debate_service.go::BroadcastStageScore`: 广播完整 JudgeScoreboard 结构
    (含 team_scores + stage_history),而非仅 room_id + judge_id;前端 WS 帧直接写入 store,
    无需额外 REST 请求补全。
  - `ServerGo/game/debate/room_manage.go`: 新增 `ScoreboardCopy(roomID, judgeID)` 方法,
    供 WS 广播时查找指定裁判看板;`Get` / `ScoreboardCopy` 走读锁(§92a 范式)。
  - `ServerGo/game/debate/stage_score.go`: 新增 `scoreboardsCopyLocked()` 锁内变体,
    供 `BuildClientState` 已持锁路径调用(避免 `sync.Mutex` 重入,§92a)。
  - `ServerGo/game/debate/view.go`: `ClientState` 增加 `Scoreboards []*JudgeScoreboard` 字段;
    `BuildClientState` 锁内注入完整打分看板数据(首次订阅时一次性下发)。
  - `ServerGo/api/debate_api.go`: 新增 `Scoreboards` handler —
    `GET /api/games/debate/rooms/:id/scoreboards`(前端首次进入房间 / WS 断线重连后拉取完整看板)。
  - `ServerGo/router/router.go`: 注册 `GET /rooms/:id/scoreboards` 路由。

- **前端 — 阶段打分时间线组件**:
  - `ClientWeb/src/components/debate/DebateStageScoreTimeline.tsx`(新): 阶段打分时间线组件,
    展示所有裁判最近 N 条 stage_score 提交的时间线(按 submitted_at_ms 倒序),
    每条记录显示:阶段(phase_cn) + 裁判名 + 提交时间 + 各队总分变化。
    数据来源: `store.scoreboards` 内每个 board.stage_history 的并集 + 排序。
  - `ClientWeb/src/hooks/useDebate.ts`: 接线 `debate.judge_scoreboard` 帧 →
    `store.scoreboards[judge_id]` 直接覆盖旧值(完整看板结构);
    `debate.state` 首次订阅时注入完整打分看板数据(遍历 state.scoreboards 写入 store)。
  - `ClientWeb/src/pages/DebateGamePage.tsx`: 集成 `DebateStageScoreTimeline` 组件
    (右栏 DebateScorePanel 下方)。
  - `ClientWeb/src/store/debate.store.ts`: `setGameState` 注入 `scoreboards` 字段
    (首次订阅时一次性覆盖 store.scoreboards)。
  - `ClientWeb/src/api/debate.ts`: 新增 `scoreboards(roomId)` API 方法。
  - `ClientWeb/src/types/debate.ts`: `DebateClientState` 增加 `scoreboards?` 字段;
    `useDebate` 导入 `DebateJudgeScoreboard` 类型。
  - `ClientWeb/src/styles/debate-stats.css`: 新增 `.debate-stage-score-timeline` 样式
    (§5 时间线组件,对齐 §26 对比度规范)。

- **测试**: `go test ./...` 全部通过(含 `game/debate`);`tsc --noEmit` 通过;
  `npm run build` 通过;`rebuild_restart_app.sh` 服务就绪 https://127.0.0.1:39001。

### 🚧 待完善(后续阶段)

- 跨局 MEMORY.md 持久化:未实现(对齐狼人杀 §131 agent_memory 模式)。
- 胜率趋势图:统计 API + 大厅表格已有(§20260831-06),趋势折线图留待后续。
- 房主暂停/继续/跳过阶段(§04 §4.2 标注「可选」):留待后续。
- 多队质询流程差异(§01 §5.2-5.4 三队互质询/四队 POI):留待后续。

### ✅ 已完成(§20260831-04 四期 — 解说主线接线 + 前端交互增强)

> **本节记录本批实现的完成范围**。新增/变更按本节跟踪。

- **后端 — 解说 Agent 主线接线**(§20260831-03 已有实现但未接入):
  - `ServerGo/main.go`:从 `llmRegistry.ListEnabled()[0]` 取首个 enabled 模型作为解说默认 model_key,
    `SetCommentatorModelKey` 注入;`SetCommentatorBroadcast` 注入回调,
    走 `DebateService.BroadcastCommentary`(spectator-only)。
  - `ServerGo/ws/debate_service.go::BroadcastCommentary` 新增:
    走 `hub.BroadcastRoomSpectators` 推送 `debate.commentary` 帧(text + style + timestamp),
    与狼人杀的 `chat.commentary` 隔离命名空间,便于前端 useDebate hook 精确订阅。
  - §130 自检:Provider 通过 `commentator.SetProvider` 注入,未启用 / Provider 失败时主循环跳过。
- **前端 — 房主操作**(DebateHostControls):
  - 「开始比赛」按钮(仅房主 + Filling 阶段可见);错误以 formError 形式展示 + `reportGlobalError` 上报。
  - 非房主显示「👁 观战者」+ 引导文案;解散(disband)按钮占位,后端无 DELETE 端点暂不开放。
- **前端 — AI 实时解说面板**(DebateCommentaryPanel):
  - 订阅 `debate.commentary` 帧(`useDebate` + `store.pushCommentary`),最多保留最近 20 条环形。
  - 最新一条高亮(金边 + 顶部样式徽章 pro/fun),历史区可折叠展开。
  - 空状态显示「解说员正在旁听,精彩处会主动点评...」。
- **前端 — 发言面板按阶段折叠**(DebateSpeechPanel):
  - 按 `opening_argument` / `rebuttal` / `cross_examination` / `free_debate` 等 8 个阶段分组,
    默认展开当前阶段;每条发言带立场徽章 + 折叠 Agent 思考 + 点赞按钮。
- **前端 — 主舞台点赞 + Agent 思考**(DebateStage):
  - 当前发言旁加 👍 点赞按钮(走 `debate.like` WS 帧),`store.likedSpeeches` 跟踪已点赞发言。
  - 发言下方折叠显示 `internal_thought`(对齐 §04 §3.1),仅 `SpectatorConfig.RevealAgentThought=true` 时展示。
  - filling / judging / result 空挡分别给出对应引导文案。
- **样式**(§26 对比度规范 + §26.3 三件套):
  - `debate.css` 增补房主操作 / 点赞 / 立场徽章 / 解说面板 / 阶段折叠样式
    (暗色主题 + 蓝/红/紫/金五色相立场区分;like-btn--active 文字 + 边框 ≥ 4.5:1)。
- **测试**:`ServerGo/game/debate/*` + `agent/debateplayer` + `agent/debatejudge` 单测全部通过(总计 18+ 单测);
  前端 `tsc --noEmit` + `npm run build` 全部通过。


### ✅ 已完成(§20260831-05 五期 — 观战者功能补全 + 房间解散)

> **本节记录本批实现的完成范围**。新增/变更按本节跟踪。

- **路由 — 观战者专属入口**:`App.tsx` 新增 `/debate/spectate/:roomId` 路由(对齐其他 5 款游戏,
  §19.5 观战者架构),复用 `DebateGamePage`(进入即调 `debateService.spectate` 加入观战集合)。
- **后端 — 房间解散端点**:
  - `DebateManager.Disband(roomID, callerUserID)`:仅房主可操作,停止 Agent + 引擎,删除房间,触发 `onRoomRemove` 钩子。
  - `DebateAPI.Disband`:DELETE `/api/games/debate/rooms/:id` REST 入口。
  - 路由注册:`debateGames.DELETE("/rooms/:id", debateAPI.Disband)`。
- **前端 — 房主解散操作**(DebateHostControls):
  - 新增「解散房间」危险按钮(红色),二次确认后调 `debateService.disband`,成功后导航回大厅。
  - 加载中 / 解散中状态互斥(禁用双按钮),失败以 `formError` + `reportGlobalError` 展示。
- **前端 — 观众提问面板**(DebateSpectatorQuestionPanel):
  - 订阅 `debate.spectator_question` WS 帧,维护最近 20 条提问环形缓冲。
  - 输入框(≤200 字,Enter 发送)+ 发送按钮,走 `debate.spectator_question` 帧。
  - 仅在 `SpectatorConfig.AllowSpectatorQuestion=true` 时渲染,提问者 ID 脱敏显示。
- **前端 — 发言历史面板**(DebateHistoryPanel):
  - 从 `/api/games/debate/rooms/:id/history` 拉取完整发言 + 质询 + 结果。
  - 按阶段分组(立论/驳论/质询/质询小结/自由辩/总结),可折叠/展开。
  - 质询记录单独渲染(问答配对,绿色=回答/橙色=提问)。
- **API 封装**:`api/debate.ts` 新增 `disband(roomId)` 方法(DELETE 请求)。
- **样式**(§26 对比度规范):
  - `debate.css` 增补观众提问面板 / 发言历史面板 / 房主危险按钮样式。
- **测试**:Go `go test ./...` 全部通过;前端 `tsc --noEmit` + `npm run build` 全部通过。
### ✅ 已完成(§20260831-06 六期 — 观众提问闭环 + 裁判宣告 + 模型胜率统计)

> **本节记录本批实现的完成范围**。新增/变更按本节跟踪。

- **后端 — 观众提问闭环**(§01 §6.1「可向裁判 Agent 提问,裁判可选择性回应」全链路落地):
  - `game/debate/room_question.go`(新文件):`DebateRoom.AddSpectatorQuestion`(≤20 条环形队列,
    超长截断 200 字,对局结束后拒收)+ `UnansweredSpectatorQuestions` + `AnswerSpectatorQuestion`
    (幂等,已回答不重复广播)。
  - `ws/debate_service.go::handleSpectatorQuestion`:首期只广播未入队(裁判永远看不到),
    现在提问先写入房间队列再广播(帧带 `question_id` 供前端与回答配对)。
  - `agent/debatejudge/judge.go`:评审 prompt 注入【观众提问】段(最近 10 条未回答);
    新增 `answer_spectator` 工具(question_id + answer ≤100 字),裁判选择性回答;
    回答写回队列并经 `debate.spectator_answer` 帧广播给全体观众。
- **后端 — 裁判宣告接线**:首期 `announce` 工具派发是空操作(文本被吞);
  现在经 `DebateManager.EmitJudgeAnnounce` → `debate.judge_announce` 帧广播给房间全体,
  前端裁判面板展示最近 3 条宣告。
- **后端 — 模型胜率统计**(§06 §9 历史统计全链路落地):
  - `game/debate/stats.go`(新文件):进程内 `statsStore`,按辩手模型累加
    TotalGames/WinCount/BestDebaterCount/场均总分(异常局不计胜负,避免 fallback 污染)。
  - `engine_phase.go::runJudgingPhase`:SetResult 后调 `DebateManager.RecordGameResult`。
  - `api/debate_api.go::Stats` + 路由 `GET /api/games/debate/stats`(按胜率降序)。
  - 前端大厅「📊 模型胜率统计」可折叠表格(模型/场次/胜场/胜率/最佳辩手/场均总分)。
- **Bug 修复 — `TruncateRune` 字节索引误用**:首期 `for i, r := range s` 的 `i` 是字节索引,
  与 rune 数上限比较导致中文只截出约 1/3 长度(立论 500 字上限实测只保留 ~167 字);
  改为按 rune 计数截断,原单测锁定了 bug 行为,已同步修正期望。
- **测试**:`room_question_test.go` 新增 6 组单测(提问入队/环形上限/回答幂等/对局结束拒收/
  统计累加与排序/异常局跳过);`go test ./...` 全部通过;前端 `tsc --noEmit` + `npm run build` 通过。

### ✅ 已完成(§20260831-08 七期 — 数据库持久化 + 历史对局复盘 + 辩题池补全)

> **本节记录本批实现的完成范围**。新增/变更按本节跟踪。

- **后端 — GORM 持久化(§03 §8 全链路落地)**:
  - `models/t_lsm_game_debate_room.go`(新文件):5 张表 —— `t_lsm_game_debate_room`(房间记录,
    含 result JSON / is_abnormal)、`t_lsm_game_debate_speech`(发言,room_id+created_at 复合索引,
    created_at 存 unix 毫秒)、`t_lsm_game_debate_score`(评审,一裁判×一队一行,确定性 ID
    `<room>:j<judge>:t<team>` 重复 upsert 幂等)、`t_lsm_game_debate_model_stats`(模型胜率,
    model_key 主键)、`t_lsm_game_debate_topic`(自定义辩题)。`db/db.go` AutoMigrate 注册。
  - `game/debate/persistence.go`(新文件):`AttachPersistence(m, gormDB)` 链式包装
    onSpeech/onJudgeScore/onResult/onGameOver 钩子 —— 先同步调 main.go 已注入的 WS 广播,
    再 `go runSafe`(recover)异步落库,不阻塞主流程;nil DB 静默 no-op 降级(单测可跑);
    启动时回读 model_stats 恢复胜率统计快照。
  - `stats.go`:抽出纯函数 `computeStatsDeltas` 作为进程内累加与 DB UPSERT 的单一事实来源;
    model_stats 逐模型 `ON DUPLICATE KEY UPDATE col = col + ?` 原子累加。
  - 房主解散(Disband)不写历史行;被 StopGame 强杀(无评审结果)的局补写 `is_abnormal=true`。
- **后端 — 历史对局 API**(§00 §4.1 增补):
  - `GET /api/games/debate/history`:分页(finished_at desc,page/page_size 默认 1/20 上限 100),
    返回 `data:{rooms,total,page,page_size}`(对齐项目分页惯例;item 字段含 room_id /
    topic_type / best_debater_team_id 等,room_id 遵循 view.go 惯例)。
  - `GET /api/games/debate/history/:id`:复盘详情 = 房间记录(JSON 列还原为嵌套对象)+
    发言列表(created_at asc)+ 评分列表;404 = ErrRoomNotFound。
- **后端 — 辩题池补全**(§03 §2.4):`GET /topics/:id`(内置池→DB 自定义池双查找)+
  `POST /topics`(仅管理员,text ≤ 200 字校验,写 topic 表,ID=`custom_`+10 位随机);
  `Topics` 列表改「内置 + DB 合并」;`resolveTopic` 支持用自定义题 ID 开房。
  新错误码 `ErrTopicNotFound = 30017`。
- **前端 — 历史战绩面板 + 复盘弹窗**:
  - `components/debate/DebateHistoryListPanel.tsx`(新):大厅「📜 历史战绩」可折叠面板
    (与模型胜率统计同款交互),分页列表(辩题/模式/胜方徽章/最佳辩手/is_abnormal 异常徽章),
    整行 + 按钮双入口打开复盘。
  - `components/debate/DebateReplayModal.tsx`(新):复盘详情弹窗 —— 胜方横幅 + 最佳辩手、
    队伍辩位卡(复用 `.team-card--{stance}`)、每队 N 裁判五维均分雷达(复用 DebateRadarChart)+
    分数条(复用 `.score-bar`)、裁判点评折叠区、按阶段分组发言(复用 PHASE_CN)。
  - `historyUtils.ts`(新)+ `styles/debate-history.css`(新,globals.css 末尾追加 import 不动既有链序);
    i18n zh-CN/en/ja 同步 40 键。
- **联调 — 契约对齐修复**(integration-tester 双向核验):
  - 后端历史列表 `id` → `room_id`(项目惯例,view.go:48,223)、补 `topic_type` /
    `best_debater_team_id`、`data` 封套内嵌分页字段(原裸数组形状前端列表恒空,P0);
    新增 `TestHistoryRoomItemJSONContract` 锁定 wire 契约。
  - 前端类型对齐真实持久化形状:`team_config` 为 `[]TeamConfig` 顶层数组(agents 内
    `seat_id`/`role_name`),speech/score 的 `id` 为 varchar → string。
- **测试**:`persistence_test.go` 10 组单测(增量计算/UPSERT 累加语义/钩子链同步广播+异步落库/
  panic recover/nil-DB no-op);`debate_history_api_test.go` 覆盖降级封装/权限门/双池查找/
  纯函数;`go test ./...` 26 ok / 0 FAIL;前端 `tsc --noEmit` + `npm run build` 通过。

### 关键设计决策

1. **纯 Agent 驱动**:狼人杀是"人类 + Agent 混合",辩论比赛是"100% Agent 参赛,人类仅房主 / 观战者"。
2. **公平分配**:蛇形洗牌确保正反方模型能力均衡,裁判使用与辩方不同的模型。
3. **完整 Bot 循环**(§20260831-02 升级):每轮 ≤5 次 tool_use,失败自纠错重试,终态工具即停,LLM 失败回退 fallback 模板发言。
4. **避免循环引用**:`agent/debaterun` 独立包解决 `debate → agent/debateplayer → debate` 的循环依赖。
5. **发言必须完成 > 名义时长**(§20260831-02):发言阶段等待预算与阶段 deadline 解耦;deadline 仅约束准备期倒计时/自由辩循环/评审收集。
6. **game/debate 不 import ws**:所有实时推送经 `DebateManager` 钩子外抛,由 main.go 接到 `ws.DebateService`(与狼人杀 Manager→GameService 同构)。

---

## 快速索引

本文件保留原始参考资料，供设计时参考。

### 设计01：标准四对四团体辩论赛流程

第一，开篇立论：正反方一辩依次发言（各 3 分钟），明确辩题定义、划定范围、抛出核心论点。

第二，驳论与质询：二辩驳论（各 2 分钟）+ 交叉质询（三辩交替提问）。

第三，自由辩论：双方累计时长均等（各 4 分钟），交替起立辩论。

第四，总结陈词：四辩收尾（各 3-4 分钟），梳理攻防、升华观点。

### 设计02：华语赛制骨干

1. 开篇立论（一辩）→ 2. 驳论/质询（二/三辩）→ 3. 质询小结 → 4. 自由辩论 → 5. 总结陈词（四辩）

评审：评委评议 → 点评 → 宣布结果。

### 设计03：两大源流

- 奥瑞冈制：申论—质询交替，注重逻辑严谨
- 新加坡制：四人制，重视观赏性和语言美感
- 当代融合：立论→攻防→自由辩论→总结

### AI 辩论适配说明

本项目的 AI 辩论比赛基于上述赛制，由 Agent 全自动执行：
- 辩方 Agent 扮演一至四辩，按阶段自动发言
- 裁判 Agent（3 个）独立评审打分
- 人类玩家仅观战 + 聊天互动
- 详细设计见上述 7 篇专题文档
