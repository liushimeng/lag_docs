# 狼人杀 13 人局 Agent 升级 — 20260810-08（信息账本二期：消费侧接入 + 说漏嘴检测 + 信息传播时序图）

> **文档版本**：2026-08-10
> **采纳来源**：[`Agent-Surpport-01.md`](../../Agent-Surpport-01.md) §12 优先级总表 **P4-E「信息账本二期（消费侧切换 + 时序图 + 说漏嘴检测）」**
>（原始出处：LongCat F1 二期 + DeepSeek §二.2「信息污染链」的语义层地基）
> **前置批次**：[`§20260810-05`](信息账本一期-17个InfoSource后端落地.md) 信息账本一期（后端「只写不读」，17 个 `InfoSource` 全部接线）
>
> **采纳理由（为什么它是知识库剩余项中最重要、最复杂的一条）**：
>
> 1. **它是唯一一条「地基已建成、但地基上什么都没有」的建议**。§20260810-05 一期落地了 311 行 `information_ledger.go` + 17 处写入接入点 + 观战者快照下发通道 `cs.InfoLedger`，但 **读取侧完全为零**：`buildAgentContextLocked` 不读账本、`BuildUserPrompt` 不渲染账本、前端 `InfoEntryJSON` 类型已声明却无任何组件消费。这正是 CLAUDE.md §130「声明了却从不接线」的**第 N+1 次**——只不过这次是**主动分期**造成的，一期文档 §5 已白纸黑字写明二期三项待做。**若不做二期，一期就是一坨纯开销的死代码**（每局 ~250 条 append + 200 条 JSON 序列化下发，零收益）。
> 2. **它是知识库中唯一能「结构性消除一整类缺陷」的建议**，而非新增玩法。LongCat §1 审计出的 D1~D6 六项「信息流失控」缺陷（bot 看不到票型 / prompt 描述错卡池 / 跨局记忆虚构对手 / …）全部同源：**「谁应该知道什么」散落在十几个字段和分支里，没有单一真相源**。一期把真相源建起来了，二期让 prompt 组装**改从真相源取数**，从此 D1 类缺陷在结构上无法诞生——这比逐个打补丁高一个数量级。
> 3. **它是「说漏嘴检测」的唯一可行前提**，而说漏嘴检测是本项目 Agent 公平性审计的终极武器：把「发言文本」与「该座位知情集合」交叉，可以**机器判定**「5 号白天说的这条信息，账本里只在狼队频道出现过」——这是目前所有 §119/§133/§135 协议层隔离约束**第一次有了运行时可验证的硬证据**，而不是只靠 code review 与人肉抽查。
> 4. **复杂度确实最高**：跨 3 层（账本引擎 / Agent prompt 组装 / 前端观战可视化）+ 触碰 CLAUDE.md 中最高危的两条路径（`buildAgentContextLocked` prompt 组装 = 直接影响全部 bot 行为；`BuildClientStateWithRoom` = R212 自死锁的案发现场），且必须同时满足 §92a 锁内变体 / §119 协议层隔离 / §128 对话即思考 / §135 单点身份判定 四条硬约束。
>
> **工作面边界**（按 CLAUDE.md §13 拆分）：
> - `ServerGo/game/werewolf/information_ledger*.go` + `room_agent.go` + `view.go` → **backend-dev**
> - `ServerGo/agent/wwtypes/context.go` + `agent/wwplayer/prompt.go` → **backend-dev**（同属 `ServerGo/`）
> - `ClientWeb/src/types/werewolf.ts` + `components/werewolf/HistoryDrawer.tsx` + `styles/` + 三语 i18n → **frontend-dev**
> - 测试 + 文档 → 主 Agent
>
> **不触碰**：`chat_message` 表、`chat_history` 500K 队列、`BotTranscript.HeartThought`、`wolfPack` 留言存储、`InfoSource` 枚举取值、17 处既有写入点的 knower 集合语义、`SnapshotJSON` 现有观战者下发（**只增不改**）。

---

## 1. 现状审计（一期落地后的精确形状）

### 1.1 写入侧：已完成（17/17）

| 来源常量 | 写入点 | Knowers |
|---|---|---|
| `role_deal` | `information_ledger.go:298` `ledgerRegisterRoleDealLocked`（4 条 StartGame 路径统一调用） | `{i}` 仅本人 |
| `public_speech` | `room_chat.go:220` `appendRoomMessage` 公开分支 | `aliveKnowerSetLocked` |
| `whisper` | `room_chat.go:152` `appendRoomMessage` whisper 分支 | `pairKnowerSet(from,to)` |
| `wolf_pack` | `agent_runner.go:1299` `WolfWhisper` | `aliveWolfKnowerSetLocked` |
| `night_seer` | `room_action.go:68` `Action_SeerCheck` | `{actor}` |
| `night_witch` | `room_action.go:135` `Action_Witch` | `{actor}` |
| `night_guard` | `room_action.go:159` `Action_GuardProtect` | `{actor}` |
| `night_wolf_vote` | `room_action.go:1016` `Action_WolfKill` | 全活狼 |
| `day_vote_map` | `room_action.go:259` `Action_DayVote` | 全房存活 |
| `sheriff_elect` / `sheriff_stream` | `room_action.go:617` / `:691` | 全房存活 |
| `hunter_shot` / `knight_duel` / `idiot_reveal` / `demon_hunter` | `room_action.go:556` / `:194` / `:110` / `:231` | 全房存活 |
| `prop_inject` | `room_prop.go:543` `enqueuePropHitLocked` | `{target}` |
| `death_event` | `room_chat.go:97` `RecordRoomActivity` | 全房存活 |

### 1.2 读取侧：为零（本期要补的三个洞）

| 洞 | 现状 | 二期目标 |
|---|---|---|
| **洞 1 — Agent 不读账本** | `buildAgentContextLocked`（`room_agent.go:701-1265`）**零引用** `r.infoLedger`；`BuildUserPrompt` 全文 grep 不到 `Ledger` | 新增 `gc.KnowledgeDigest`，prompt 末尾渲染「🗂 你的知情清单」 |
| **洞 2 — 前端不渲染** | `types/werewolf.ts:220` 有 `info_ledger?: InfoEntryJSON[]`，`ClientWeb/src` 无任何组件消费 | HistoryDrawer 新增第 6 sub-tab「🗂 信息流」，观战者专属 |
| **洞 3 — 无说漏嘴检测** | 账本 `Knows(seat, source)` 只看「最近一条匹配 source」，无法做文本交叉 | 新增 `information_leak.go` 泄漏检测器 + 观战者 `cs.InfoLeaks[]` |

### 1.3 一期遗留的两个能力缺口（本期顺手补）

- **`Knows(seat, source)` 语义过窄**：只判定「最近一条匹配 source 的条目」，无法回答「seat 是否知道包含关键词 K 的任何一条信息」——说漏嘴检测必须逐条扫描。→ 新增 `EntriesForSeat(seat, limit)` / `EntriesNotKnownBy(seat)`。
- **`redactLedgerFact` 把角色词一律替换为 `▪`**：这对「不成为第 5 条身份下发通道」是正确的（§135），但**副作用是账本文本失去了做关键词交叉的能力**（"我验了 3 号是狼人" → "我验了 3 号是▪"）。→ 二期在 `InfoEntry` 增加**不下发、不进 prompt** 的私有字段 `rawDigest`（仅座位号 + 数字指纹，见 §2.3），泄漏检测走指纹而非明文。

---

## 2. 设计

### 2.1 总体：一个真相源，三条消费链路

```
                    ┌──────────────────────────────────┐
   17 处写入点 ───▶ │  InformationLedger (r.infoLedger) │  ← §20260810-05 一期（不改）
                    └───────────────┬──────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
   ① 行为侧（本期新增）      ② 观战侧（本期新增）      ③ 审计侧（本期新增）
   KnowledgeDigest          InfoEntryJSON 时序图      InfoLeak 说漏嘴检测
   → gc.KnowledgeDigest     → cs.InfoLedger（已有）   → cs.InfoLeaks[]
   → prompt「🗂 知情清单」   → HistoryDrawer 🗂 tab    → HistoryDrawer 🗂 tab 告警区
   （全体 bot 可见自己的）    （spectator only）        （spectator only）
```

**三条链路共享同一 `r.infoLedger`，互不写入**——读取侧全程只读，杜绝一期建立的写入不变量被破坏。

### 2.2 链路 ①：行为侧 —— `KnowledgeDigest` 注入 Agent prompt

#### 2.2.1 为什么不是「把账本原文塞进 prompt」

一期账本单局 ~250 条、每条 ≤120 rune，全量塞入 prompt ≈ 30K rune ≈ 15K token/轮/bot × 7 bot = **每轮 105K token**，直接击穿 §197 的长预算与成本模型。

**因此二期设计的核心是「摘要而非原文」**：`KnowledgeDigest` 是**按来源聚合的计数 + 最近 N 条要点**，而不是逐条流水账。

#### 2.2.2 数据结构（`ServerGo/agent/wwtypes/context.go` 新增）

```go
// KnowledgeDigestEntry 是「本座位从某来源获知的信息」的聚合摘要。
// 2026-08-10 §20260810-08 信息账本二期 —— 行为侧消费链路。
type KnowledgeDigestEntry struct {
    Source     string   `json:"source"`      // InfoSource 字符串值
    Count      int      `json:"count"`       // 本座位从该来源获知的条目总数
    LastRound  int      `json:"last_round"`  // 最近一条的 round
    Highlights []string `json:"highlights"`  // 最近 ≤2 条要点（已 redact，≤60 rune）
}

// KnowledgeDigest 是注入 GameContext 的「我的知情清单」。
// §119：仅本人 bot 可见；不进 chat_message / chat_history。
// §135：Fact 已在写入侧 redactLedgerFact，不含身份明文。
type KnowledgeDigest struct {
    Seat        int                    `json:"seat"`
    TotalKnown  int                    `json:"total_known"`   // 本座位知情条目总数
    TotalInRoom int                    `json:"total_in_room"` // 全房条目总数（信息劣势感知）
    Entries     []KnowledgeDigestEntry `json:"entries"`       // 按 Count 降序，≤6 条
}
```

`GameContext` 新增字段：

```go
// KnowledgeDigest 是本 bot 的「知情清单」摘要（§20260810-08 信息账本二期）。
// 由 buildAgentContextLocked 从 r.infoLedger 聚合；nil 表示账本为空。
KnowledgeDigest *KnowledgeDigest `json:"knowledge_digest,omitempty"`
```

#### 2.2.3 账本侧新增聚合方法（`information_ledger.go`，全部 `*Locked` 语义）

```go
// DigestForSeat 聚合「某座位知情的全部条目」为按来源分组的摘要。
// caller 必须持 r.mu（§92a）。maxHighlights 控制每组要点条数。
func (l *InformationLedger) DigestForSeat(seat int, maxHighlights int) *SeatDigest
```

- 逐条扫描 `l.entries`（≤400 条，O(n) 且 n 有硬上限，单次 <10μs，锁内可接受）；
- 命中 `e.KnowerSeats[seat]` 的条目按 `e.Source` 分组计数；
- 每组保留最近 `maxHighlights`（默认 2）条 `Fact`，按 60 rune 二次截断；
- 按 `Count` 降序输出，最多 6 组（避免 prompt 膨胀）。

**预算核算**：6 组 × (1 行标题 + 2 条 ≤60 rune 要点) ≈ 400 rune ≈ 200 token/轮/bot，7 bot 合计 ~1.4K token/轮。与 §120 的既有 prompt 块同量级，可接受。

#### 2.2.4 prompt 渲染（`agent/wwplayer/prompt.go` 新增 `KnowledgeDigestBlock`）

拼接位置：`BuildUserPrompt` 末尾，**在 `HypothesisTableBlock` 之前**——语义上「我知道什么」应先于「我猜谁是谁」。

```
【🗂 你的知情清单（第 3 天）】
你共掌握 24 条信息;本局全房累计产生 87 条 —— 你处于信息劣势,发言时注意别暴露你知道得比表现出来的多。
- 公开发言 ×12（最近:「3号说他昨晚验了…」/「7号反驳…」）
- 私聊 ×5（最近:「你信我吗」）
- 狼队密语 ×4（最近:「今晚刀 5」）
- 预言家查验 ×2（最近:「查验 seat=3」）
⚠️ 注意:狼队密语 / 私聊 / 夜间技能属于**私密来源**,白天公开发言引用这些内容等于自曝身份。
```

**最后一行是本块的战术价值核心**——它把「说漏嘴」这个抽象风险，变成每轮 prompt 中的显式约束。

#### 2.2.5 §119 隔离验证

- `KnowledgeDigest` 只经 `GameContext` → user prompt，**不**写 `BotTranscript`、**不**入 `chat_message` / `chat_history`；
- 内容源自 `redactLedgerFact` 之后的 `Fact`，天然不含身份明文（§135）；
- 每个 bot 只能拿到 `DigestForSeat(自己的 seat)`，`buildAgentContextLocked` 的 `seat` 参数是唯一入口。

### 2.3 链路 ③：审计侧 —— 说漏嘴检测（`information_leak.go`）

#### 2.3.1 检测原理

一条公开发言若**引用了只在私密来源出现过的具体信息**，即为「说漏嘴」。判定要素：

1. **私密来源集合**：`whisper` / `wolf_pack` / `night_seer` / `night_witch` / `night_guard` / `night_wolf_vote` / `prop_inject` / `role_deal`（其余为公开来源）。
2. **指纹 —— 两套编号并存，必须按模式分支归一化到引擎内部 0-indexed**：账本 fact 在同一账本内**两套编号并存**，漏解析或错解析都会让检测完全失效 —— 这是本期最容易被后人踩的坑，必须落文档：
   - **0-indexed（结构化 Action_* 写入点）**：`seer_check seat=%d target=%d`（`room_action.go:69`）、`wolf_vote seat=%d target=%d reason=%s`（`:1017`）、`wolf_pack from=%d text=%s`（`agent_runner.go:1300`）、`role_deal seat=%d`（`:307`）—— 机器生成，`%d` 直接取自 `int(seat)`。
   - **1-indexed（人类可读）**：`InfoSourcePublicSpeech` / `InfoSourceWhisper` / `InfoSourceDeathEvent` 的 fact 来自人类/bot 发言原文（`room_chat.go:97`/`:152`/`:220`），且 `wolf_pack` 内嵌的 `text` 字段也是人手写（"今晚刀5号"）；`activity_emitter.go:778` 死亡事件拼成 `"💀 " + strconv.Itoa(seat+1) + "号 死亡(...)"`；prompt 渲染统一按 1-indexed 输出（`agent/wwplayer/prompt.go:236` `itoa(seat+1) + "号→"` / `:253` / `:320` 等）。
   - **归一化规则**：`extractSeatRefs` 维护两份正则列表：
     - `seatRefZeroIndexed` 匹配 `seat=N` / `target=N` / `from=N` / `to=N`（`\b` 词边界避免误命中），原值保留作为 0-indexed ref；
     - `seatRefOneIndexed` 匹配 `N号` / `第N位`，捕获的 N 是 1-indexed，**归一化时 -1**；
     - 两者结果合并到同一 `map[int]bool`，范围校验 `0 <= seat < MaxPlayers`。这套规则下的「私密 seat=4 / 公开 5号」会被正确交叉命中（4 == 5-1）。
   - **R213 教训**：早期版本对三种形式统一按 0-indexed 解析，导致真实泄漏系统性漏报（私密 `seat=4` vs 公开「5号」= 内部 4 永远对不上）且产生系统性误报（「5号」被解析成 ref=5 与另一条 `seat=5` 私密条误交叉）。
3. **判定**：某座位 S 的公开发言 P，其指纹集合 F(P) 中若存在座位号 n，满足
   - 存在一条私密条目 E，`E.KnowerSeats[S] == true` 且 `n ∈ F(E.Fact)`，且
   - 在 P 之前**不存在**任何公开条目 E' 使 `n ∈ F(E'.Fact)`，
   - **且 `n != S`**（R213 修复：跳过自指；`role_deal` 给每个座位种下「私知自己座位号」，不跳过会产生每局 ~13 条稳定噪声，淹没真实信号），
   则记一条 `InfoLeak{Seat:S, Seq:P.Seq, HintSeat:n, FromSource:E.Source}`。

即：**「这个座位号，你只可能从私密渠道知道，但你公开说出来了」**。

> **⚠️ 定位声明（防止误用）**：本检测是**观战者侧的复盘线索**，不是作弊裁决器。座位号在狼人杀里是最高频词，误报不可避免（例如「我觉得 5 号今天很沉默」纯属公开观察）。因此：
> - **绝不**据此惩罚、quarantine、扣分或干预对局；
> - **绝不**下发给对局内玩家（含 bot），避免污染博弈；
> - UI 文案必须写明「疑似 / 仅供复盘参考」。

#### 2.3.2 数据结构

```go
// InfoLeak 一条「疑似说漏嘴」记录（§20260810-08 审计侧）。
// 仅 spectator 下发；绝不进入 GameContext / prompt / chat 通道。
type InfoLeak struct {
    Seq        int64      `json:"seq"`         // 触发检测的公开发言条目 Seq
    Seat       int        `json:"seat"`        // 疑似说漏嘴的座位
    Round      int        `json:"round"`
    Phase      string     `json:"phase"`
    HintSeat   int        `json:"hint_seat"`   // 被提及、且仅私密可知的座位号
    FromSource InfoSource `json:"from_source"` // 该私密信息的来源
    Excerpt    string     `json:"excerpt"`     // 公开发言摘录（≤60 rune）
}
```

#### 2.3.3 计算时机：**懒计算，不进热路径**

**不**在 `appendRoomMessage` 里做（每条发言 O(n²) 扫描会拖慢聊天热路径）。改为在 `BuildClientStateWithRoom` 的 `viewer < 0` 分支**按需计算**，并加**结果缓存**：

```go
// leakCacheSeq 记录上次计算时账本的 seq；账本未增长则直接复用缓存。
leakCache    []InfoLeak
leakCacheSeq int64
```

观战者刷新频率 ≈ 每秒 1 次，账本 ≤400 条，最坏 O(400 × 400) = 160K 次 map 查询 ≈ 1ms，且有 seq 缓存兜底（无新条目时 0 开销）。**锁内可接受**。

> **§92a 硬约束**：`detectLeaksLocked` 必须是锁内变体，由已持 `r.mu` 的 `BuildClientStateWithRoom` 调用——这正是 R212 自死锁的案发函数，**绝不允许**在其中新增任何 `Lock()`。

### 2.4 链路 ②：观战侧 —— 信息传播时序图

前端 `HistoryDrawer` 新增第 6 sub-tab **「🗂 信息流」**（仅 spectator，与 🤖 独白 / 🔮 假说同隔离级别）。

```
┌─ 🗂 信息流 ────────────────────────────┐
│ ⚠️ 疑似说漏嘴 2 条（仅供复盘参考）      │
│  • 第2天 5号 提及「3号」— 该信息仅在    │
│    狼队密语中出现过                     │
│                                        │
│ 座位 │ D1              D2         D3   │
│  0   │ ●公 ●私                    ●公  │
│  1   │      ●狼 ●狼         ●公        │
│ …                                      │
│ 图例:●公开 ●私聊 ●狼队 ●夜间技能 ●道具  │
└────────────────────────────────────────┘
```

- **横轴 = `Round`（天）分组，纵轴 = 座位 0..12**，每条 `InfoEntryJSON` 在其全部 `knower_seats` 行上打一个色点；
- 点色按 `source` 归类（公开 / 私聊 / 狼队 / 夜间技能 / 道具 / 系统）；
- hover 显示 `fact` 全文 + 知情座位列表；
- **纯 CSS Grid 实现**，不引入 D3 / ReactFlow（§8 不新增打包依赖）；
- 顶部告警区渲染 `info_leaks`，无数据时整块不渲染。

**§26 对比度约束**：6 种来源色点必须满足暗色主题 ≥4.5:1；点直径 ≥8px 且带 1px 描边（避免 `.is-night brightness(0.4)` 滤镜下不可辨）。

---

## 3. 改动清单

### 3.1 后端（`ServerGo/`）

| # | 文件 | 改动 | 约束 |
|---|---|---|---|
| B1 | `game/werewolf/information_ledger.go` | 新增 `SeatDigest` / `SeatDigestEntry` 结构 + `DigestForSeat(seat, maxHighlights)` + `entriesSnapshot()` 只读访问器 | §92a 全 `*Locked` 语义 |
| B2 | `game/werewolf/information_leak.go`（新建，≤260 行） | `InfoLeak` 结构 + `extractSeatRefs(fact)` 指纹 + `isPrivateSource(src)` + `DetectLeaks(ledger)` | 纯函数，不持房间引用 |
| B3 | `game/werewolf/room.go` | `WerewolfRoom` 新增 `leakCache []InfoLeak` + `leakCacheSeq int64` | 紧邻 `infoLedger` 声明 |
| B4 | `game/werewolf/room_restart_vote.go` | `restartGameLocked` 清零账本处同步清零 leak 缓存 | 与一期 `infoLedger` 重置同点 |
| B5 | `game/werewolf/room_agent.go` | `buildAgentContextLocked` 末尾（`HypothesisTable` 注入**之前**）填充 `gc.KnowledgeDigest` | 已持 `r.mu`，锁内直读 |
| B6 | `game/werewolf/view.go` | `ClientGameState` 新增 `InfoLeaks []InfoLeak`；`viewer < 0` 分支调 `r.detectLeaksLocked()` | **绝不新增 Lock**（R212） |
| B7 | `agent/wwtypes/context.go` | 新增 `KnowledgeDigest` / `KnowledgeDigestEntry` + `GameContext.KnowledgeDigest` 字段 | 镜像结构，避免 werewolf→agent 循环导入（§133 教训 5） |
| B8 | `agent/wwplayer/prompt.go` | 新增 `KnowledgeDigestBlock(ctx)`；在 `HypothesisTableBlock` **之前**拼接 | §128 只增块不改既有语义 |

### 3.2 前端（`ClientWeb/`）

| # | 文件 | 改动 |
|---|---|---|
| F1 | `src/types/werewolf.ts` | 新增 `InfoLeakJSON`；`WerewolfGameState` 加 `info_leaks?: InfoLeakJSON[]` |
| F2 | `src/components/werewolf/InfoFlowPanel.tsx`（新建，≤240 行） | 时序图网格 + 泄漏告警区 + 图例 |
| F3 | `src/components/werewolf/HistoryDrawer.tsx` | `SubTab` 加 `'infoflow'`；tabs 数组 + body 分支；**spectator 才渲染该 tab** |
| F4 | `src/styles/werewolf-panels.css` | `.ww-infoflow*` 样式（当前 1003 行，新增 ~110 行仍 <1800，§4 OK） |
| F5 | `src/i18n/types.ts` + `locales/{zh-CN,en,ja}` | 新增 `werewolf.infoflow.*` 共 13 键（§23 教训：新 UI 字段必须同时进 types.ts + 三语） |

### 3.3 测试

| ID | 断言 |
|---|---|
| L2-01 | `DigestForSeat` 只聚合 `KnowerSeats[seat]==true` 的条目 |
| L2-02 | `DigestForSeat` 按 Count 降序且最多 6 组 |
| L2-03 | `Highlights` 按 60 rune 截断且为最近 N 条 |
| L2-04 | `extractSeatRefs` 正确识别「3号」「seat=7」「第5位」三种形式 |
| L2-05 | `isPrivateSource` 覆盖全部 17 个 `InfoSource`（**无遗漏枚举**，§130 防线） |
| L2-06 | 公开来源已出现过的座位号**不**触发泄漏 |
| L2-07 | 仅私密来源出现的座位号被公开引用 → 触发一条 `InfoLeak` |
| L2-08 | 非知情者引用某座位号**不**触发（他不可能从该私密条目获知） |
| L2-09 | `detectLeaksLocked` 在账本 seq 未变时返回缓存（不重算） |
| L2-10 | `viewer >= 0` 时 `cs.InfoLeaks` 为空（§135 spectator-only） |
| L2-11 | `gc.KnowledgeDigest` 仅含本 seat 知情条目 |
| L2-12 | `KnowledgeDigestBlock` 在 digest 为 nil 时返回空串（不注入空块） |

---

## 4. 硬约束对照表

| 约束 | 本期如何满足 |
|---|---|
| **§92a 锁内变体** | `DigestForSeat` / `detectLeaksLocked` 全部 `*Locked` 语义，自身不加锁；调用点 `buildAgentContextLocked` / `BuildClientStateWithRoom` 均已持 `r.mu`。**新增方法一律先 grep 调用链上游**（R212 教训） |
| **§119 协议层隔离** | `KnowledgeDigest` 只进 GameContext→prompt；`InfoLeak` 只进 spectator ClientGameState。二者均**不**写 `chat_message` / `chat_history` / `BotTranscript` |
| **§128 对话即思考** | 不新增独立 LLM 调用、不新增工具；只在既有 user prompt 末尾追加一个只读块 |
| **§130 接线验证** | 每个新 helper 写完立即 grep 生产调用点；`isPrivateSource` 由 L2-05 穷举全部 17 个枚举值兜底 |
| **§135 身份公开公平性** | 账本 Fact 在写入侧已 `redactLedgerFact`；泄漏检测走**座位号指纹**而非角色词；`InfoLeaks` 严格 `viewer < 0` |
| **§111 内存模型** | 复用一期单源 `entries`（≤400 条）；digest / leak 均为**派生计算**，不新增 per-seat 存储 |
| **§121 数据形状** | `info_leaks` 是 `ClientGameState` 顶层数组（非 wrapper 对象），前端 `InfoLeakJSON[]` 直接对应 |
| **§4 行数上限** | `information_ledger.go` 311 → ~400 行；新建 `information_leak.go` ≤260 行；`werewolf-panels.css` 1003 → ~1113 行。全部 <1800 |
| **§26 对比度** | 6 色点 ≥4.5:1 + ≥8px + 1px 描边；新 className 写完 grep 验证 CSS 命中（§26.5） |
| **§197 长预算** | 本期不新增任何 LLM 调用，不涉及 |

---

## 5. 决策记录

- **D-01 为什么 digest 而非账本原文进 prompt**：全量 ~250 条 ≈ 15K token/轮/bot，7 bot × 每轮 = 105K token，成本不可接受且触发 §197 长响应风险。摘要方案压到 ~200 token/bot。
- **D-02 为什么泄漏检测用座位号指纹而非文本关键词**：一期 `redactLedgerFact` 已把角色词抹成 `▪`（§135 正确决策，不应回退），文本交叉失效；座位号是狼人杀信息的最小可识别单元，且天然不含身份。
- **D-03 为什么泄漏检测懒计算 + 缓存而非写入时增量**：写入路径（`appendRoomMessage`）是聊天热路径，每条发言 O(n²) 扫描会拖慢全房广播；观战者读取频率低且有 seq 缓存，代价可忽略。
- **D-04 为什么 `KnowledgeDigestBlock` 排在 `HypothesisTableBlock` 之前**：认知顺序「我知道什么」→「我据此猜谁是谁」，与 LLM 推理链自然顺序一致；且 prompt 末尾（最高注意力段）留给假说表更利于决策。
- **D-05 为什么泄漏检测只做观战者提示而不做惩罚**：座位号误报率高（「我觉得 5 号沉默」是纯公开观察），一旦用于扣分/quarantine 会造成大量误伤；且把检测结果回灌给 bot 会污染博弈（bot 会学会规避检测而非规避说漏嘴）。定位为**复盘线索**是唯一安全的落点。
- **D-06 为什么 `gc.KnowledgeDigest` 对全体 bot 开放而非仅狼人**：好人同样需要感知「我处于信息劣势」；且 `TotalInRoom` 对比是公平的（双方都知道全房总量，不知道对方具体知道什么）。

---

## 6. 三期展望（不在本期范围）

1. **账本反向驱动 prompt 组装**：把 `RecentSpeeches` / `WhisperInbox` / `WolfVotes` 等散落字段**全部**改从账本按 `KnowerSeats` 查询装配，彻底消除 D1 类缺陷的诞生土壤（本期只做「叠加 digest」，不动既有字段，降低回归风险）。
2. **泄漏检测升级为语义级**：接 LLM 做「这句公开发言是否引用了私密信息」的二元判定，取代座位号指纹的启发式。
3. **信息污染链（RumorGraph）**：DeepSeek §二.2 的谣言传播有向图，以本期账本为物理层地基。

---

## 7. 相关文档

- 一期：[`§20260810-05 信息账本一期`](信息账本一期-17个InfoSource后端落地.md)
- 假说表：[`§20260810-07 多假说并行推演`](多假说并行推演与信任矩阵.md)
- 承诺账本：[`§20260810-06 行为承诺与兑现追踪`](行为承诺与兑现追踪CommitmentLedger.md)
- 对话即思考：[`狼人杀对话即思考设计`](狼人杀对话即思考设计.md)
- 知识库：[`Agent-Surpport-01.md`](../../Agent-Surpport-01.md)
